node_modules
dist
# `bun build server.ts` output, produced in the image build.
server.js
# The default AI_GATEWAY_DATA_DIR — the credentials this gateway holds.
.data
storybook-static
.turbo
.vite
*.tsbuildinfo
test-results
playwright-report
.env
.env.local
