{
  "name": "@tale/search",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "setup": "echo 'Docker service - no local setup'",
    "serve": "docker compose up search -d && docker compose logs -f search",
    "build": "echo 'No build step'",
    "docker:build": "docker compose build search",
    "logs": "docker compose logs -f search"
  }
}
