# Apps — Manual Test Plan

> **Purpose**: Exercise the Apps marketplace (shipped #1911) — the Apps hub
> grid, an app's detail page (org-scoped views vs. the project-scoped
> membership hub vs. an Install prompt), the install/setup wizard, the
> lifecycle ⋯ menu (Reinstall / Uninstall / Remove from project), the
> readiness checklist, and the in-app run-detail (workflow DAG) page —
> including its project-nested twin.

## Scope & routes

Every route below is backed by a real route file under
`app/routes/dashboard/$id/**` (verified). `{org}` is the 16+ char org id in
the dashboard URL; `{slug}` is an app's directory slug (e.g. `issue-desk`);
`{projectId}` / `{executionId}` are ids.

| Surface                | Route                                                                                    |
| ---------------------- | ---------------------------------------------------------------------------------------- |
| Apps hub (grid)        | `/dashboard/{org}/apps`                                                                  |
| App detail             | `/dashboard/{org}/apps/{slug}`                                                           |
| App run detail (DAG)   | `/dashboard/{org}/apps/{slug}/runs/{executionId}?wf={workflowSlug}`                      |
| Project-scoped app     | `/dashboard/{org}/projects/{projectId}/apps/{slug}`                                      |
| Project-scoped app run | `/dashboard/{org}/projects/{projectId}/apps/{slug}/runs/{executionId}?wf={workflowSlug}` |

> **Route note**: there is **no** `/dashboard/{org}/projects/{projectId}/apps`
> index route — a project-scoped app is reached only via its `{slug}` (from the
> hub's membership list or the project nav). The run-detail route takes a
> `?wf=<workflowSlug>` search param; without it the DAG canvas is empty by
> design (`validateSearch` in the route file).

## Prerequisites

Bring the stack up and sign in per [SETUP.md](SETUP.md).

> **⚠️ Environment precondition — which catalog you see depends on
> `TALE_CONFIG_BUILTIN_DIR`** (verified live both ways):
>
> - The **SETUP.md bring-up leaves it unset**, so the real product catalog at
>   `builtin-configs/apps/` renders: the hub shows an installable **template
>   grid** (e.g. **Issue resolution desk**), and F4–F11, F14–F17, B3–B4 all run
>   in **mode A** — the wizard's GitHub step connects offline through the mock
>   gateway.
> - The **e2e suite's own webServer pins the variable to an empty fixture dir**
>   (`playwright.config.ts` — "dev default can't leak the real catalog into
>   hermetic test orgs"), so on THAT stack the hub renders **No apps yet**,
>   every detail page 404s, and the install surface is unreachable.
>
> The reference app slug used in URLs below is **`issue-desk`** (the product
> catalog bundle). Only cases needing real workflow runs or large collections
> (a populated DAG in F12, F18–F19) remain env-gated.

> **Agent note**: app routes never 500 — they degrade to an EmptyState. Verify
> by the visible EmptyState title (role/text), not by a toast. For any
> lifecycle write, verify by reload + read-back of the on-page state (badge /
> membership row / readiness item), never the transient success toast.

## Automated coverage

The Apps feature has **no dedicated e2e spec** (verified: `apps.spec.ts` does
not exist, and neither `page-loads.spec.ts` nor `navigation.spec.ts` visits an
`/apps` route). The whole surface is therefore manual-only — a real coverage
gap for a feature shipped in #1911.

| Case(s) | Status         | e2e spec |
| ------- | -------------- | -------- |
| F1–F13  | ⛔ manual-only | —        |
| F14–F19 | ⛔ manual-only | —        |
| B1–B4   | ⛔ manual-only | —        |

Legend: ✅ fully automated · 🔶 partially automated · ⛔ manual-only (no spec).

## Functional tests

| ID  | Test                         | Steps (route + control)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Expected (verifiable)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| --- | ---------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1  | Hub renders                  | `/dashboard/{org}/apps`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Header **Apps** (`apps.title`) with an **Upload app** button (`apps.upload.uploadApp`; shown only with the developer-settings capability). With the real builtin catalog the hub shows the installable **template grid** plus the **Search apps…** field (`apps.searchPlaceholder`). Only when the builtin dir is pinned empty AND nothing is uploaded: EmptyState **No apps yet** (`apps.empty.title`) + **Installed packs appear here as apps.** (`apps.empty.description`), and the search field is absent                                                     |
| F2  | App detail (uninstalled)     | `/dashboard/{org}/apps/issue-desk`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | With the real builtin catalog this renders the **details page** (scope line, What's included, Install — see F16), NOT a 404; the **App not found** EmptyState (`apps.notFound.title` + `apps.notFound.description`) appears only for a slug that exists nowhere (B1) or when the builtin dir is pinned empty; no console error                                                                                                                                                                                                                                    |
| F3  | Breadcrumb back-link         | On `/dashboard/{org}/apps/{slug}`, click **Apps** in the breadcrumb (the `<Link to /dashboard/$id/apps>`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | URL returns to `/dashboard/{org}/apps`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| F4  | Install (org-scoped)         | (mode A with the real builtin catalog — see the environment note) Hub → **Install** (`apps.install.install`) on a no-requirements org app                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Card badge flips to **Installed** (`apps.install.installed`); persists on reload                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| F5  | Install wizard (project app) | (mode A with the real builtin catalog — see the environment note) App detail of a project-scoped app → **Install** → wizard **Set up {name}** (`apps.installWizard.title`), observed live as **4 steps**: **Project** (step 1 of 4) → **Install** ("Ready to install… It uses these integrations: GitHub.") → **{integration}** (connect fields + **I'll do this later**) → **Done** → **Finish** (`apps.installWizard.finish`). A readiness continuation (**Finish setup** → **Connect**) adds an **Agent mode** step (per-agent **Managed** vs **Bring your own key**)                                                                                                                                                                                                                                                                                                                                                                                               | Navigates to `/dashboard/{org}/projects/{projectId}/apps/{slug}`; app bound to that project (membership row persists on reload)                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| F6  | Org views (org-scoped app)   | (mode A with the real builtin catalog — see the environment note) `/dashboard/{org}/apps/{slug}` for an installed org-scoped app                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | The app's views render (tabbed shell or flat); a lifecycle ⋯ menu (`apps.install.menuLabel`) sits on the first view                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| F7  | Membership hub (project app) | (mode A with the real builtin catalog — see the environment note) `/dashboard/{org}/apps/{slug}` for an installed project-scoped app                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Projects** section (`apps.membership.title`); **Add to a project** (`apps.membership.addProject`); each bound project links to `…/projects/{projectId}/apps/{slug}`                                                                                                                                                                                                                                                                                                                                                                                             |
| F8  | Add to a project             | (mode A with the real builtin catalog — see the environment note) Membership hub → **Add to a project** → wizard project step → **Finish**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | New membership row appears and persists on reload                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| F9  | Readiness checklist          | (mode A with the real builtin catalog — see the environment note) Installed app whose required integration is unconnected                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Warning Alert **Finish setup** (`apps.readiness.title`) with **Connect** (`apps.readiness.connectButton`) per missing integration                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| F10 | Reinstall                    | (mode A with the real builtin catalog — see the environment note) ⋯ menu → **Reinstall** (`apps.install.reinstall`) → confirm dialog **Reinstall app** (`apps.install.reinstallTitle`) → **Reinstall**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Toast **App reinstalled** (`apps.install.reinstalled`); app still installed (badge unchanged on reload)                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| F11 | Remove from project          | (mode A with the real builtin catalog — see the environment note) Inside a bound project: ⋯ → **Remove from project** (`apps.install.removeFromProject`) → confirm **Remove from this project?** (`apps.install.removeFromProjectTitle`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Membership row gone on reload; org-level install + other projects unaffected                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| F12 | Run detail (DAG)             | `/dashboard/{org}/apps/{slug}/runs/{executionId}?wf={workflowSlug}`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **← Back to app** link — visible label **Back to app** (`apps.runs.backToApp`) — returns to `/dashboard/{org}/apps/{slug}`; with an installed workflow the DAG renders, else **This workflow has no steps.** (`apps.workflow.none`)                                                                                                                                                                                                                                                                                                                               |
| F13 | Project-scoped app route     | `/dashboard/{org}/projects/{projectId}/apps/issue-desk`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Renders **inside the project shell** (project tab strip visible). Bound + installed → app views; not bound → **Not added to this project** (`apps.membership.notInProjectTitle`); unknown app → **App not found**                                                                                                                                                                                                                                                                                                                                                 |
| F14 | Private-app upload           | (mode A) Prep — a verbatim zip of `builtin-configs/apps/issue-desk` is **rejected** ("issue-desk" is a built-in app — reserved slug, `APP_SLUG_RESERVED`). Build the sample by copying it to a renamed folder (e.g. `issue-desk-qa/`), re-scoping the workflow ids/paths inside to the new slug (the uploader validates `workflows/<slug>/…` scoping), and zipping so the archive contains that **single top-level folder** (a flat zip is rejected: "Bundle must contain a single top-level folder named after the app"). `/dashboard/{org}/apps` → **Upload app** (`apps.upload.uploadApp`) → dialog **Upload private app** (`apps.upload.dialogTitle`) → drop the `.zip` on **Upload app bundle** (`apps.upload.dropZoneLabel`) or click **Select folder** (`apps.upload.selectFolder`) → **Upload app** (`apps.upload.submit`). Then re-upload the same slug → **Replace existing app?** (`apps.upload.replaceTitle`) → **Replace** (`apps.upload.replaceConfirm`) | Preview shows scope badge **Project app** / **Org app** (`apps.upload.scopeProject` / `apps.upload.scopeOrg`) and **Workflows** / **Agents** / **Requires integrations** / **Bundle files** counts (`apps.upload.workflows`, `apps.upload.agents`, `apps.upload.requiresIntegrations`, `apps.upload.bundleFiles`; file count via `apps.upload.fileCount`). Submit → toast **App uploaded** (`apps.upload.uploadSuccess`) + hint (`apps.upload.installHint`); after reload the app card is on the hub. Re-upload → **App replaced** (`apps.upload.replaceSuccess`) |
| F15 | Hub search                   | (mode A, after F14) `/dashboard/{org}/apps` → type into **Search apps…** (`apps.searchPlaceholder`) a matching term, then a non-matching one                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Matching term narrows the grid to matching cards; non-matching term → EmptyState **No apps match your search.** (`apps.searchNoResults`). Scope (verified live): the search covers builtin templates AND uploaded apps, matching **name and description — not the slug**                                                                                                                                                                                                                                                                                          |
| F16 | App details page             | (mode A, after F14) `/dashboard/{org}/apps/issue-desk` for an uploaded-but-not-installed app                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | No longer only the not-found state: scope line **Project app** / **Organization app** (`apps.details.scopeProject` / `apps.details.scopeOrg`); **What's included** (`apps.details.includesTitle`) listing **Pages** / **Workflows** / **Agents** (`apps.details.pages` / `apps.details.workflows` / `apps.details.agents`); **What you'll need** (`apps.details.requiresTitle`) with hint **You'll connect these during setup.** (`apps.details.requiresHint`); an **Install** button (`apps.install.install`)                                                    |
| F17 | Per-project config scoping   | (mode A — verified live: the wizard's GitHub step connects offline via the mock gateway) Install the project-scoped app into TWO projects (wizard, F5 path) → in each project's app page open **Configure** (`apps.config.configure`) → **Configuration** (`apps.config.title`) → set different values (e.g. repository) → **Save** (`apps.config.save`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Toast **Configuration saved** (`apps.config.saved`); reload each project — the values are independent per project (verified live). An unconfigured project shows **Configuration needed** (`apps.config.setupTitle`) — note the banner currently does NOT clear after a successful save (see Issues #6)                                                                                                                                                                                                                                                           |
| F18 | Collection pagination        | (env-gated — not hermetically verified: needs an app view with a Collection block backed by more rows than one page) Scroll to the Collection block → **Load more** (`apps.list.loadMore`; busy label **Loading…**, `apps.list.loadingMore`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Clicking **Load more** appends the next page of rows (config-driven perPage, default 50). Unconnected integration shows **This integration isn't connected yet. Connect it to load data.** (`apps.list.notConnected`); unconfigured app shows **Configure this app to load data.** (`apps.list.needsConfig`)                                                                                                                                                                                                                                                      |
| F19 | Run-state row chips          | (env-gated — needs a real run parked behind the sandbox cap; pair with governance.md F13 with the quota set to 1) Open a Collection whose row's latest run is parked; separately, one whose latest run failed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Parked row shows an orange **Queued for capacity** badge (`apps.runs.queuedForCapacity`) with tooltip (`apps.runs.queuedForCapacityHint`); failed row shows **Failed** (`apps.runs.failed`) with tooltip (`apps.runs.failedHint`)                                                                                                                                                                                                                                                                                                                                 |

## Boundary & error tests

| ID  | Test                      | Input                                                                                                                                                                                                                              | Expected                                                                                                           |
| --- | ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| B1  | Unknown app slug          | `/dashboard/{org}/apps/does-not-exist`                                                                                                                                                                                             | EmptyState **App not found** (`apps.notFound.title`); HTTP 200, no error boundary, no console error (verified)     |
| B2  | Run detail without `wf`   | `/dashboard/{org}/apps/{slug}/runs/{executionId}` (no `?wf=`)                                                                                                                                                                      | Page renders with the **Back to app** link and an empty DAG region — no crash (verified)                           |
| B3  | Uninstall blocked by bind | (mode A with the real builtin catalog — see the environment note) ⋯ → **Uninstall** while ≥1 project still has the app bound                                                                                                       | Blocked up front with toast **Remove the app from its {count} project(s)…** (`apps.install.uninstallBlockedCount`) |
| B4  | Uninstall confirm         | (mode A with the real builtin catalog — see the environment note) ⋯ → **Uninstall** (no bindings) → delete dialog **Uninstall app** (`apps.install.uninstallTitle`) with warning (`apps.install.uninstallWarning`) → **Uninstall** | App removed from the org; hub shows the empty state again on reload                                                |

## Accessibility (WCAG 2.1 AA)

| ID  | Check                | Expected                                                                                                                    |
| --- | -------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| A1  | Hub cards            | Each card is a link with an accessible name = the app name (`aria-label={app.name}`)                                        |
| A2  | Lifecycle ⋯ menu     | Trigger has an accessible name **Manage {name}** (`apps.install.menuLabel`); keyboard-operable                              |
| A3  | Install wizard steps | Progress is a labelled region **Setup steps** (`apps.installWizard.stepsAriaLabel`); Back/Next/Finish reachable by keyboard |
| A4  | Breadcrumb back-link | The breadcrumb **Apps** segment is a focusable link with a visible focus ring                                               |

## Performance

| ID  | Metric            | Target                                                                                 |
| --- | ----------------- | -------------------------------------------------------------------------------------- |
| P1  | Hub first render  | EmptyState / grid visible < 1.5 s after navigation (mode A, local self-hosted backend) |
| P2  | App detail render | Detail content (views / not-found / hub) visible < 1.5 s (mode A, local backend)       |
| P3  | Run-detail open   | Back-link + DAG region visible < 1.5 s (mode A, local backend)                         |

## Issues Found

| #   | Test ID | Route / URL                                   | Severity | Description                                                                                                                                                                                                                                                                                                                                                                                                            | Screenshot                                                       |
| --- | ------- | --------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- |
| 1   | F12     | `/dashboard/{org}/apps/{slug}/runs/{id}?wf=…` | low      | The "Back to app" link has no i18n key — the route uses `t('runs.backToApp', { defaultValue: 'Back to app' })` but `apps.runs.backToApp` is MISSING; it won't localize. **RESOLVED:** the key now exists in `en.json` ("Back to app") — asserted in F12.                                                                                                                                                               | `apps/S5-run-withWf.png`                                         |
| 2   | F1/F2   | `/dashboard/{org}/apps`, `/apps/{slug}`       | med      | No in-UI app catalog/discovery surface. **RESOLVED:** the hub now renders the builtin template grid (installable) plus the **Upload app** entry point — discovery and install both exist in-UI.                                                                                                                                                                                                                        | `apps/S1-hub.png`, `apps/S2-appdetail.png`                       |
| 3   | all     | all apps routes                               | low      | Coverage gap: the Apps marketplace ships with zero e2e specs.                                                                                                                                                                                                                                                                                                                                                          | —                                                                |
| 4   | F5/F17  | install wizard                                | med      | The first install wizard (from the details **Install**) declares 4 steps but closes silently after step 2 (Install), never showing its integration/Done steps — setup continues only via the **Finish setup** alert. The **Add to a project** wizard continues past Install normally; the two flows are inconsistent.                                                                                                  | `defect-f17-wizard-closes-before-github-step.png`                |
| 5   | F5/F17  | readiness wizard (Agent mode)                 | med      | Selecting **Managed** for one agent (Desk Implementer) does not persist: after Finish ("Everything it needs is connected") the page still shows "…needs setup to run." and reopening the wizard shows it reverted to **Bring your own key**; the other agent's Managed choice persisted. Reproduced twice.                                                                                                             | `defect-f17-desk-implementer-managed-mode-not-persisted.png`     |
| 6   | F17     | `…/projects/{projectId}/apps/{slug}`          | low      | The **Configuration needed** banner does not clear after a successful config save + reload, even while the app's data panels start loading with the saved values. **RESOLVED:** the banner now tracks only REQUIRED config fields — a field marked `optional` (issue-desk's `testCommand`/`repoNotes`) no longer holds it open, so setting the repository clears it (regression-tested in `app-page.test.tsx`, #2349). | `defect-f17-configuration-needed-banner-persists-after-save.png` |
| 7   | F14     | `/dashboard/{org}/apps`                       | low      | Uploaded private apps cannot be deleted from the UI (only Install / Reinstall / Uninstall exist), and the hub shows an uploaded copy and a builtin with identical display names with no uploaded-vs-builtin badge.                                                                                                                                                                                                     | —                                                                |

## Test summary

```
Area: Apps
Functional: ___/19   Boundary: ___/4   A11y: ___/4   Perf: ___/3
Issues: 7 logged, 3 resolved (crit 0 / high 0 / med 2 / low 2 open)
Status: PASS / FAIL
```
