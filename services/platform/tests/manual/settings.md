# Settings — Manual Test Plan

> **Purpose**: Exercise the settings surface along its real rail — **Personal**
> (Account, Preferences, Notifications), **Organization**
> (Organization, Teams, Members, AI providers, Connectors, Skills, Branding,
> Sandboxes, Governance, Metrics) and **Advanced** (API: REST / MCP / WebDAV,
> Enterprise SSO, Data residency). Governance has its own guide
> ([governance.md](governance.md)); data-residency depth lives in
> [data-residency.md](data-residency.md) (one route case here); connector
> depth (per-type credentials, OAuth, imap-smtp) lives in
> [connectors.md](connectors.md); 2FA/passkeys/sessions live in
> [auth.md](auth.md). The token-sources page and the per-provider detail page
> were retired — both providers and connectors are now one credential table
> each, and metrics gets one smoke case (a dedicated metrics guide lands
> later).

## Scope & routes

The settings rail (`navigation.userSettings`) groups pages as **Personal** /
**Organization** / **Advanced** (`settings.menu.railSections.personal` /
`…railSections.organization` / `…railSections.advanced`). The rail label for
the `/settings/personalization` route is **Preferences**
(`navigation.personalization`). Governance, Metrics, and API are expandable
disclosure rows whose children render indented.

| Surface                    | Route                                                                      |
| -------------------------- | -------------------------------------------------------------------------- |
| Settings index (workspace) | `/dashboard/{org}/settings`                                                |
| Personal overview (mobile) | `/dashboard/{org}/settings/personal`                                       |
| Account                    | `/dashboard/{org}/settings/account`                                        |
| Preferences                | `/dashboard/{org}/settings/personalization`                                |
| Notifications              | `/dashboard/{org}/settings/notifications`                                  |
| Organization               | `/dashboard/{org}/settings/organization`                                   |
| Teams                      | `/dashboard/{org}/settings/teams`                                          |
| Members                    | `/dashboard/{org}/settings/members`                                        |
| AI providers               | `/dashboard/{org}/settings/providers` · `…?provider=<slug>` opens filtered |
| Connectors                 | `/dashboard/{org}/settings/connectors`                                     |
| Skills                     | `/dashboard/{org}/settings/skills`                                         |
| Branding                   | `/dashboard/{org}/settings/branding`                                       |
| Sandboxes                  | `/dashboard/{org}/settings/sandboxes`                                      |
| Metrics (smoke only)       | `/dashboard/{org}/settings/metrics/usage` (index redirects here)           |
| API → REST (API keys)      | `/dashboard/{org}/settings/api/rest` (`…/settings/api` redirects here)     |
| API → MCP                  | `/dashboard/{org}/settings/api/mcp`                                        |
| API → WebDAV               | `/dashboard/{org}/settings/api/webdav`                                     |
| Enterprise SSO             | `/dashboard/{org}/settings/enterprise-sso`                                 |
| Data residency             | `/dashboard/{org}/settings/data-residency`                                 |

**Legacy redirects** (author against the targets, but B1–B3 assert the
bounces): `/dashboard/{org}/settings/api-keys` and
`/dashboard/{org}/settings/api/runtimes` → `…/api/rest`;
`/dashboard/{org}/settings/webdav` → `…/api/webdav`;
`/dashboard/{org}/settings/mcp` and `/dashboard/{org}/settings/mcp-servers` →
`…/connectors`; `/dashboard/{org}/settings/people` → `…/members`;
`/dashboard/{org}/settings/logs` → `…/governance/logs`;
`/dashboard/{org}/settings/deployment` → `…/data-residency`;
`/dashboard/{org}/settings/providers/{providerName}` → the providers index
with `?provider=<slug>` applied.

## Prerequisites

Bring the stack up and sign in per [SETUP.md](SETUP.md) as an **owner**. Role
gating (from `lib/permissions/ability.ts`): the Organization-group pages plus
Enterprise SSO, Data residency, Governance, and Metrics need `orgSettings`
read (**owner/admin**); Providers, Connectors, Sandboxes, and the API section
need `developerSettings` read (**owner/admin/developer**); Skills and the
Personal group are open to every member. B4–B5 need a second account per role
(mint via `POST /api/auth/sign-up/email`, add under Settings → Members); F16
needs a second **already-existing** account. Mode A makes the connector
add-credential smoke (F25) deterministic. Restore any toggled setting after
the run.

> **Agent note**: pages on the header **Save** / **Discard** cluster
> (`common.actions.save` / `common.actions.discard`) — Account profile,
> Preferences custom-instructions text, Organization, Branding,
> Enterprise SSO, Data residency — flash **Saved** (`common.actions.saved`)
> and settle back to a disabled Save; there is no page toast. Everything else
> saves through its own dialog, row action, or on-flip toggle. Verify every
> persisted write by reloading and reading the value back, never by a
> transient toast. On desktop a bare `/settings` hit client-side-redirects to
> a permission-appropriate leaf — a momentarily blank main panel there is
> expected, not a render error. Destructive flows marked "cancel-only" are
> opened and dismissed, never confirmed.

## Automated coverage

| Case(s)                                                    | Status         | e2e spec                                                                                         |
| ---------------------------------------------------------- | -------------- | ------------------------------------------------------------------------------------------------ |
| F3 (account name)                                          | ✅ automated   | `settings.spec.ts`                                                                               |
| F12 (org rename)                                           | ✅ automated   | `settings-depth.spec.ts`                                                                         |
| F18 (teams create/delete)                                  | ✅ automated   | `settings-depth.spec.ts`                                                                         |
| F32 (API key lifecycle)                                    | ✅ automated   | `settings-depth.spec.ts`                                                                         |
| F11 (theme & language)                                     | ✅ automated   | `preferences.spec.ts` (Manage-account menu)                                                      |
| F6 (preference toggles)                                    | 🔶 partial     | `settings-depth.spec.ts` (custom-instructions toggle only; memories + org-default hint manual)   |
| F28 (branding)                                             | 🔶 partial     | `settings-depth.spec.ts` (accent color only; logo/favicon/reset manual)                          |
| F20–F21 (providers page + add)                             | 🔶 partial     | `settings.spec.ts` (empty state + add-catalog picker; no credential is actually created)         |
| F24 (connectors page)                                      | 🔶 partial     | `settings.spec.ts` (empty state + add-catalog picker)                                            |
| F14–F15 (members)                                          | 🔶 partial     | `rbac.spec.ts` (a member cannot see the add-member control; the add flows themselves are manual) |
| F1 (rail)                                                  | 🔶 component   | — (no e2e; `settings-rail.test.tsx`)                                                             |
| F9 (notification prefs)                                    | 🔶 component   | — (no e2e; `notification-preferences-settings.test.tsx`)                                         |
| F16–F17 (member dialogs)                                   | 🔶 component   | — (no e2e; `member-add-dialog.test.tsx`, `member-row-actions.test.tsx`, `member-table.test.tsx`) |
| F33 (MCP endpoint)                                         | 🔶 component   | — (no e2e; `mcp-endpoint-section.test.tsx`)                                                      |
| F35 (Enterprise SSO)                                       | 🔶 component   | — (no e2e; `enterprise-sso-form.test.tsx`)                                                       |
| F36 (data residency)                                       | 🔶 component   | — (no e2e; `data-residency-settings.test.tsx`)                                                   |
| F2, F4–F5, F7–F8, F13, F19, F22–F23, F25–F27, F29–F31, F34 | ⛔ manual-only | —                                                                                                |
| B1–B10, A1–A5, P1–P3                                       | ⛔ manual-only | —                                                                                                |

Legend: ✅ fully automated · 🔶 partially automated / component test only ·
⛔ manual-only (no spec).

## Functional tests

| ID  | Test                              | Steps (route + control)                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Expected (verifiable)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --- | --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1  | Rail structure & index redirect   | Desktop: open `/dashboard/{org}/settings` as owner                                                                                                                                                                                                                                                                                                                                                                                                                              | The URL is replaced by a permission-appropriate leaf; the rail shows the three section headers **Personal** / **Organization** / **Advanced** (`settings.menu.railSections.*`); the personalization row is labelled **Preferences** (`navigation.personalization`); **Governance**, **Metrics**, and **API** are disclosure rows (chevron, `aria-expanded`) whose children indent when open; the active row is visually marked                                                                                                                                                                                   |
| F2  | Mobile settings overviews         | Narrow viewport (or emulate mobile): open `/dashboard/{org}/settings`, then `/dashboard/{org}/settings/personal`                                                                                                                                                                                                                                                                                                                                                                | The workspace overview lists grouped entries — **You** (`settings.menu.groups.you`), **Organization** (`settings.menu.groups.organization`), **Advanced** (`settings.menu.groups.advanced`) — each with its description (e.g. `settings.menu.account.description`); `/settings/personal` shows only the **You** group; on desktop `/settings/personal` bounces to `…/settings/account`                                                                                                                                                                                                                           |
| F3  | Account profile name              | `/dashboard/{org}/settings/account` → **Name** field (`settings.account.profile.name`) → edit → header **Save** (`common.actions.save`)                                                                                                                                                                                                                                                                                                                                         | The Save cluster flashes **Saved** (`common.actions.saved`) and disables again — no page toast; the new value is still in the Name field after a reload; the **Email** field (`settings.account.profile.email`) is read-only                                                                                                                                                                                                                                                                                                                                                                                     |
| F4  | Account security                  | Same page → **Security** section (`settings.account.security.title`) → open the change-password dialog (`auth.changePassword.title`)                                                                                                                                                                                                                                                                                                                                            | The dialog opens with current/new/confirm fields and enforces the password policy (`auth.validation.passwordMinLength` etc.); 2FA, passkeys, and session depth are covered in [auth.md](auth.md) — here only confirm the sections render                                                                                                                                                                                                                                                                                                                                                                         |
| F5  | Account bulk chat actions         | Same page → **Your chats** section (`settings.account.chats.title`) → click **Archive all chats** (`settings.account.chats.archiveAll`), read the dialog, **cancel**; same for **Delete all chats** (`settings.account.chats.deleteAll`) — **cancel-only**                                                                                                                                                                                                                      | Each button opens a destructive confirm dialog (shared dialog copy — the per-dialog keys were consolidated in #2897) stating the consequence before anything happens; cancelling changes nothing (chat list unchanged after reload)                                                                                                                                                                                                                                                                                                                                                                              |
| F6  | Preference toggles                | `/dashboard/{org}/settings/personalization` (heading **User preferences**, `personalization.page.title`) → flip the **Custom instructions** toggle (`personalization.page.customInstructionsToggle.label`) and the **Memories** toggle (`personalization.page.memoriesToggle.label`)                                                                                                                                                                                            | Each flip saves immediately — toast **Preferences updated.** (`personalization.toasts.preferencesUpdated`) — and persists across reload; each section's description carries the org-default hint (`personalization.page.enable.followingOrgDefault` / `…overridingOrgDefault`); theme and language are **not** on this page (F11)                                                                                                                                                                                                                                                                                |
| F7  | Custom instructions text          | With the toggle on, type into the instructions field (`personalization.page.customInstructions.placeholder`) → header **Save**                                                                                                                                                                                                                                                                                                                                                  | The field only exists while the toggle is on; saving runs through the header cluster (Saved flash, no toast); the text survives a reload; the field shows a character counter and text over the limit shows the inline error (`personalization.errors.tooLong`)                                                                                                                                                                                                                                                                                                                                                  |
| F8  | Memories & pending sections       | Same page → **Saved memories** (`personalization.page.memories.title`) and **Pending suggestions** (`personalization.page.pending.title`)                                                                                                                                                                                                                                                                                                                                       | With no data both render their empty lines (`personalization.page.memories.empty` / `personalization.page.pending.empty`); with the chat backend down the memories area shows **Chat isn't connected yet** (`chat.backendUnavailable.title`) instead of crashing                                                                                                                                                                                                                                                                                                                                                 |
| F9  | Notification preferences          | `/dashboard/{org}/settings/notifications` → flip e.g. **Task assigned to me** (`notificationPreferences.fields.taskAssigned.label`); inspect **Review requests** (`notificationPreferences.fields.taskReview.label`)                                                                                                                                                                                                                                                            | Flips save silently (no Save bar, no success toast — a failure raises `notificationPreferences.saveFailed`); the flipped state persists across reload; the review-requests toggle is locked on with the hint (`notificationPreferences.fields.taskReview.lockedHint`); the **Email delivery** section (`notificationPreferences.deliveryTitle`) carries the **Email me actionable alerts** toggle (`notificationPreferences.fields.actionableEmail.label`)                                                                                                                                                       |
| F11 | Theme & language (Manage account) | Click the **Manage account** trigger (top-right user icon, `auth.userButton.manageAccount`) → theme tabs (`auth.userButton.themeSystem` / `…themeLight` / `…themeDark`); **Language** submenu (`auth.userButton.language`) → pick EN / DE / FR                                                                                                                                                                                                                                  | The chosen theme applies immediately (the `html` element gains/loses the `dark` class) and survives reload; the chosen language re-renders the UI strings and survives reload — neither lives on a settings page                                                                                                                                                                                                                                                                                                                                                                                                 |
| F12 | Organization details              | `/dashboard/{org}/settings/organization` → **Organization name** (`settings.organization.organizationName`) → edit → header **Save**; check **Default language (for agents)** (`settings.organization.defaultLocale`) and **Organization ID** (`settings.organization.organizationId`) with its copy button (`settings.organization.copyOrganizationId`)                                                                                                                        | Saved flash, no toast; the new name survives reload; the locale select persists its pick the same way; the org ID is read-only and its copy button fills the clipboard                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| F13 | Org danger zone                   | Same page → **Danger zone** (`settings.organization.dangerZoneTitle`) → **Delete organization** (`settings.organization.deleteConfirmAction`) — **cancel-only**                                                                                                                                                                                                                                                                                                                 | The confirm dialog (`settings.organization.deleteDialogTitle`) names the org and warns the deletion is irreversible (`settings.organization.deleteDialogDescription`); cancelling changes nothing                                                                                                                                                                                                                                                                                                                                                                                                                |
| F14 | Members table                     | `/dashboard/{org}/settings/members`                                                                                                                                                                                                                                                                                                                                                                                                                                             | The members table renders under the description (`settings.organization.membersDescription`) with the current owner row; search (`settings.organization.searchMember`) narrows it; roles render from `settings.roles.*` (Owner/Admin/Developer/Editor/Member)                                                                                                                                                                                                                                                                                                                                                    |
| F15 | Add member (new account)          | Same page → **Add member** (`settings.organization.addMember`) → fill **Name** / **Email** / **Password** (`settings.form.name` / `settings.form.email` / `settings.form.password`) + **Role** (`settings.form.role`) → submit (**Add member**, `dialogs.addMember.title`)                                                                                                                                                                                                      | Toast **Member added** (`toast.success.newMemberCreated.title`); the result dialog (`dialogs.memberAdded.title`) shows the credentials block with the save-now warning (`dialogs.memberAdded.credentialsWarning`); after reload the new member row is in the table                                                                                                                                                                                                                                                                                                                                               |
| F16 | Add existing user hides password  | Needs a second existing account. Same dialog → enter the **existing** account's email                                                                                                                                                                                                                                                                                                                                                                                           | The password field is replaced by the existing-user hint (`dialogs.addMember.existingUserHint`); on submit the toast is **Member added** (`toast.success.existingUserAdded.title`) and the result dialog shows the existing-user notice (`dialogs.memberAdded.existingUserNotice`) with **no** credentials block                                                                                                                                                                                                                                                                                                 |
| F17 | Member row actions                | A member row's action menu → **Edit member** (`settings.organization.editMember`) → change the role → save; open **Transfer ownership** (`settings.organization.transferOwnership`) — **cancel-only**; **Remove member** (`settings.organization.removeMember`) on a throwaway member                                                                                                                                                                                           | Role change toasts (`settings.organization.memberUpdated`) and survives reload; your own role is not editable (`settings.organization.cannotChangeOwnRole`); the transfer confirm (`settings.organization.transferOwnershipConfirmation`) warns you will be demoted — cancel; removing toasts (`settings.organization.memberRemoved`) and the row is gone after reload                                                                                                                                                                                                                                           |
| F18 | Teams create & delete             | `/dashboard/{org}/settings/teams` → **Create team** (`settings.teams.createTeam`) → **Team name** (`settings.teams.teamName`) → create; then the row's **Delete team** (`settings.teams.deleteTeam`) → confirm                                                                                                                                                                                                                                                                  | Toast `settings.teams.teamCreated`; the row (columns `settings.teams.columns.name` / `…columns.members` / `…columns.created`) is present after reload; the delete confirm (`settings.teams.deleteConfirmation`) warns members are removed; after delete + reload the row is gone (toast `settings.teams.teamDeleted`)                                                                                                                                                                                                                                                                                            |
| F19 | Team member management            | A team row → **Edit team** (`settings.teams.editTeam`) → **Members** (`settings.teams.manageMembers`) → add/remove members → **Save changes** (`settings.teams.saveChanges`)                                                                                                                                                                                                                                                                                                    | The member checklist explains multi-team membership (`settings.teams.memberChecklistHint`); a team never drops to zero members — the last one is blocked with the hint (`settings.teams.lastMemberHint`); the member count survives reload                                                                                                                                                                                                                                                                                                                                                                       |
| F20 | Providers page                    | `/dashboard/{org}/settings/providers`                                                                                                                                                                                                                                                                                                                                                                                                                                           | The **Credentials** section (`settings.providers.credentialsSection.title`) renders the credential table (or the empty state `emptyStates.providers.title` on a fresh org) with **Add credential** (`settings.credentials.addCredential`); the header offers **Refresh catalogs** (`settings.providers.catalogs.refresh`) whose result dialog lists per-catalog model counts (`settings.providers.catalogs.refreshed`); the **Harnesses** section (`settings.providers.harnesses.title`) renders below                                                                                                           |
| F21 | Add a provider credential         | **Add credential** → the catalog pane (`settings.credentials.catalog.title`) lists configured providers first with a **Configured** badge (`settings.credentials.catalog.configured`), then the rest alphabetically → pick a vendor → fill **Name** (`settings.credentials.name`) + **API key** (`settings.providers.dialog.secret`), optionally narrow the **Model allowlist** (`settings.providers.dialog.allowlist`) → **Add credential** (`settings.credentials.create`)    | Toast **Credential added** (`settings.credentials.createdToast`); after reload the row is in the table with the vendor column (`settings.providers.vendorColumn`), the auth method (`settings.credentials.columns.method`), and — as the vendor's first credential — the **Default** badge (`settings.credentials.default`); the stored secret is never echoed back                                                                                                                                                                                                                                              |
| F22 | Credential row actions            | A credential row's action menu (`settings.credentials.actionsLabel`) → **Make default** (`settings.credentials.makeDefault`) on a second credential; **Disable** / **Enable** (`settings.credentials.disable` / `…enable`); **Edit credential** (`settings.credentials.edit`) → rename → save; the replace-secret entry (`settings.providers.replace.apiKeyTitle`); **Delete** (`settings.credentials.delete`)                                                                  | Each action survives reload: the Default badge moves, a disabled row shows **Disabled** (`settings.providers.credential.disabled`), the rename toasts `settings.credentials.savedToast`; the replace dialog states the stored secret is never shown; deleting confirms (`settings.credentials.deleteTitle`) — deleting the default adds the warning (`settings.credentials.deleteDefaultWarning`) — then toasts `settings.credentials.deletedToast` and the row is gone; with no default left for a vendor the table shows the no-default notice (`settings.credentials.noDefault`)                              |
| F23 | Vendor filter & search            | On providers: the **Provider** filter (`settings.providers.vendorFilterLabel`) and the search field (`settings.credentials.searchPlaceholder`); on connectors the same filter is labelled **Connector** (`settings.connectors.vendorFilterLabel`)                                                                                                                                                                                                                               | Filtering by vendor narrows the rows to that vendor; searching narrows by credential name; clearing restores the full table                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| F24 | Connectors page                   | `/dashboard/{org}/settings/connectors`                                                                                                                                                                                                                                                                                                                                                                                                                                          | The credential table renders under the description (`settings.connectors.sectionDescription`) — or the empty state (`emptyStates.connectors.title`) on a fresh org — with **Add credential** opening the same two-step catalog picker; connector auth methods render from `settings.connectors.authMethod.*` (API key / Token / Username & password / OAuth)                                                                                                                                                                                                                                                     |
| F25 | Connector add-credential smoke    | Mode A. **Add credential** → pick an API-key connector (e.g. Tavily) → fill the key (`settings.connectors.dialog.apiKey`) → create                                                                                                                                                                                                                                                                                                                                              | The connect test runs against the mock gateway and succeeds offline; toast `settings.credentials.createdToast` and the row is present after reload. Per-connector depth (OAuth consent, imap-smtp split SMTP, reconnect) is [connectors.md](connectors.md)'s job                                                                                                                                                                                                                                                                                                                                                 |
| F26 | Skills table smoke                | `/dashboard/{org}/settings/skills`                                                                                                                                                                                                                                                                                                                                                                                                                                              | The table renders (any member may see it) with columns from `skills.columns.*` (Name / Description / Visibility / Usage / Labels), visibility badges (`skills.visibility.org` / `…team` / `…private`), and search (`skills.searchPlaceholder`); a fresh org shows the seeded builtin skills or the empty state (`emptyStates.skills.title`)                                                                                                                                                                                                                                                                      |
| F27 | Skills add-menu smoke             | **Add skill** (`skills.addMenu.label`) → **Blank skill** (`skills.createMenu.blank`)                                                                                                                                                                                                                                                                                                                                                                                            | The menu offers **Blank skill** / **Upload zip** / **Upload folder** (`skills.createMenu.uploadZip` / `…uploadFolder`); the create dialog (`skills.createDialog.title`) opens with slug-pattern validation (`skills.createDialog.namePatternError`); dismiss with Escape — authoring/upload depth belongs to a future skills guide                                                                                                                                                                                                                                                                               |
| F28 | Branding accent color             | `/dashboard/{org}/settings/branding` → **Accent color** (`settings.branding.accentColor`) → pick/enter a hex → header **Save**                                                                                                                                                                                                                                                                                                                                                  | The **Branding preview** (`settings.branding.preview`) re-tints live while editing; Saved flash, no toast; the hex survives reload; primary buttons and the sidebar highlight pick up the accent in light **and** dark themes                                                                                                                                                                                                                                                                                                                                                                                    |
| F29 | Branding images & reset           | Same page → **Upload logo** (`settings.branding.uploadLogo`) / **Upload favicon** (`settings.branding.uploadFavicon`); then the header's **Reset** action (`common.actions.reset`) — **cancel-only**                                                                                                                                                                                                                                                                            | Image uploads save immediately with their own toasts (they do not pass through the Save cluster); the uploaded logo appears in the preview and survives reload; the reset confirm (`settings.branding.resetConfirmTitle`) warns removed images can't be restored — cancel                                                                                                                                                                                                                                                                                                                                        |
| F30 | Sandboxes page                    | `/dashboard/{org}/settings/sandboxes`                                                                                                                                                                                                                                                                                                                                                                                                                                           | A fresh org shows **No active sandboxes** (`sandboxes.empty.title`); with a live sandbox session (env-gated) the table renders columns `sandboxes.columns.owner` / `…agent` / `…status` / `…task` / `…spend` and row actions **Stop** / **Pin** / **Destroy** (`sandboxes.actions.stop` / `…pin` / `…destroy`; Destroy confirms via `sandboxes.destroyConfirm.title`) — a monitoring surface, nothing to save                                                                                                                                                                                                    |
| F31 | Metrics smoke                     | Expand the rail's **Metrics** group; open `/dashboard/{org}/settings/metrics`                                                                                                                                                                                                                                                                                                                                                                                                   | The group lists six children from `metrics.groups.*` (Usage / Feedback / Chat health / Harness turns / Automations / Projects); the index redirects to `…/settings/metrics/usage`, which renders the period picker (`metrics.period.label`) and content without console errors — full coverage arrives with a dedicated metrics guide                                                                                                                                                                                                                                                                            |
| F32 | API keys lifecycle                | `/dashboard/{org}/settings/api/rest` → **Create API key** (`settings.apiKeys.createKey`) → **Key name** (`settings.apiKeys.form.name`) + **Expiration** (`settings.apiKeys.form.expiresIn`) → **Create key** (`settings.apiKeys.createKeySubmit`); read **Your API key** (`settings.apiKeys.yourApiKey`) → **Done** (`common.actions.done`); later the row's **Revoke key** (`settings.apiKeys.revokeKey`)                                                                      | Toast `settings.apiKeys.keyCreated`; the secret is revealed exactly once (gone after Done); the row is in the table after reload (columns `settings.apiKeys.columns.name` / `…key` / `…created` / `…lastUsed`); revoking confirms (`settings.apiKeys.revokeKeyTitle`), toasts `settings.apiKeys.keyRevoked`, and the row is gone after reload                                                                                                                                                                                                                                                                    |
| F33 | MCP endpoint page                 | `/dashboard/{org}/settings/api/mcp` → copy the endpoint (`settings.mcpEndpoint.copyEndpoint`); read the tool inventory; **Copy example request** (`settings.mcpEndpoint.copyExample`)                                                                                                                                                                                                                                                                                           | The page (`settings.mcpEndpoint.title`) renders three tool groups — **Authoring** (`settings.mcpEndpoint.tools.authoring.title`), **Run & trigger management** (`settings.mcpEndpoint.tools.management.title`), **Skills & knowledge** (`settings.mcpEndpoint.tools.capability.title`) — and the **Try it** example (`settings.mcpEndpoint.exampleTitle`); auth help points at REST API keys (`settings.mcpEndpoint.authLink`); with an F32 key filled in, the copied `tools/list` request returns those tools                                                                                                   |
| F34 | WebDAV app-passwords              | `/dashboard/{org}/settings/api/webdav` → **Generate a new app-password** (`webdav.create.title`) → **Label** (`webdav.create.labelLabel`) → **Generate** (`webdav.create.submit`); read **Connection details** (`webdav.connectionDetails.title`); later the row's **Revoke** (`webdav.list.revoke`)                                                                                                                                                                            | The password is shown once under the save-now banner (`webdav.create.savedTitle`) and dismissed with **I have saved it** (`webdav.create.dismiss`); the row joins the **App-passwords** list (`webdav.list.title`) after reload; the connection details show username + URL (`webdav.connectionDetails.usernameLabel`); revoking confirms (`webdav.revokeDialog.title`) and marks the row revoked after reload                                                                                                                                                                                                   |
| F35 | Enterprise SSO configure          | `/dashboard/{org}/settings/enterprise-sso` → pick a **Protocol** (`settings.enterpriseSso.protocolLabel`; options `settings.enterpriseSso.protocol.*`) → fill issuer/client fields (`settings.enterpriseSso.issuerLabel` / `…clientIdLabel` / `…clientSecretLabel`) → **Test connection** (`settings.enterpriseSso.test`) → header **Save**; in **SCIM provisioning** (`settings.enterpriseSso.scim.section`) click **Generate token** (`settings.enterpriseSso.scim.generate`) | The form hint says saving happens from the header (`settings.enterpriseSso.formHint`) — Saved flash, no toast; Test keeps its own result (`settings.enterpriseSso.testOk` / `…testFailed`); after reload the status shows **Sign-in is enabled.** (`settings.enterpriseSso.connected`); the read-only **Redirect URL** (`settings.enterpriseSso.redirectUrlLabel`) is copyable; the SCIM token is revealed once (`settings.enterpriseSso.scim.tokenCreatedHelp`) beside the base URL (`settings.enterpriseSso.scim.baseUrlLabel`); a **Setup guide** (`settings.enterpriseSso.guide.title`) renders per protocol |
| F36 | Data residency route              | `/dashboard/{org}/settings/data-residency`                                                                                                                                                                                                                                                                                                                                                                                                                                      | The page renders the **External Postgres** surface (`settings.dataResidency.externalPostgres`); a non-operator sees the **Read-only access** notice (`settings.dataResidency.readOnly.title`) naming their sign-in email — full depth (stores, tests, restart) is [data-residency.md](data-residency.md)'s job                                                                                                                                                                                                                                                                                                   |

## Boundary & error tests

| ID  | Test                        | Input                                                                                                                                                                                       | Expected                                                                                                                                                                                                                                                                                                                                                                                        |
| --- | --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B1  | Legacy API redirects        | Open `/dashboard/{org}/settings/api-keys`, `/dashboard/{org}/settings/api/runtimes`, `/dashboard/{org}/settings/webdav`, and `/dashboard/{org}/settings/api`                                | The first two land on `…/settings/api/rest` (API keys table), the third on `…/settings/api/webdav`, the fourth on `…/settings/api/rest` — each renders the live page, never a 404                                                                                                                                                                                                               |
| B2  | Legacy page redirects       | Open `/dashboard/{org}/settings/mcp`, `/dashboard/{org}/settings/mcp-servers`, `/dashboard/{org}/settings/people`, `/dashboard/{org}/settings/logs`, `/dashboard/{org}/settings/deployment` | The MCP pair lands on `…/settings/connectors`; people on `…/settings/members`; logs on `…/settings/governance/logs`; deployment on `…/settings/data-residency` — each renders the live page                                                                                                                                                                                                     |
| B3  | Provider deep-link redirect | Open `/dashboard/{org}/settings/providers/openrouter`                                                                                                                                       | Redirects to the providers index with `?provider=openrouter` — the vendor filter (`settings.providers.vendorFilterLabel`) arrives pre-applied to that vendor; clearing the filter clears the URL param                                                                                                                                                                                          |
| B4  | Role gating — member        | Sign in as a **member** role account                                                                                                                                                        | The rail shows only the Personal section plus **Skills**; direct URLs are refused with the full access-denied message — `/dashboard/{org}/settings/organization` → `accessDenied.organization`, `…/settings/providers` → `accessDenied.providers`, `…/settings/enterprise-sso` → `accessDenied.enterpriseSso`, `…/settings/sandboxes` → `sandboxes.accessDenied` — never a partial/blank render |
| B5  | Role gating — developer     | Sign in as a **developer** role account                                                                                                                                                     | The rail additionally shows Providers, Connectors, Sandboxes, and API (all usable), but none of the `orgSettings` pages (Organization, Teams, Members, Branding, Governance, Metrics, Enterprise SSO, Data residency); a direct hit on `…/settings/branding` shows `accessDenied.branding`                                                                                                      |
| B6  | Required-name validation    | Clear the account **Name** and Save; clear the **Organization name** and Save; create a team with a blank **Team name**                                                                     | Each is blocked inline — `settings.account.profile.nameRequired`, `settings.organization.nameRequired`, `settings.teams.teamNameRequired`; after a reload the original values are unchanged and no team row was added                                                                                                                                                                           |
| B7  | API key edge cases          | Create with a blank **Key name**; after F32's revoke, call a REST endpoint with the revoked key (e.g. `curl -H "Authorization: Bearer <key>"` against the API)                              | The blank name is blocked inline (`settings.apiKeys.form.nameRequired`), no row appears after reload; the revoked key is rejected (401/403) — it no longer authenticates the REST or MCP endpoint                                                                                                                                                                                               |
| B9  | WebDAV label bounds         | **Generate a new app-password** with an empty/whitespace label; then type past 64 characters                                                                                                | An empty label leaves the dialog unsubmittable (Generate does nothing); the input caps typing at 64 chars (`maxLength`) — the server guard is unreachable from the UI; no app-password is created. (Optional, heavy: creating at the cap → `webdav.create.errorLimit`)                                                                                                                          |
| B10 | SSO form guards             | On `/dashboard/{org}/settings/enterprise-sso`: **Test connection** with required fields empty; enter a malformed issuer (no scheme)                                                         | The empty test is refused with `settings.enterpriseSso.testMissingFields`; the malformed URL shows the inline validation (`settings.enterpriseSso.validation.url`); with `SITE_URL` / `BETTER_AUTH_SECRET` unset on the server the warning banner renders (`settings.enterpriseSso.deploymentWarning.title`) — env-dependent, note rather than force it                                         |

## Accessibility (WCAG 2.1 AA)

| ID  | Check                | Expected                                                                                                                                                                                                                          |
| --- | -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A1  | Settings rail        | The rail is a labelled `nav` (`navigation.userSettings`); the active row is visually marked; the Governance/Metrics/API disclosure rows are buttons exposing `aria-expanded` and toggle via keyboard with visible focus           |
| A2  | Save cluster         | The header **Save** / **Discard** controls are keyboard reachable and operable; the Saved state is conveyed as text, not color alone                                                                                              |
| A3  | Toggle rows          | Every preference/notification toggle is a labelled switch (accessible name matches its visible label); the locked review-requests toggle conveys its disabled state to assistive tech                                             |
| A4  | Secret reveal & copy | The one-time reveal/copy controls have accessible names — API key (`settings.apiKeys.keyCopied` flow), WebDAV app-password (`webdav.create.copy`), SCIM token, org ID (`settings.organization.copyOrganizationId`)                |
| A5  | Color picker & menus | The branding color controls are keyboard-operable, not pointer-only (`settings.branding.pickColorAria`, `settings.branding.hexValueAria`); credential row-action menus are labelled per row (`settings.credentials.actionsLabel`) |

## Performance

| ID  | Metric               | Target                                                                                                                   |
| --- | -------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| P1  | Rail navigation      | < 1 s to switch between two already-loaded settings pages (mock stack, local backend)                                    |
| P2  | Save → persisted     | < 2 s from clicking **Save** to the value being read back after reload (mock stack, local backend)                       |
| P3  | Providers first load | The Credentials and Harnesses sections render (content or empty state) in < 3 s on the mock stack — no unbounded spinner |

## Issues Found

| #   | Test ID | Route / URL | Severity (crit/high/med/low) | Description | Screenshot |
| --- | ------- | ----------- | ---------------------------- | ----------- | ---------- |
|     |         |             |                              |             |            |

## Test summary

```
Area: Settings
Functional: ___/36   Boundary: ___/10   A11y: ___/5   Perf: ___/3
Issues: ___ (crit __ / high __ / med __ / low __)
Status: PASS / FAIL
```
