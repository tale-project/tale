# Error-surface provocation map

Which error each surface can produce, and how to reach it deliberately. Used by
the suites when a box says "expect the error state", and as the checklist for
"should this even be reachable from here?".

Add a row when a new error becomes reachable, with the shortest path that
provokes it and the recovery the surface offers.

## Reachable from the UI

| Surface                                                                               | How to reach it                                                                                                                                                                                                                                                                              |
| ------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Sandbox limits exceed deployment capacity (`sandboxes.limits.totalExceedsDeployment`) | In Settings > Sandboxes, edit the three limits so their total exceeds deployment capacity. The total and error update; saving stays blocked until the total fits. If the operator lowers capacity after the page loads, saving also catches the new limit.                                   |
| Sandbox deployment capacity unavailable (`sandboxes.limits.capacityUnavailable`)      | In a disconnected sandbox-service test stack, raise a limit in Settings > Sandboxes so the total grows. The page explains that lowering still saves and that infrastructure data must be refreshed before raising; reconnect and use Refresh. Lowering a limit in that state saves normally. |

## Reachable on the API surface only

Never a rendered message — seeing one of these in the UI is itself a bug.

| Failure                                                                          | Where it shows up                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| -------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `SANDBOX_QUOTA_EXCEEDS_DEPLOYMENT` (400)                                         | Save a `sandbox_quota` policy whose three limits add up above the spawner's current `maxSessions`. The error data includes `total` and `maxSessions`; the existing policy remains unchanged. The UI renders a localized explanation, never the raw code.                                                                                                                                                                                                                                                                      |
| `SANDBOX_CAPACITY_UNAVAILABLE` (503)                                             | Save a `sandbox_quota` policy whose total is HIGHER than the saved total while the authoritative deployment capacity cannot be read. A total that does not grow saves without the capacity. The existing policy remains unchanged; retry after the service recovers.                                                                                                                                                                                                                                                          |
| `CHAT_MODEL_UNKNOWN` / `CHAT_MODEL_AMBIGUOUS` (400)                              | `POST /api/v1/threads/{id}/messages` with a `model` that `GET /api/v1/models` does not list, or one listed under two providers with no `providerSlug`. Nothing is queued; the 202 of a good send names the resolved `providerSlug`.                                                                                                                                                                                                                                                                                           |
| `CHAT_TURN_NOT_RUNNING` (404)                                                    | `DELETE /api/v1/threads/{id}/generation` while the poll says `idle`: the body carries `data.lastMessageId` / `data.lastStatus` (the idle poll's pair), so a stop that lost the race against a fast model reads what settled without a second call. During a turn the same call answers 202 `cancelling`; while the poll says `queued` it also answers 202, naming the promised reply, and that reply settles as `cancelled` with empty `parts` and no model call.                                                                                                                                                                                                                                                                                                                                                                                                    |
| `MESSAGE_NOT_FOUND` (404) on a thread message                                    | `GET /api/v1/threads/{id}/messages/{messageId}` with an id the thread does not carry — another thread's message included; the thread itself must be visible first (`THREAD_NOT_FOUND` otherwise). A good id answers the message in the list's shape, `pending` while its turn runs.                                                                                                                                                                                                                                           |
| `CHAT_TURN_IN_PROGRESS` (409) on delete                                          | `DELETE /api/v1/threads/{id}` while a turn runs, or while the poll says `queued`; cancel the turn first, then the delete answers 204.                                                                                                                                                                                                                                                                                                                                                                                         |
| `CHAT_TURN_IN_PROGRESS` (409) on send                                            | `POST /api/v1/threads/{id}/messages` while the poll says `queued` or `streaming`: nothing is queued, the running turn keeps its `messageId`, and a concurrent pair of sends yields exactly one 202. Retry once the poll says `idle`.                                                                                                                                                                                                                                                                                          |
| `BUDGET_EXCEEDED` (429) on send | `POST /api/v1/threads/{id}/messages` while a budget cap that binds the key holder is reached — their own, a team's, the organization's or the key's; a rule at **Max requests** 1 after one send does it: nothing is queued, `data` names the cap and `Retry-After` the wait until its period resets. A cap reached after the 202 settles the reply with `errorCode: budget_exceeded`. In the app the same refusal is a localized toast (`chat.toast.budgetExceeded`), never the code. |
| `CHAT_TURN_IN_PROGRESS` (409) on archive | `PATCH /api/v1/threads/{id}` with `archived: true` while the poll says `queued` or `streaming`: nothing is written and the turn runs on; cancel it or wait for `idle`, then archive. A thread (or its project) archived AFTER a send was accepted settles that reply `cancelled` with the prompt kept, never a 500. |
| `KNOWLEDGE_ENTRY_SUPERSEDED` (409)                                               | `PATCH /api/v1/knowledge-entries/{id}` on an entry a newer version replaced; edit the active one. Deleting a superseded entry removes that row only; deleting the active entry removes the chain and drops the document's passages from the search corpus at once.                                                                                                                                                                                                                                                            |
| `DOCUMENT_STALE` (409)                                                           | `PATCH /api/v1/documents/{id}` with an `expectedUpdatedAt` older than the row's `updatedAt` (read the document, patch it once, then patch again with the first `updatedAt`). Nothing is written; the 200 of a good patch carries the new `updatedAt`.                                                                                                                                                                                                                                                                         |
| `PRECONDITION_FAILED` (412)                                                      | `PATCH /api/v1/documents/{id}` with `If-Match: "stale"` (any tag but the one `GET /api/v1/documents/{id}` last answered as `ETag`, or a `W/` tag — the comparison is strong): 412, `data.etag` naming the current tag, nothing written; the same patch with the current tag applies. The tag covers the whole read, `indexing` included, so a document still indexing answers 412 until it settles. `expectedUpdatedAt` stays the row-level precondition (409 `DOCUMENT_STALE`). Round g (2026-09-14): the header used to be ignored — a silent overwrite. |
| `DOCUMENT_NOT_FOUND` (404)                                                       | `GET`/`PATCH`/`DELETE /api/v1/documents/{id}`, `POST …/retry-indexing` or `GET …/content` with a project file's id, another organization's document, a trashed or expired one, an unknown id — or, on `…/content`, a file whose bytes the store no longer holds. One opaque answer for all of them.                                                                                                                                                                                                                           |
| `DOCUMENT_RECORD_FROZEN` / `DOCUMENT_RECORD_REPLACEMENT_REQUIRED` (400)          | `PATCH /api/v1/documents/{id}` with `content`, `mimeType`, `extension` or `sourceProvider` on a controlled record (mark it controlled in the app first): frozen while in review or approved (`data.state`), a draft outside the attested replacement flow. A title, folder, team or metadata patch of the same record goes through.                                                                                                                                                                                           |
| `TEAM_ACCESS_DENIED` (403)                                                       | `POST /api/v1/documents` or `PATCH /api/v1/documents/{id}` with a `teamId` the key holder is not a member of (any id that is not one of the holder's teams). Nothing is written.                                                                                                                                                                                                                                                                                                                                              |
| `KNOWLEDGE_ENTRY_FORBIDDEN` (403)                                                | `POST`/`PATCH`/`DELETE /api/v1/knowledge-entries[/{id}]` with a key minted by a read-only member. Nothing is written; the `knowledge:mutate` budget is still charged.                                                                                                                                                                                                                                                                                                                                                         |
| `KNOWLEDGE_ENTRY_STORE_TIMEOUT` (503)                                            | `POST` or `PATCH /api/v1/knowledge-entries` while the object store accepts the connection but never answers the PUT (a `tc` delay or a stalled MinIO) for 30 seconds. Nothing is written; retry once the store answers. An unreachable store is a plain 500 instead.                                                                                                                                                                                                                                                          |
| `KNOWLEDGE_ENTRY_DUPLICATE` (409) | `POST /api/v1/knowledge-entries` with a `topic` that matches an active entry's once trimmed, its whitespace collapsed and case-folded (`Refund Policy` beside `refund  policy`), or `PATCH /api/v1/knowledge-entries/{id}` renaming a topic onto another active entry's; the sentence names the existing topic. Nothing is written — supersede or patch the existing entry instead; a superseded or deleted entry frees its topic. |
| `DOCUMENT_HAS_KNOWLEDGE_ENTRY` (409)                                             | `DELETE /api/v1/documents/{id}` — or a `PATCH` of `title` / `content` — on the `documentId` a knowledge entry answers (`sourceProvider: knowledge`); `data.entryId` names the entry. A folder or metadata patch of the same document goes through.                                                                                                                                                                                                                                                                            |
| `FILE_NOT_FOUND` / `FOLDER_NOT_FOUND` (404) on delete or retry-indexing          | `DELETE /api/v1/projects/{id}/files/{documentId}` or `POST …/files/{documentId}/retry-indexing` for a Hub document, a content-only document, a trashed file or another project's file — one opaque answer, and the retry never reaches the indexing core; `DELETE /api/v1/projects/{id}/folders/{folderId}` for a Hub folder or another project's folder. The row stays.                                                                                                                                                                                                                                                                                       |
| `PROJECT_KEY_TAKEN` (409)                                                        | `POST /api/v1/projects` with an explicit `key` another project holds. A derived key that collides is re-derived instead.                                                                                                                                                                                                                                                                                                                                                                                                      |
| `PROJECT_KEY_INVALID` (400)                                                      | `POST /api/v1/projects` with an explicit `key` of one character, seven characters or a symbol (`"a-b"`); the door caps the length at 6 and the domain refuses the shape. Nothing is created.                                                                                                                                                                                                                                                                                                                                  |
| `FILE_TOO_LARGE` (400)                                                           | `POST /api/v1/projects/{id}/uploads` with `"size": 104857601` (one byte over the 100 MiB platform ceiling) — refused before anything is presigned, `data.limitBytes` = 104857600 — or `POST …/files` binding a blob that large (the landed size is what the bind judges). Nothing is presigned or written; an organization cap below the ceiling answers `UPLOAD_POLICY_REJECTED` instead, and the mint's `maxBytes` says which applies.                                                                                      |
| `FILE_SIZE_INVALID` (400)                                                        | `POST /api/v1/conversations/uploads` with an empty body (`Content-Length: 0`) — there is nothing to stage, so no blob is written and no intent recorded. A body over 30 MiB answers `BODY_TOO_LARGE` (413) instead; a body with bytes answers `{ storageId }`.                                                                                                                                                                                                                                                                |
| `FOLDER_NOT_ACCESSIBLE` (403)                                                    | `POST /api/v1/documents` or `PATCH /api/v1/documents/{id}` with a `folderId` naming a Hub folder shared with a team the key holder is not a member of (share a folder with team A in the app, mint the key as a member of team B only). Nothing is written; a folder of a project answers `FOLDER_NOT_FOUND` (404) instead.                                                                                                                                                                                                   |
| `PROJECT_DUPLICATE_EXTERNAL_ID` (409) for a normalized twin                      | `POST /api/v1/projects` twice with the same `externalItemId` once in NFD (`acme-cafe` + U+0301) and once in NFC (`acme-café`), or once padded with spaces: the second answers 409, and `GET /api/v1/projects?externalItemId=` finds the one project under either spelling. A legacy twin `0093` could not fold (an NFD key beside its NFC holder, unreachable by the lookup) is repaired by `0098`: the holder keeps the key, the twin reads with no `externalItemId` and re-keys through `PATCH`; the canonical unique indexes (`projects_org_external_item_canonical`, `tasks_project_external_canonical`) refuse a raw re-creation with `23505`.                                                                                                                                                                                                                                                    |
| `INVALID_QUERY` on a padded lookup (400)                                         | `GET /api/v1/projects?externalItemId=%20%20` (blank once trimmed), or `?externalItemId=x&limit=5` — a lookup takes no page parameter. `GET /api/v1/projects?archived=nope` names `archived`.                                                                                                                                                                                                                                                                                                                                  |
| `ROLE_FORBIDDEN` (403) on the project lifecycle                                  | `PATCH /api/v1/projects/{id}` `{"archived": true}` or `DELETE /api/v1/projects/{id}` with an editor's key; an admin's key archives (the project leaves `GET /api/v1/projects`, appears under `?archived=only`, refuses writes with `PROJECT_ARCHIVED`) and deletes (204, then 404).                                                                                                                                                                                                                                           |
| `PROJECT_HAS_BOUND_AUTOMATIONS` (409)                                            | `DELETE /api/v1/projects/{id}` while `GET /api/v1/projects/{id}/automations` lists an installed automation; `data.automations` names it. Uninstall it (`DELETE /api/v1/projects/{id}/automations/{name}`), then the delete answers 204.                                                                                                                                                                                                                                                                                       |
| `PROJECT_HAS_PROTECTED_RECORDS` / `PROJECT_LEGAL_HOLD` (409)                     | `DELETE /api/v1/projects/{id}` (cascade, the default) on a project holding an approved controlled record, or a document under a legal hold; `{"mode": "detach"}` releases documents instead and passes the record guard. The app's delete dialog answers the same codes.                                                                                                                                                                                                                                                      |
| `FOLDER_NAME_TAKEN` (409)                                                        | Rename a project folder (app) to a sibling's name in another case. Through REST it answers only when the re-read after a lost create race finds nothing (practically never): `POST /api/v1/projects/{id}/folders` with `{"name": "INBOX"}` beside `inbox` answers 200 `created: false` with the stored spelling.                                                                                                                                                                                                              |
| `FOLDER_NAME_INVALID` / `FOLDER_DEPTH_EXCEEDED` (400)                            | `POST /api/v1/projects/{id}/folders` with `{"name": "a/b"}`, `"a\b"`, a name carrying a tab or a NUL, `"."` or `".."` — the sentence names the rule broken (a path separator, a control character, `.`/`..`) and `data.issues` names `name`; or with a `parentId` twenty levels deep. A name over 128 characters or of only whitespace is `INVALID_BODY` naming `name`, and so is a blank `parentId` (`""`) naming `parentId` — omit it for a root folder.                                                                                                                                                                                                                                                                                                                      |
| `UPLOAD_POLICY_REJECTED` / `UNSUPPORTED_FILE_TYPE` at the mint (400)             | `POST /api/v1/projects/{id}/uploads` with `{"fileName": "x.bin"}` (format allowlist), a name without an extension (`"CON"`, `"attachment-4711"` — whatever `contentType` says, `text/plain` included, and `"program.exe"` with it), or a name the org's `upload_policy` extension/MIME rules refuse — nothing is presigned, no bytes travel; the same body without `fileName` mints, and the bind refuses instead.                                                                                                                                                                                                                                                           |
| `BLOB_NOT_FOUND` (404)                                                           | `POST /api/v1/projects/{id}/files` binding a handoff whose presigned `url` was never PUT to; the intent is rolled back, so PUT the bytes and bind the same `uploadId` again.                                                                                                                                                                                                                                                                                                                                                  |
| `OBJECT_STORE_UNCONFIGURED` (503)                                                | Any mint, bind or `…/content` read on a deployment with no object store connected (`OBJECT_STORE_*` unset, no org bucket).                                                                                                                                                                                                                                                                                                                                                                                                    |
| `PROJECT_AGENT_SECRET_UNKNOWN` (400)                                             | `POST` or `PUT /api/v1/projects/{id}/agents…` with `secrets: ["NO_SUCH_SECRET"]`; `data.secrets` names it and nothing is written. The app's agent dialog prunes the same name silently.                                                                                                                                                                                                                                                                                                                                       |
| `PROJECT_AGENT_STALE` (409)                                                      | `PUT /api/v1/projects/{id}/agents/{agentId}` with an `expectedUpdatedAt` older than the agent's `updatedAt` (read it, save once, save again with the first stamp); `data.updatedAt` is the current one and nothing is written.                                                                                                                                                                                                                                                                                                |
| `PROJECT_AGENT_NAME_TAKEN` (409)                                                 | `POST /api/v1/projects/{id}/agents` (or `PUT …/agents/{agentId}`, or the app's agent dialog) with a `name` another agent of the same project carries in any case (`reviewer` against a stored `Reviewer`). Nothing is written; the dialog shows the name error inline, the door answers the 409 like every other duplicate (`PROJECT_KEY_TAKEN`, `FOLDER_NAME_TAKEN`).                                                                                                                                                        |
| `PROJECT_NOT_FOUND` before `TASK_NOT_FOUND` (404)                                | `GET /api/v1/projects/<unknown>/tasks/<unknown>` answers `PROJECT_NOT_FOUND`; the same task id under a real project answers `TASK_NOT_FOUND`.                                                                                                                                                                                                                                                                                                                                                                                 |
| `TASK_ARCHIVED` (403)                                                            | `POST /api/v1/projects/{id}/tasks/{taskId}/comments` or `…/start` on a task archived in the app; the task and its comments still read as 200.                                                                                                                                                                                                                                                                                                                                                                                 |
| `AUTOMATION_NOT_FOUND` (404) on task intake                                      | `POST /api/v1/projects/{id}/tasks` with an `automationSlug` nobody saved; nothing is created. The same door answers a saved-but-undeployed owner as 409 `AUTOMATION_NOT_DEPLOYED` — one code, one status. |
| `AUTOMATION_NOT_DEPLOYED` (409) on task intake                                   | `POST /api/v1/projects/{id}/tasks` with an `automationSlug` that is saved but has no deployed version — the sentence names it; nothing is created. Used to be a 404 `AUTOMATION_NOT_FOUND` whose sentence said "not deployed". |
| `AUTOMATION_NOT_FOUND` (404) on task start                                       | `POST /api/v1/projects/{id}/tasks/{taskId}/start` with a `workflowSlug` nobody saved; nothing starts and the execute budget is not charged. Used to be a 200 `started: false, reason: "not_started"` — the same answer an undeployed workflow got. |
| `AUTOMATION_NOT_DEPLOYED` (409) on task start                                    | `POST /api/v1/projects/{id}/tasks/{taskId}/start` with a `workflowSlug` that is saved but has no deployed version — the sentence names it; nothing starts and the execute budget is not charged. `reason: "not_started"` remains only for a deployment withdrawn between the door's check and the start. |
| `SETUP_FOLDER_MISSING` (400) on task intake                                      | `POST /api/v1/projects/{id}/tasks` with a `setupFolderName` no root folder of the project carries (the match ignores case and surrounding whitespace); nothing is created. With the folder present, the task's `externalUrl` reads back as that folder's id — the Setup-folder binding a folder-driven automation reads off `input.task.externalUrl`, resolved again on every repeat — and the same body with `externalUrl` beside it is 400 `INVALID_BODY` naming `setupFolderName`. |
| `INVALID_BODY` for a padded task key (400)                                       | `POST /api/v1/projects/{id}/tasks` with `externalId: "   "` — `data.issues` says `must not be blank`; `"  case-001  "` is accepted and re-picks the task created as `case-001`.                                                                                                                                                                                                                                                                                                                                               |
| `INVALID_BODY` for a blank key, a blank parent or an empty batch (400) | `POST /api/v1/projects` with `key: "   "` — `data.issues` names `key`, `must not be blank` (omit the key to derive it from the name); `POST /api/v1/projects/{id}/folders` with `parentId: ""` names `parentId`; `POST /api/v1/contacts/bulk` with `{"contacts": []}` names `contacts` (`must have at least 1 item`) — nothing is created, never a 201. |
| `INVALID_BODY` for a website-search `limit` (400)                                | `POST /api/v1/websites/{id}/search` with `{"query": "x", "limit": 9999}` (or `0`, `-1`, `2.5`): `data.issues` names `limit` and the rule (`must be at most 100`, `must be at least 1`, a whole number) — a body field is refused, never clamped, the rule the two knowledge-search doors already followed; a list's `?limit=9999` clamps instead (the query-parameter rule). Nothing is searched. |
| `WEBSITE_DOMAIN_NOT_CRAWLABLE` (400)                                             | `POST /api/v1/websites` naming `localhost`, a link-local or RFC 1918 host, or a cloud metadata address, with `TALE_ALLOW_PRIVATE_CRAWL_HOSTS` unset.                                                                                                                                                                                                                                                                                                                                                                          |
| `WEBSITE_DOMAIN_NOT_CRAWLABLE` (400) for a resolved address                      | `POST /api/v1/websites` naming a public hostname whose DNS answer is loopback, private or metadata (`127.0.0.1.nip.io`, `10.0.0.1.nip.io`, `169.254.169.254.nip.io`) with `TALE_ALLOW_PRIVATE_CRAWL_HOSTS` unset; the sentence names the resolved address and no row is created. A name DNS cannot answer yet registers and fails on its first scan instead.                                                                                                                                                                  |
| `WEBSITE_DOMAIN_IMMUTABLE` (400)                                                 | `PATCH /api/v1/websites/{id}` with a `domain` other than the stored one (`renamed.example`, or the stored host as a URL with a path). The stored value itself, in any case, is accepted, and the patch answers 200 with the website.                                                                                                                                                                                                                                                                                          |
| `WEBSITE_DUPLICATE_DOMAIN` (409)                                                 | `POST /api/v1/websites` (or the app's **Add website**) naming a domain the organization already tracks — under the same spelling, or its `www.`/apex sibling (`www.example.com` against a stored `example.com`) — or a `urls` list posted onto a domain registered as a whole-site crawl. Nothing is written or queued; `data.websiteId` and `data.domain` name the stored row. Only a `urls` list posted onto a domain registered as a list under the same spelling passes (200, the existing id).                           |
| `WEBSITE_INVALID_LIST_URL` (400)                                                 | `POST /api/v1/websites` with `urls` carrying an entry off the domain (`https://elsewhere.example/x` for `example.com`), a non-http(s) scheme, or an entry that is not a URL; the message names the entry. The list is validated before the domain is even looked up, so nothing is written.                                                                                                                                                                                                                                   |
| `CONTACT_DUPLICATE_EMAIL` / `CONTACT_DUPLICATE_EXTERNAL_ID` (409)                | `POST /api/v1/contacts` (or a `PATCH` onto the key) with the email of a live contact in another case (`Ada@Example.COM` beside `ada@example.com`), or its `externalId`; a trashed contact frees both keys. In `POST /api/v1/contacts/bulk` the twin fails its own row under `errors[].errorCode` and the rest land.                                                                                                                                                                                                           |
| `CONTACT_DUPLICATE_EMAIL` / `CONTACT_DUPLICATE_EXTERNAL_ID` (409) on a restore | `POST /api/v1/contacts/{id}/restore` on a trashed contact whose email or `externalId` a live contact has since taken (a CRM re-import that recycled the id): 409, the contact stays in the trash. A contact not in the trash answers 200 with the contact (a no-op); an unknown id 404 `CONTACT_NOT_FOUND`; a body other than `{}` 400 `INVALID_BODY`. |
| `CONTACT_STALE` / `PRODUCT_STALE` (409)                                          | `PATCH /api/v1/contacts/{id}` or `PATCH /api/v1/products/{id}` with an `expectedUpdatedAt` older than the row's `updatedAt` (read it, patch once, patch again with the first stamp); nothing is written, and the 200 of a good patch carries the new `updatedAt`.                                                                                                                                                                                                                                                             |
| `CONTACT_IDENTITY_REQUIRED` (400)                                                | `PATCH /api/v1/contacts/{id}` with `{"name": null, "email": null, "externalId": null}` (or `""` for each) on a contact that carries only those; clearing one while another stays answers 200, and a patch that leaves all three alone always passes.                                                                                                                                                                                                                                                                          |
| `DUPLICATE_PRODUCT_NAME` / `DUPLICATE_PRODUCT_EXTERNAL_ID` (409)                 | `POST /api/v1/products` with the name of an existing product in another case (`widget` beside `Widget`), or its `externalId`; a `PATCH` renaming onto either answers the same. The app's create and edit dialogs map the name twin to their own message.                                                                                                                                                                                                                                                                      |
| `INVALID_BODY` on a free-form field (400)                                        | `POST /api/v1/contacts` with `metadata` (or `address`) nested nine levels deep, holding more than 500 keys in total, or over 64 KiB of JSON; `data.issues[0].path` names the path (`metadata.a.a.…`). The same on `/api/v1/products` and `/api/v1/documents`.                                                                                                                                                                                                                                                                 |
| `INVALID_BODY` on a typed product field (400)                                    | `POST /api/v1/products` with `currency: "ZZZ"` (or `"123"`, `"$"`), or `imageUrl: "javascript:alert(1)"`; `usd` is accepted and stored as `USD`. A blank `name` (`"   "`) on create or patch answers `must not be blank`, and a `PATCH` with `phone: ""` clears the phone (200) rather than keeping it.                                                                                                                                                                                                                       |
| `METHOD_NOT_ALLOWED` (405)                                                       | Any `/api/v1` route with a verb it does not serve (`DELETE /api/v1/models`), and any verb but `POST` on `/api/v1/mcp`; `Allow` lists the verbs served.                                                                                                                                                                                                                                                                                                                                                                        |
| `IDEMPOTENCY_KEY_REUSED` (409)                                                   | `POST /api/v1/automations/{name}/runs` twice with the same `Idempotency-Key` and a different `input`: the first answers 202, the second 409 and starts nothing. The same body again answers 202 `duplicate: true` with the first run's id.                                                                                                                                                                                                                                                                                    |
| `IDEMPOTENCY_KEY_REUSED` (409) on a chat send                                    | `POST /api/v1/threads/{id}/messages` twice with the same `Idempotency-Key` and a different `content`: the first answers 202, the second 409 and queues nothing. The same body again answers 202 `duplicate: true` with the first send's `messageId`; a concurrent keyed pair runs one turn; a keyed send refused mid-turn (409) is not remembered.                                                                                                                                                                            |
| `IDEMPOTENCY_KEY_REUSED` on the MCP `start_run` tool | `tools/call` `start_run` twice with the same `idempotencyKey` argument and a different `input`: the second is the tool's own refusal (`isError`, `code: IDEMPOTENCY_KEY_REUSED`) and starts nothing; the same arguments again answer the first run's handle with `duplicate: true`. The `Idempotency-Key` HTTP header is not read on `/api/v1/mcp`. |
| `INVALID_HEADER` (400)                                                           | `POST /api/v1/automations/{name}/runs` (or its project twin, or `POST /api/v1/threads/{id}/messages`) with `Idempotency-Key: ordér` — any character outside printable ASCII (0x20–0x7E: `é` in either normalization, a tab, DEL): the envelope names the header under `data.issues`, nothing starts or is queued and nothing is remembered. A header the client sent but left blank (`Idempotency-Key:` or spaces only — send it from a raw socket; curl drops a blank `-H`) answers 400 too (`must not be blank`), as does a value over 255 characters; omit the header to start without a key. A control character never reaches the platform (the edge closes the connection first). Round g (2026-09-14): a blank value used to be read as no key, so three retries carrying `"   "` billed three runs. |
| `RUN_ACTIVE` (409)                                                               | `DELETE /api/v1/runs/{runId}` (or the project form) on a run whose status is `queued`, `running` or `waiting`; cancel it, then the delete answers 204 and the read 404.                                                                                                                                                                                                                                                                                                                                                       |
| `AUTOMATION_VERSION_UNKNOWN` (404) on a read                                     | `GET /api/v1/automations/{name}?version=99` on an automation that exists, or `?version=deployed` while nothing is deployed; an unknown automation answers `AUTOMATION_NOT_FOUND` instead.                                                                                                                                                                                                                                                                                                                                     |
| `CAPABILITY_NOT_FOUND` / `CAPABILITY_INPUT_INVALID` (MCP tool refusal) | `tools/call` `invoke_capability` with an id the registry does not hold (a saved-only automation is not in it — deploy first) or with `input` its schema refuses: the refusal carries `code`, `hint` (the nearest id) and `isError`; nothing runs. `get_knowledge` lifts the knowledge door's own code (`KNOWLEDGE_UNAVAILABLE` when the search itself failed). |
| `AUTOMATION_TRIGGER_INVALID` (400) for an event                                  | `PUT /api/v1/automations/{name}/triggers` with `{"kind": "event", "event": "no.such.event"}` or a reserved name (`task.assigned`); the sentence lists the events the platform raises. Nothing is saved.                                                                                                                                                                                                                                                                                                                       |
| `AUTOMATION_TRIGGER_INVALID` (400) for a date no month has                       | `PUT /api/v1/automations/{name}/triggers` with `{"kind": "schedule", "cron": "0 0 30 2 *"}` (or `0 0 31 4,6,9,11 *`); the sentence names the day and the months — "day-of-month 30 never occurs in month 2". `0 0 31 * *`, `0 0 29 2 *` and `0 0 30 2 1` bind. Nothing is saved.                                                                                                                                                                                                                                              |
| `INVALID_BODY` (400) for a key of another trigger kind                           | `PUT /api/v1/automations/{name}/triggers` with `{"kind": "webhook", "cron": "0 9 * * *"}`: `data.issues` names `cron` as a field this body does not take (`cron`/`timezone` are the schedule's, `event` the event trigger's, `rotateToken` the webhook's). The MCP `set_trigger` twin answers `AUTOMATION_TRIGGER_INVALID` naming the key. Nothing is saved.                                                                                                                                                                  |
| `AUTOMATION_INPUT_INVALID` (400)                                                 | `POST …/runs` with an input the automation's `inputs` schema refuses (`{"input": null}` against `{"type": "object"}`); `data.issues` names each field. No run exists.                                                                                                                                                                                                                                                                                                                                                         |
| `AUTOMATION_PROJECT_FORBIDDEN` (403) on the webhook door                         | `POST /api/projects/{id}/automations/webhook/{token}` with a project id that does not exist, is archived, or where the automation is not installed — one body for all three, naming neither the case nor the automation.                                                                                                                                                                                                                                                                                                      |
| `RATE_LIMITED` (429) on the webhook door                                         | 241 deliveries inside a minute from one address (before the token is checked), or 41 to one trigger; `Retry-After` names the wait.                                                                                                                                                                                                                                                                                                                                                                                            |
| `INVALID_BODY` for a NUL byte (400)                                              | Any REST write whose JSON carries `\u0000` in a string; `data.issues` names the field. Used to be a 500 from Postgres.                                                                                                                                                                                                                                                                                                                                                                                                        |
| `INVALID_URL` (400) | Any `/api/v1` request whose URL carries `%00` in the path or the query — `GET /api/v1/skills/{slug}/files/a%00b`, `GET /api/v1/contacts?cursor=%00`: the guard ahead of every route answers the envelope with `X-Tale-Api-Version` and `no-store` before the key is read, so a keyless request gets it too. A NUL inside a JSON string is the body's own `INVALID_BODY` (the row above). |
| `SKILL_EXISTS` (412)                                                             | `PUT /api/v1/skills/{slug}` with `If-None-Match: *` on a slug that already has a bundle; nothing is written, and the same body without the header updates it in place.                                                                                                                                                                                                                                                                                                                                                        |
| `INVALID_SKILL_SLUG` (400) naming the rule                                       | `PUT /api/v1/skills/<100 letters>` — the sentence says "at most 64", not the charset, and echoes at most 64 characters; `PUT /api/v1/skills/claude` says the name is reserved. `GET` and `DELETE` on the same slugs answer 404 `SKILL_NOT_FOUND` instead.                                                                                                                                                                                                                                                                     |
| `INVALID_BODY` on a skill body (400)                                             | `PUT /api/v1/skills/{slug}` with a `body` over 507,893 bytes of UTF-8 (a character count under the cap still counts), with an unknown key such as `slug`, or with `visibility: "private"` — the option list names `team` and `org` only.                                                                                                                                                                                                                                                                                      |
| `CONVERSATION_SOURCE_NOT_FOUND` (404) / `INTEGRATION_NOT_OWNED` (403) on a claim | `POST /api/v1/conversations/deliveries/claim` with a `source` no snapshot ever named, or one only another API key user's snapshots named; a source of your own answers 200 with its (possibly empty) deliveries.                                                                                                                                                                                                                                                                                                              |
| `CONVERSATION_CONTACT_TRASHED` (409)                                             | Create a contact with an `externalId`, mirror one conversation for it through `POST /api/v1/conversations/sync`, `DELETE /api/v1/contacts/{id}` (204), then sync a NEWER content snapshot of that conversation: 409, nothing written. `GET /api/v1/conversations/sync?source=&externalId=` reports `contactStatus: "trashed"`. A `deleted: true` teardown still applies (200). Create a second contact under the same `externalId` and sync the old conversation again: still 409 — the thread is bound to the trashed row, never re-resolved by the recycled id. Round g (2026-09-14): the snapshot used to apply, and the freed id could hand one person’s history to the next. |
| `CONVERSATION_CLOSED` (409) on a content snapshot | Mirror a conversation through `POST /api/v1/conversations/sync`, tear it down with a `"deleted": true` snapshot (200, `applied: true`), then sync a content snapshot of it at a higher version: 409, nothing written — the thread stays closed with no messages, and `GET /api/v1/conversations/sync?source=&externalId=` reports `sourceDeleted: true` with `status: "closed"`. An Inbox reply to the torn-down thread is the same 409. Round i (2026-09-15): the content snapshot used to reopen the thread with its messages, and the receipt never said a teardown had landed. |
| `CONVERSATION_CONTACT_CONFLICT` (409) after a re-key | `PATCH /api/v1/contacts/{id}` with a new `externalId`, then `POST /api/v1/conversations/sync` naming the OLD `externalContactId`: 409 naming the id the conversation is bound to, nothing written — the binding followed the re-key, so a snapshot naming the NEW id applies and the receipt (`GET …/sync`) names it beside `contactId`. |
| `DELIVERY_NOT_FOUND` vs `ATTACHMENT_NOT_FOUND` (404)                             | `GET /api/v1/conversations/deliveries/{id}/attachments/0` with an id nobody claimed answers the first; a claimed delivery with fewer attachments than the position — or a position such as `abc`, `-1`, `1.5` — answers the second.                                                                                                                                                                                                                                                                                           |
| `SKILL_STALE` (412)                                                              | `PUT` or `DELETE /api/v1/skills/{slug}` with `If-Match: "stale"`, a weak `W/"…"` copy of the current tag, or `If-Match: *` on a slug that holds nothing; `data.etag` names the current tag (`null` when nothing is stored) and the file on disk is unchanged. The `etag` a `GET` answers (its `ETag` header too) lets the same write through with 200 and a new tag; `If-None-Match` with that tag on the `GET` answers 304 with no body.                                                                                     |
| `SKILL_FILE_NOT_FOUND` vs `SKILL_NOT_FOUND` (404)                                | `GET /api/v1/skills/{slug}/files/missing.md` on a real skill answers the first, as do `.turbo/x`, `node_modules/x.js` and `..%2FSKILL.md` — paths the skill's `files[]` never lists; the same path on an unknown or malformed slug answers the second. `files/SKILL.md` and any listed path answer the bytes as an attachment.                                                                                                                                                                                                |
| `NOT_FOUND` (404) at the edge for a dot-segment under `/api/` | `curl --path-as-is https://<host>/api/v1/skills/{slug}/files/../../../etc/passwd` — or `/api/v1/../v1/me`, or `/x/../api/v1/me`, whose dots fold INTO the prefix (the backend's URL parser folds them the same way and would serve it): a `.` or `..` segment, raw or `%2e`-encoded, closed by a raw `/`, `?` or the end of the URI, anywhere in a request whose raw URI or whose folded path is under `/api/`, is refused by the proxy before any backend lane — the JSON envelope with an `X-Request-Id` of its own, `Cache-Control: no-store` and NO `X-Tale-Api-Version` (the edge knows no contract version). The same traversal with its slashes encoded too (`%2e%2e%2f…`, `..%2FSKILL.md`) passes the edge, reaches the route and answers `SKILL_FILE_NOT_FOUND`. Without the proxy (the `:3000` dev modes) the URL parser collapses the dots first and the request lands wherever they resolve — an unrouted path's plain 404. |
| `SKILL_MALFORMED` (422) for a bundle the file layer refuses                      | With filesystem access, plant a symlink inside a bundle directory (`ln -s /etc/hostname <org>/skills/<slug>/link.md`): `GET /api/v1/skills/{slug}/files/link.md` answers 422 naming `skills/<slug>/link.md`, never the server path and never a 500; `GET /api/v1/skills/{slug}` walks the bundle and answers the same.                                                                                                                                                                                                        |
| `DELIVERY_RETRY_UNAVAILABLE` (409)                                               | `POST /api/v1/conversations/deliveries/{id}/retry` on a delivery that is still queued or leased, or already acknowledged; a dead-lettered one (a permanent `fail`, or ten transient ones) answers 200 and shows `queued` again in `GET /api/v1/conversations/deliveries?source=…`, where it showed `failed` with `attempts: 10` before. An id nobody owns answers 404 `DELIVERY_NOT_FOUND`.                                                                                                                                   |
| `INVALID_QUERY` on the delivery listing (400)                                    | `GET /api/v1/conversations/deliveries?source=x&status=lost` — `status` takes `queued`, `leased`, `failed` or `delivered`; a missing `source`, or a blank `cursor`, is refused the same way. The listing never carries `claimToken` or `body`.                                                                                                                                                                                                                                                                                 |
| `ATTACHMENT_NOT_STAGED` / `ATTACHMENT_SIZE_MISMATCH` (400)                       | `POST /api/v1/conversations/sync` naming an attachment `storageId` never staged through `POST /api/v1/conversations/uploads` (or whose two-hour window lapsed) answers the first — so does a `storageId` that door never handed out, malformed (`"e4-fake-storage-id"`) or shaped like another organization's key (`s3:tale/other-org/<uuid>`), never a 403 `BLOB_REF_INVALID`; a `storageId` that landed with a `size` other than the one declared answers the second. Nothing of the snapshot is applied either way.        |
| Deleted snapshot at the stored version (200, not 409)                            | `POST /api/v1/conversations/sync` with `deleted: true` and the source's current `version` (the maximum `9007199254740991` included) closes the mirror; a second one replays as `applied: false`.                                                                                                                                                                                                                                                                                                                              |
| `INVALID_SESSION` (400) on a browser-session import                              | `POST /api/v1/browser-sessions/import` (as an allowlisted operator) with `domain: "localhost"`, `127.0.0.1`, `../etc/passwd` or `*.example.com`; `HTTPS://Example.COM/path` is accepted and stored as `example.com`.                                                                                                                                                                                                                                                                                                          |
| `BROWSER_SESSION_NOT_FOUND` (404)                                                | `DELETE /api/v1/browser-sessions/{id}` twice as an allowlisted operator: 204, then 404. A non-allowlisted key gets the 403 before any body or id is looked at.                                                                                                                                                                                                                                                                                                                                                                |
| `invalid_token` on OIDC userinfo (401 + `WWW-Authenticate: Bearer`)              | `GET /api/auth/oauth2/userinfo` with an expired or made-up bearer token. The token endpoint answers an unknown grant as `unsupported_grant_type`.                                                                                                                                                                                                                                                                                                                                                                             |
| `ORG_SLUG_REQUIRED` (400) on a read                                              | Any `/api/v1` read — `GET /api/v1/contacts`, say — with a key whose user belongs to two organizations and no `X-Organization-Slug`; the dashboard's last-opened organization never answers a machine call; the 400 lists the slugs under `data.organizations` (`GET /api/v1/me` answers them too once one is named). A single-organization key never sees this.                                                                                                                                                               |
| `ORG_SLUG_INVALID` (404) vs `ORG_FORBIDDEN` (403)                                | `X-Organization-Slug: no-such-org` (or a value no slug can be, such as `Acme Inc` or 200 characters — echoed at most 64) answers 404 on every method; a slug of an organization the key's user is no member of, or is disabled in, answers 403 naming the slug. Both before any body is read.                                                                                                                                                                                                                                 |
| `BODY_TOO_LARGE` (413)                                                           | Any REST write with a body over 1 MiB (`POST /api/v1/contacts` with a 2 MiB `metadata`), or over the route's own cap (a document's inline `content` 32 MiB, contacts bulk 8 MiB, a conversation snapshot 8 MiB, a staged upload 30 MiB, a skill save 4 MiB); a declared `Content-Length` past the cap is refused before a byte is read. The sentence names the cap (`Request body exceeds the 1 MiB limit of this route`) on every route, the MCP endpoint and the delivery doors included; the envelope carries `requestId`. |
| `INVALID_BODY` for bytes JSON cannot carry (400)                                 | Any REST write whose body is not UTF-8 (`printf '{"name":"\xff"}'`) — `The body is not valid UTF-8`; or whose JSON carries a whole number beyond 2^53 − 1 (`{"externalId": 12345678901234567890}`) — the issue names the key and says to send it as a string. Neither is rounded or repaired.                                                                                                                                                                                                                                 |
| `INVALID_LIMIT` (400)                                                            | Any list with `?limit=2.5`, `?limit=abc` or `?limit=1e2`: the sentence promises a whole number and names the range. `?limit=0` and `?limit=9999` are clamped, not refused; a blank `?limit=` is `INVALID_QUERY` (`must not be blank`), like every blank parameter. The envelope names `limit` under `data.issues`, like every refused parameter.                                                                                                                                                                                                                                                            |
| `INVALID_QUERY` on a list or a write (400)                                       | `GET /api/v1/contacts?statuss=active` (a parameter the route does not take — `data.issues` names it), `?limit=1&limit=100` (given more than once), `?status=` on a filter (`must not be blank`), a blank `?cursor=` or `?limit=` (the empty `continueCursor` a last page answers is never read as the first page again), `?foo=1` on a lookup such as `GET /api/v1/me`; and any query string at all on a write — `DELETE /api/v1/contacts/{id}?force=true` refuses instead of ignoring `force`.                               |
| `INVALID_CURSOR` (400)                                                           | `GET /api/v1/contacts?cursor=abc`, a cursor edited by one character, or a cursor another list answered (a products cursor on the contacts list); the position is never guessed and the first page is never answered in its place. The envelope names `cursor` under `data.issues`.                                                                                                                                                                                                                                                                                             |
| `INTERNAL_ERROR` (500) carries `requestId`                                       | Not provoked deliberately; when it shows up, the envelope's `requestId` equals the response's `X-Request-Id` (your own, when you sent one) and is what to quote. Every response, an error included, carries `X-Request-Id`.                                                                                                                                                                                                                                                                                                   |
| `URI_TOO_LONG` (414)                                                             | Any `/api/v1` request whose URL (path and query) is over 32 KiB — `GET /api/v1/contacts?q=<33,000 a's>` — answers 414 in the envelope with `requestId`, `X-Tale-Api-Version` and `Cache-Control: no-store`, before any route is looked up, on HTTP/1.1 and HTTP/2 alike, through the proxy too: the backend guard is the one 414 producer (the edge no longer answers a twin of its own). One byte under the cap reaches the route (400 `INVALID_QUERY` for a parameter it does not take); a URL past the edge's 64 KiB header budget meets the bare 431 below — except in the few KiB of slack HTTP/1.1 lets through: a 66 KiB URL still reaches the guard and answers 414 (on HTTP/2 the budget is exact). |
| 431 at the edge (no envelope)                                                    | A request whose headers as a whole exceed the proxy's 64 KiB budget (a 70 KiB `X-Organization-Slug`): over HTTP/1.1 a bare text 431 from Go, never JSON; over HTTP/2 the connection is closed with no response at all (a single header field past the budget fails HPACK decoding) — both documented as the one refusal that is not an envelope. Below the budget, a 1 KiB slug is `ORG_SLUG_INVALID` with the echo cut at 64 characters. Neither refusal carries `X-Request-Id` — correlate by time and client address in the proxy log. |
| 400 at the edge (no envelope) for a control character in a header                | Any request whose header VALUE carries a byte below 0x20 (other than tab) or DEL — `curl --http1.1 -H $'Idempotency-Key: a\x01b' -H 'Authorization: Bearer …' -d '{}' …/runs`: over HTTP/1.1 a bare text `400 Bad Request` from Go with `Connection: close`, never JSON; over HTTP/2 the stream is reset (a request with a body ends the connection with GOAWAY) with no response at all. The header never reaches the platform — no run is started, no idempotency key is remembered — so the envelope cannot apply; documented beside the 431 as the edge's own refusals. Printable ASCII in `Idempotency-Key` (the spec's pattern) never meets this. Neither refusal carries `X-Request-Id` — correlate by time and client address in the proxy log. |
| `WEBSITE_DOMAIN_INVALID` (400) for a scheme | `POST /api/v1/websites` with `domain: "http://example.com"` (any scheme, `https://` included): refused by name — a domain is a bare host, crawled over HTTPS only; a trailing dot is stripped, an empty label (`a..b`) refused. |
| `BODY_LENGTH_MISMATCH` (400) at the edge                                         | Over HTTP/2, any request whose body ends before its declared length — `curl --http2 -X POST https://<host>/api/v1/contacts -H 'Authorization: Bearer …' -H 'Content-Type: application/json' -H 'Content-Length: 50' --data-binary '{}'` (2 bytes sent, 50 declared) — on every path, for a declared length under the route's cap too: the JSON envelope from the proxy with `Cache-Control: no-store`, an `X-Request-Id` of its own repeated as `requestId`, and NO `X-Tale-Api-Version`; the platform never saw the request (a declared length over the cap may draw its 413 first — either is right). Over HTTP/1.1 the same request waits for the missing bytes until the client gives up or the 15-minute 408. Used to be the HTML maintenance page as a 502. |
| `BODY_CHUNK_MALFORMED` (400) at the edge                                         | Over HTTP/1.1 with `Transfer-Encoding: chunked`, a body whose chunk framing is malformed — `printf 'POST /api/v1/mcp HTTP/1.1\r\nHost: <host>\r\nAuthorization: Bearer <key>\r\nContent-Type: application/json\r\nTransfer-Encoding: chunked\r\nConnection: close\r\n\r\nZZZZ\r\ngarbage\r\n0\r\n\r\n' \| openssl s_client -connect <host>:443 -servername <host> -ign_eof -quiet` (curl cannot send a bad chunk): 400, the JSON envelope from Caddy (`Server: Caddy`, an `X-Request-Id` of its own, no `X-Tale-Api-Version`), no `Retry-After`. The same bytes over HTTP/2 have no chunked framing and answer the backend's own 400. Round g (2026-09-14): it used to be 502 `UPSTREAM_UNAVAILABLE` with retry advice. |
| `UPSTREAM_UNAVAILABLE` (502 / 503 / 504) at the edge                             | `docker compose stop backend-api`, then `GET /api/v1/me` — or any machine door: `/api/*`, `/scim/v2/*`, `/http_api/*`, `/events`, `/status.json`, `/openapi.json`, `/.well-known/*` — answers the JSON envelope from the proxy with the error's own status, `Retry-After: 5`, `Cache-Control: no-store`, an `X-Request-Id` of its own repeated as `requestId`, and NO `X-Tale-Api-Version`; `GET /` (a browser navigation, an asset, `/dav/*`) still answers the HTML maintenance page with its meta-refresh. 502 is an upstream that refused the connection, 503 one the proxy's health checker has marked down, 504 one that timed out. `docker compose start backend-api`, and the same call answers 200 within seconds. |
| `REQUEST_TIMEOUT` (408)                                                          | Any request whose body stops arriving before the listener's deadline — the deployment's is 15 minutes for headers and body together (`requestTimeout`, `lib/http-hygiene.ts`); provoke it against a local harness whose `serverOptions` carry a short `requestTimeout`/`headersTimeout` (a few seconds) and `connectionsCheckingInterval`, with `curl --limit-rate 1k -H 'Content-Type: application/octet-stream' --data-binary @a-2MB-file http://localhost:3005/api/v1/conversations/uploads`: a 408 in the envelope with a fresh `requestId` (the in-flight request's own id is not reachable at the socket layer — the edge's refusals do the same), `X-Tale-Api-Version` and `Cache-Control: no-store`, then the connection closes. A connection whose response already began is closed without one; bytes that are not HTTP answer 400 `HTTP_ERROR` the same way. |
| `UNAUTHORIZED` (401) for a key in `x-api-key`                                    | Any request — `/api/v1`, `/api/auth/*`, `/api/app/*`, `/events` — carrying the key as an `x-api-key` header instead of `Authorization: Bearer` answers 401 with `WWW-Authenticate: Bearer` and a sentence naming the bearer form, before any door reads it: the header is the platform's internal hand-off and used to turn a leaked REST key into a signed-in session (it minted further keys and opened every app route).                                                                                                   |
| `SIGN_UP_CLOSED` (403) on account creation | `POST /api/auth/sign-up/email` with any fresh address on a deployment that already holds an account — from the sandbox network (`http://backend-api:3005`) as easily as from the edge. The route answers only while the deployment is empty, so the setup flow and the managed CLI's bootstrap keep working; an administrator creates every later account under Settings > Members. A throwaway test stack reopens it with `TALE_ALLOW_OPEN_SIGN_UP=true`, which the dev orchestrator and the dev compose overlay already set. |
| `ORGANIZATION_CREATION_FORBIDDEN` (403) on organization creation | `POST /api/auth/organization/create` from a signed-in account whose address is not in `TALE_ORGANIZATION_CREATORS`, on a deployment that already holds an organization (the first one is always allowed; an unset variable keeps the route open to every signed-in user). The same rule answers `GET /api/app/organizations/capabilities` with `{"canCreate":false}`, which is why the app shows no **Create organization** entry. A managed deployment without `organizations.creators` never reaches this code: its proxy refuses the route first. |
| `UNAUTHORIZED` (401) with no credential at all | `GET /events`, `GET /api/app/…` or a control route with neither a session cookie nor a bearer key: the coded envelope (`{error, code: "UNAUTHORIZED"}`, `Cache-Control: no-store`) — it used to answer a bare `{"error":"unauthorized"}` a client branching on `code` could not read. |
| `PROJECT_AGENT_HARNESS_INVALID` (400) | `POST /api/v1/projects/{id}/agents` with `harness: "cursor"` (shipped, but it brings its own credentials) or any unknown slug: the envelope names the eligible set in `data.harnesses` — the list `GET /api/v1/models` answers under `harnesses` — and nothing is written. |
| `OBJECT_STORE_UNAVAILABLE` (503) on a download                                   | `GET /api/v1/projects/{id}/files/{documentId}/content` (or its `HEAD`) while the object store is unreachable — stop the dev MinIO, or point `OBJECT_STORE_*` at a closed port — answers 503 with `Retry-After: 5`; the file's row on `GET …/files?folderId=` still lists it (there is no single-file read — the contract debt ledger in `.agents/repo.md` names it), and a document that has no blob or belongs to another project is `FILE_NOT_FOUND`, never 503.                                                                                                                                          |
| `ROLE_FORBIDDEN` (403) on the notification export | `GET /api/v1/notifications/sync` with the key of a member who is neither Owner nor Admin and holds no live `tale:notifications.export` grant, or whose grant expired or was revoked. Nothing about the recipient is read, and `GET /api/v1/me` reads `capabilities.notificationExport: false` for the same key. An Admin's grant (`POST /api/app/governance/competences`) lifts it. |
| `COMPETENCE_CAPABILITY_UNKNOWN` (400) | `POST /api/app/governance/competences` from an Admin session with a `competence` under the reserved `tale:` namespace that is not a platform capability — `tale:admin`, or `TALE:notifications.export` in another case. Nothing is written; the refusal is audited as `competence_grant_denied`. |

## Must never appear

An unhandled rejection, a React error boundary, a raw backend validation error
rendered as a toast, or any `error`-level console line not on the known-benign
list in [`not-a-finding.md`](not-a-finding.md). There is no repro by definition;
any sighting is a finding.

## Agent run diagnostics

These bounded diagnostics belong to the failed agent node or task run when a
vision polyfill is required. A serving model proven to support native vision
retains its direct image-reading path without consulting the polyfill policy.
Diagnostics do not contain provider responses, endpoint values or credentials.
Correct the configuration or restore the service, then explicitly retry the run.

| Failure                           | Provocation and recovery                                                                                                                                                                                                                                                     |
| --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `VISION_MODEL_POLICY_INVALID`     | Supply a malformed `vision_model` policy for a text-only or unknown serving target in an isolated test. Configure both `providerSlug` and `modelId`, or explicitly choose Auto. No polyfill is selected before this refusal.                                                 |
| `VISION_MODEL_POLICY_UNAVAILABLE` | Make the policy read fail, corrupt its stored file, or remove access to the configured root/organization. Restore valid governance configuration; a real absent policy file in an available root still means Auto, but a cached earlier value cannot bypass this fresh read. |
| `VISION_MODEL_UNAVAILABLE`        | Pin a missing/non-vision model, remove its provider or active gateway credential, or exclude it from the credential allowlist. Restore the exact pin's serving prerequisites. A text-only turn cannot substitute another provider or proceed without the required polyfill.  |
| `VISION_MODEL_RESOLUTION_FAILED`  | Make the explicitly pinned provider, credential or catalog resolver fail. Restore that service and retry; its private response is not copied into the diagnostic.                                                                                                            |

## Server transcription model routing

These bounded codes appear in model status and composer capability metadata.
The server dictation door returns 409 for a configuration refusal and 503 for
an unavailable policy/provider resolver. Browser speech recognition remains
independently available; video links can still use published captions.

| Failure | Provocation and recovery |
| --- | --- |
| `NO_TRANSCRIPTION_MODEL` | Choose Automatic with no active compatible default credential and transcription model. Configure one under AI providers, then retry the capability read. |
| `TRANSCRIPTION_MODEL_UNAVAILABLE` | Pin a removed model, disable its default credential, or exclude it from the credential allowlist. Restore that exact model or explicitly save another choice/Automatic; no substitute is called. |
| `TRANSCRIPTION_MODEL_POLICY_INVALID` | Store malformed YAML/JSON or a partial pin in an isolated test. Choose Automatic or a complete provider/model pair and save to repair it. |
| `TRANSCRIPTION_MODEL_POLICY_UNAVAILABLE` | Deny the policy read or make the org config root unavailable. Restore readable configuration; an absent file in an available root remains Automatic. |
| `TRANSCRIPTION_MODEL_RESOLUTION_FAILED` | Break provider catalog or credential resolution with no usable Automatic alternative, or break the pinned provider. Restore the service and retry; private responses and credentials never appear in the code. |

## Unreachable from the UI, still enforced

| Guard                                                            | Guarded by                                                                                         | Proven by                                                    |
| ---------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| Organization workload total must fit current deployment capacity | Live total validation and disabled Save; the server checks the spawner independently before saving | Backend policy-save checks in [automation.md](automation.md) |
