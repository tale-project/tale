# What the automated suites already own

Manual effort is expensive; spend it where a headless run cannot judge. Read
this before hand-verifying anything, and read the seam notes before running a
suite alongside the automated ones — they drive the same stack.

## Coverage map

One row per case group, carried over from the per-suite coverage tables the
guides used to hold. **Don't** re-verify an automated row by hand: a red there
is a spec failure and belongs in the gate, not in a round.

Legend: ✅ fully automated · 🔶 partially automated · ⛔ manual-only (no spec).

| Suite | Boxes | Status | Owning spec |
|---|---|---|---|
| [knowledge](../suites/knowledge.md) | Authenticated image intake, MIME/5 MiB limits, reserved-source refusal, organization/parent/binding access, retention lock, absolute REST URL replay, and explicit UI removal | ✅ automated | `backend/domains/products/images.test.ts`, `backend/domains/products/images.routes.test.ts`, `backend/domains/files/routes.register.test.ts`, `backend/domains/files/service.product-images.test.ts`, `backend/rest/product-images.test.ts`, `backend/rest/v1-core.test.ts`, `backend/rest/v1-core.crm.test.ts`, `app/features/products/hooks/use-product-image-upload.test.ts`, `app/features/products/components/product-edit-dialog.test.tsx` |
| [knowledge](../suites/knowledge.md) | Screenshot seeding waits for existing table rows despite loading and notification status regions, and matches active/retired WebDAV device labels exactly | ✅ browser | `tests/e2e/specs/docs-screenshot-seed.spec.ts` (synthetic DOM; no organization writes) |
| [knowledge](../suites/knowledge.md) | Native contact and product dates reach list and paginated UI records without losing cursor metadata | ✅ unit | `app/lib/backend/engagement.test.ts` |
| [settings](../suites/settings.md) | WebDAV native null timestamps leave active credentials revocable; revoked credentials have a status and no action | ✅ component | `app/features/settings/webdav/components/webdav-settings.test.tsx` |
| [settings](../suites/settings.md) | WebDAV discovery reaches the backend through development and preview proxies without widening CORS origins | ✅ integration | `vite-plugins/webdav-preflight.test.ts` (real Vite servers and HTTP backend) |
| [navigation](../suites/navigation.md) | Optional analytics: runtime disablement, safe SPA pageviews, private route templates, DNT/GPC and collector boundary | ✅ automated | `packages/ui/src/analytics/browser.test.ts`, `packages/ui/src/analytics/server.test.ts` |
| [settings](../suites/settings.md) | Foreground egress proxy boots, stops gracefully and restarts with read-only `/tmp` | ✅ automated | `tests/integration/container-smoke-test.ts` and `compose.test.yml` (real proxy health, refused temporary-file creation, shutdown exit/log and same-container restart) |
| [settings](../suites/settings.md) | Explicit native configuration recovery retains the superseded plan and verified writes; rejects unrelated edits, omitted resources and wrong organizations; replays interrupted replacement through managed provisioning | ✅ automated | `tools/cli/src/lib/config/platform-apply.test.ts` |
| [settings](../suites/settings.md) | Managed Compose failures identify validation or startup and preserve pending runtime receipts without exposing private inputs | ✅ automated | `tools/cli/src/lib/deployment/runtime-command.test.ts` (exit/exception paths, safe fallback and failure propagation through the real runtime orchestration with injected Docker responses) |
| [projects](../suites/projects.md) | Project-agent REST CRUD, project isolation, roles, secret grants (an unknown name refused as `PROJECT_AGENT_SECRET_UNKNOWN`), the `expectedUpdatedAt` precondition (`PROJECT_AGENT_STALE`) and session parity | ✅ automated | `backend/rest/v1-project-agents.test.ts`, `backend/domains/projects/service.agents.test.ts`, `backend/domains/projects/routes.test.ts`, `backend/rest/project-agents-check.ts` (real HTTP/Postgres checks in `checkRestProjectAgents`, the refusal and the conditional save included) |
| [projects](../suites/projects.md) | Project-agent model search accepts the API model ID as well as the display name and provider, and saves the selected provider/model pair | ✅ component | `app/features/projects/components/project-agent-dialog.test.tsx` |
| [projects](../suites/projects.md) | REST project tasks, threads, files, installed automations, runs and token webhooks: path isolation, member/editor roles, archived-project refusals, strict bodies, MCP access and opaque private resources | ✅ automated | `backend/rest/project-scope-check.ts` (the real HTTP/Postgres checks — including the `Idempotency-Key` replay, reuse and concurrent pair, the run delete, and the webhook door's one 403 — plus concurrent archive/queued-turn checks in `checkRestProjectAgents`), `backend/rest/v1-tasks.test.ts`, `backend/rest/v1-threads.scope.test.ts`, `backend/rest/v1-automations.project-scope.test.ts`, `backend/rest/v1-automations.contract.test.ts` (version reads, run summaries with `status`/`include`/cursor, `Idempotency-Key`, run delete), `backend/domains/automations/triggers.webhook.test.ts` (the 403 collapse, statuses by code, the sender and trigger budgets), `backend/domains/automations/store.idempotent-start.test.ts`, `backend/domains/automations/store.delete-run.test.ts`, `lib/shared/event-types.test.ts` (an event trigger binds only an event a producer raises), `backend/rest/v1-core.contract.test.ts` (knowledge scope), `backend/domains/chat/store.test.ts` (queued-turn scope) |
| [settings](../suites/settings.md) | `SET-B11` (REST model discovery) | 🔶 backend | `backend/rest/v1-threads.contract.test.ts`, `backend/rest/v1-threads.test.ts` — org/user scoping, empty catalog, credential-free response, provider forwarded; live provider response stays manual |
| [knowledge](../suites/knowledge.md) | Retrieval admits every fused candidate against its live document BEFORE the page is cut (a trashed top match never empties a `limit: 1` page; `limit: 1` is a prefix of `limit: 3`), repeated passages are dropped after admission, `fusedScore` / `legs` / `diagnostics.admitted` are answered, an active entry's delete de-indexes its document at once, `<!DOCTYPE>` never leads a passage | ✅ automated | `lib/knowledge/retrieve.test.ts`, `lib/knowledge/dedupe.test.ts`, `backend/core/knowledge/search.test.ts`, `backend/domains/knowledge_entries/service.delete.test.ts`, `lib/knowledge/html-to-text.test.ts`, the `knowledge RAG loop` lane in `backend:integration` |
| [knowledge](../suites/knowledge.md) | REST documents: `PATCH` answers 200 with the document and its `indexing`, `expectedUpdatedAt` → 409 `DOCUMENT_STALE`, a knowledge entry's backing document refuses delete and content edits (409 `DOCUMENT_HAS_KNOWLEDGE_ENTRY`); REST project files and folders delete (204, opaque 404s, the shared 409/503 purge refusals, the `folder:mutate` budget) | ✅ automated | `backend/rest/v1-core.test.ts`, `backend/rest/v1-projects.test.ts`, `scripts/openapi/spec.test.ts`, the `REST resources` and `REST machine journey` lanes in `backend:integration` |
| [projects](../suites/projects.md) | REST project lifecycle and keys: `GET /projects` as list (keyset cursor, `archived` filter) or lookup, `PATCH` archive/restore and `DELETE` (admin, the `project:delete-cascade` budget, `mode`, the 409 refusals with their data), `externalItemId` and the task external ref canonical (NFC + trimmed) at the door, the domain and the backfill (`0093`), folder get-or-create folding case with the DB-enforced sibling index (`0092`) and its 409, the mint-time upload type gate, `runId` beside `executionId`, the project-first 404 order and the archived-task refusal; after the 2026-09-12 round-d evaluation: the `0098` twin repair (detach the normalization twins `0093` skipped, canonical-expression unique indexes on projects and tasks), `automationSlug` → 404 `AUTOMATION_NOT_FOUND` / 409 `AUTOMATION_NOT_DEPLOYED`, `ProjectFile.size` + `indexing` and the bind 201’s `size` + `mimeType`, `Task.archivedAt`, the trigger health stamps on the automation listing row, `deployed` on the trigger `PUT`, and the `__` hint on a slash-bearing automation URL | ✅ automated | `backend/rest/v1-projects.test.ts`, `backend/rest/v1-tasks.test.ts`, `backend/domains/projects/service.key.test.ts`, `service.delete.test.ts`, `service.agents.test.ts`, `backend/domains/folders/service.get-or-create.test.ts`, `backend/domains/tasks/external-ref.test.ts`, `lib/shared/utils/external-key.test.ts`, `scripts/openapi/spec.test.ts`, `backend/rest/project-scope-check.ts`, `backend/rest/v1-automations.contract.test.ts`, `backend/rest/v1-automations.project-scope.test.ts`, the `REST machine journey`, `REST task owner refusals + trigger health on the listing (round d)` and `REST external-key twin repair (0098)` lanes in `backend:integration` |
| [automations](../suites/automations.md) | Explicit vision-policy admission before a managed text-only turn | ✅ automated | `backend/core/lib/providers/resolve_vision_model.test.ts` (exact pin, missing model/provider, inactive or disallowed credentials, unavailable catalog/policy, safe diagnostics, Auto and native-vision preservation); `backend/domains/governance/vision-policy-read.test.ts` (real corrupt/oversize/symlink policy files, missing config root/org, fresh pin/slug reads, no cached Auto bypass); `backend/core/automations/agent_host.vision_policy.test.ts` (refusal before an operation, key or scheduled inference; exact successful pin carried to the turn). Actual model OCR quality and target connectivity remain deployment checks. |
| [knowledge](../suites/knowledge.md) | REST documents and entries after the 2026-09-12 evaluation: `GET /api/v1/documents/{id}/content` streams a file-backed Hub document (the shared download choreography — `Content-Disposition`, `Range` 206/416, HEAD, 503 + `Retry-After`) and serves a content-only document's inline text; a knowledge-entry create/supersede answers `{id, documentId}`; `GET .../knowledge-entries/{id}/versions` and `?topic=` + `supersededAt`; the entry's `content_hash` column written and backfilled (`0095`); `PATCH /documents/{id}` merges `metadata` per RFC 7396 and skips a no-op write (`updatedAt` untouched); search hits carry `matchedLegs` / `similarity` / `keywordScore`, admission runs before fusion, and the assistant's floor comes from `embedding.json` `minSimilarity` | ✅ automated | `backend/rest/v1-core.test.ts`, `backend/rest/shared.test.ts`, `backend/domains/documents/service.update.test.ts`, `backend/domains/knowledge_entries/service.test.ts`, `lib/knowledge/fusion.test.ts`, `lib/knowledge/retrieve.test.ts`, `backend/core/knowledge/search.test.ts`, `backend/domains/knowledge/admin.test.ts`, `scripts/openapi/spec.test.ts`, the `REST resources` lane in `backend:integration` (downloads an entry-backed document and compares the bytes to the entry's content) |
| [knowledge](../suites/knowledge.md) | REST knowledge search when the embedding provider fails (account refusal → 409 `EMBEDDING_CREDIT_EXHAUSTED`, never a 429; other provider failures → 503 `EMBEDDING_UPSTREAM_ERROR`; balance/plan refusals not retried) and `retry-indexing` skip reasons | ✅ automated | `backend/rest/v1-core.contract.test.ts`, `backend/core/knowledge/embedding.test.ts` |
| [knowledge](../suites/knowledge.md) | REST knowledge, documents and websites after the 2026-09-12 round-d evaluation: a knowledge-entry `PATCH` that repeats the active row (same trimmed `topic` and `content`) writes no version and answers that row's id, releasing the blob if one was uploaded first; `POST /api/v1/documents/{id}/retry-indexing` runs through the app's own guards (`queueRagIndexingRetry` — the write role, the `file:rag-retry` budget as the door-wide 429, the `unsupported` and `in-progress` skips, a bind-time opt-out opted back in — `rag-opt-out` is no longer a reason); every page of `GET /api/v1/websites/{id}/pages` carries `failCount` / `lastError` / `lastErrorKind` / `lastErrorAt` (knowledge-db public_web `10`) and the declared `discovered` / `active` status, the website its `failedPageCount` (`0099`) beside the attempted `crawledPageCount`, a listed page that redirects into a private address is refused before it is dialed (a loopback listener sees zero hits) and reads `insecure_public_http`, a listed page is never dead (a re-list resets its count, every scan probes it); every REST operation with its own byte cap names it in its 413 and `storageId` is declared opaque | ✅ automated | `backend/domains/knowledge_entries/service.test.ts`, `backend/domains/documents/service.retry-indexing.test.ts`, `backend/rest/v1-core.contract.test.ts`, `backend/rest/v1-websites.test.ts`, `backend/core/knowledge/crawl.test.ts`, `scripts/openapi/spec.test.ts` (the own-413 guard), the `REST resources`, `websites page failure` and `websites revival` lanes in `backend:integration` |
| [settings](../suites/settings.md) | `SET-F33` (MCP endpoint) — the wire contract behind the page: envelope validation, version negotiation, batches, tool-argument validation against the advertised schema (document tools included), `params` typing, the unissued `tools/list` cursor, 405 + `Allow` on other verbs, `isError` on failure-shaped results, `get_docs` serving a reference and never the builder prompt | ✅ automated | `backend/core/automations_builder/mcp_http.test.ts`, `lib/engine/selftest/dispatch.test.ts` |
| [projects](../suites/projects.md) | REST chat lifecycle: door-side model resolution (`CHAT_MODEL_UNKNOWN` / `CHAT_MODEL_AMBIGUOUS`), model facts and the default marker on `GET /models`, message `status` (a stop settles as `cancelled`) and `usage` (`costEstimateCents` included), the 202’s pre-minted `messageId` and the `queued` poll state until the worker opens the turn, streaming progress on the poll (`since` and `reasoningSince` slice the text and the reasoning independently, each with its offset/length pair and the reset-to-0 rule), `reasoningEffort` / `maxOutputTokens` on a send (a cap above the model’s ceiling refused; the cap a per-turn budget the tool rounds share — a later round gets what the earlier ones left, a round with nothing left is not run, a cut round marks the turn `finishReason: length` and its calls settle `invalid_args` unrun), `usage.costEstimateCents` rounded to a millionth of a cent, a named `locale` asking for the reply language on the system prompt and on the message (`localeFixed` through the job into the runtime directive and the wire-only notice; the app lane untouched), archive with `archivedAt` echoed from the domain’s own stamp / rename / delete / cancel routes, the stop of a send still queued (202 `cancelling` → a `cancelled` reply without a model call) and the 404’s `data.lastMessageId` / `data.lastStatus`, hidden regenerate-siblings invisible by id, blank-prompt and locale refusals | ✅ automated | `backend/rest/v1-threads.test.ts`, `backend/rest/v1-threads.contract.test.ts`, `backend/rest/v1-threads.scope.test.ts`, `backend/domains/chat/rest-turn.test.ts`, `backend/domains/chat/store.test.ts`, `lib/chat/turn.test.ts`, `lib/chat/context.test.ts`, `lib/chat/effort.test.ts`, `lib/chat/tools.test.ts`, `lib/shared/utils/language-name.test.ts`, `scripts/openapi/spec.test.ts`, the `REST resources` lane in `backend:integration` (the queued stop and the 404’s data on the real schema) |
| [projects](../suites/projects.md) | REST contract guards: every handler code is in the error-code registry the OpenAPI `Error.code` enum publishes, every `/api/v1` operation documents `X-Organization-Slug`, `/openapi.json` binds `servers` to the request origin, knowledge hits carry `documentId` | ✅ automated | `backend/rest/error-codes.test.ts`, `scripts/openapi/spec.test.ts`, `server.test.ts`, `backend/domains/knowledge/service.document-ids.test.ts` |
| [settings](../suites/settings.md) | OIDC error conformance: userinfo 401 `invalid_token` with the Bearer challenge, RFC 6749 envelopes on the token and authorization endpoints, the organization claim in discovery | ✅ automated | `backend/auth/oidc.test.ts` (pure mapping), `backend/auth/oauth-conformance.test.ts` (the mount wrapper), `backend/auth/oidc-integration.ts` (real-Postgres lane in `backend:integration`) |
| [projects](../suites/projects.md) | REST door hygiene after the 2026-09-12 evaluation: a key in `x-api-key` refused everywhere (401), a URL over 32 KiB answered 414 `URI_TOO_LONG` in the envelope, one trailing slash under `/api/v1/` routed, key-less `OPTIONS` → 204 + `Allow` with no CORS headers, `X-Tale-Api-Version` + `no-store` on every answer (the 404/405 catch-all included), `Content-Length` on a JSON `HEAD`, no content headers on a 204, a blank `cursor`/`limit` refused, the slug header folded to lowercase, house prose for every schema refusal, a 413 that names the cap | ✅ automated | `backend/lib/http-hygiene.test.ts`, `backend/rest/v1.not-found.test.ts`, `backend/rest/v1.test.ts`, `backend/rest/shared.test.ts`, `scripts/openapi/spec.test.ts` (declared response headers, required `Error.code`, the code-naming debt list, the contract fingerprint ↔ `info.version` lock) |
| [projects](../suites/projects.md) | REST error diagnostics across every family: `INVALID_BODY` naming the failing field with `data.issues`, `INVALID_CURSOR` / `INVALID_LIMIT` for a mangled cursor or a non-numeric page size, a case-insensitive Bearer scheme and `WWW-Authenticate` on every 401 | ✅ automated | `backend/rest/v1.test.ts`, `backend/rest/v1-core.contract.test.ts`, `backend/rest/v1-core.test.ts`, `backend/rest/shared.test.ts` |
| [settings](../suites/settings.md) | `SET-F30`, `SET-F37`, `SET-B12`, `SET-A6`; governance `GOV-F13`, `GOV-B7` | 🔶 component + adapter | `app/features/settings/sandboxes/sandbox-quota-editor.test.tsx` (three limits, automatic total, exact-capacity acceptance, over-capacity and unavailable-capacity refusal, preservation, Save/Discard, reader permissions), `sandbox-quota-editor.browser.test.tsx` (keyboard save and discard in Chromium), `sandbox-capacity.test.tsx` (deployment count/capacity, organization count without a separate ceiling, measured resources, unknown state, refresh), `sandboxes-settings.test.tsx` (writer-only workspace queries and metadata visibility, including cached rows), `app/lib/backend/settings-sandbox.test.ts` (org-scoped fetch and quota cache invalidation); real infrastructure readings, persistence after reload and assistive-technology feedback remain manual |
| [settings](../suites/settings.md) | Authoritative sandbox quota save boundary | ✅ automated | `backend/domains/governance/routes.test.ts` (exact-capacity acceptance, over-capacity and unavailable-capacity rejection before audit/file writes, forged total ignored, deployment capacity reread at each save, invalid-field and role boundaries) |
| [settings](../suites/settings.md) | Idle sandbox reclamation under deployment capacity pressure | ✅ automated | `services/sandbox/src/session/session-routes.test.ts` (released compute reclaimed before admission, persistent workspace retained, busy/pinned/unknown/legacy runtime protection, delayed release after reacquire, concurrent creates and stop failure); `services/sandbox-runtime/daemon/src/activity-gate.test.ts` (atomic idle claim, active-operation protection and frozen execution gate); `services/sandbox/src/backend/docker/docker-session-backend.test.ts`, `backend/kubernetes/k8s-session-backend.test.ts` (incarnation-fenced compute removal and retained workspace); `backend/domains/sandbox/idle-release.test.ts` (ticket capture, transactional queueing and committed-state checks), `idle-release.integration.ts` (28 real-Postgres checks: organization/allocation/owner guards, admission and release ordering, atomic cancellation and release-job rollback, post-op retry on an already-stopped workflow); `backend/core/node_only/sandbox/agent_session.test.ts` (`ensureAgentSession` refuses a false allocation resume before acquiring runtime compute) |
| [settings](../suites/settings.md) | Build-cache network allocation, session isolation and idle helper retirement | ✅ automated | `services/sandbox/src/buildkit-network-pool.test.ts`, `buildkit-resources.test.ts`, `buildkit-lifecycle.test.ts` (daemon routes/DNS, dense allocation, owned empty-network repair, immutable container retirement, active-session protection and retained caches); `session/buildkit-network-guard.test.ts`, `backend/docker/docker-session-build-cache.test.ts` (guard before attachment, planned network identity, leases and failure cleanup) |
| [settings](../suites/settings.md) | Adaptive inner Docker networks, IPv6 startup guards and conservative Kubernetes capacity | ✅ automated | `services/sandbox-runtime/daemon/src/inner-docker-network.test.ts`, `build-cache-startup.test.ts`, `env-store.test.ts` (routes, Pod addresses, DNS/service observations, operator override, retained Pod Docker bridges, matching daemon/NAT ranges, protected startup environment); `services/sandbox/src/config.test.ts`, `session/docker-session-args.test.ts`, `backend/kubernetes/k8s-session-pod-spec.test.ts` (validated DinD-only override on both backends, no Docker cache or unsafe sysctls in Pods); `egress-firewall.test.ts`, `capacity.test.ts` (IPv6 guard fallback, exact configured-resolver DNS exceptions, unknown pods counted without hiding capacity); `tools/cli/src/lib/compose/services/compose-parity.test.ts` |
| [tasks](../suites/tasks.md) | `TASK-F17` (retry after a harness switch), `TASK-B5` / [governance](../suites/governance.md) `GOV-F18` (spend cap + usage ledger) | 🔶 backend | `backend/core/tasks/agent_run_host.reap.test.ts` (drive-failure cancel, predecessor reap-and-wait before launch), `backend/domains/tasks/kick-plan.test.ts` (predecessor carried across a harness switch), `backend/core/chat/external_turn_shared.test.ts` (window vs EOF, background-task ledger), `backend/domains/governance/budget-gate.test.ts` + `backend/domains/sandbox/turn-budget.test.ts` (allowance math, reservations, refusal), `backend/core/node_only/sandbox/gateway_key_settlement.test.ts` + `backend/core/automations/agent_host.release.test.ts` + `backend/domains/sandbox/spend-settlement.test.ts` + `gateway-keys.test.ts` (spend booked before delete, deferred facts, ledger attribution), `backend/integration-check.ts` (`checkSandboxGatewayKeyReclaim`: real-Postgres reservations, refusal, settle, sweep); the live process count in the container and the Usage page reading stay manual |
| [projects](../suites/projects.md) | Runtime image carries every managed harness (Qwen Code included) | ✅ automated | `tests/integration/container-sandbox-runtime-test.ts` (registry-driven: every `managed: true` harness's exec binary on the agent PATH), `services/sandbox-runtime/daemon/src/tale-qwen-run.test.ts`, `tale-hermes-run.test.ts` (returned SDK failures end as errors; resume restores the stored conversation) |
| [performance](../suites/performance.md) | Validated JSON reads on `/api/app/*` and `/api/v1/*`: every 200 JSON `GET`/`HEAD` carries an `ETag` over its bytes and `Cache-Control: private, no-cache`, a matching `If-None-Match` (weak and list forms included) answers a bodiless 304 that keeps the validator and the request id, a route's own validator or directive is kept, and streams, downloads, errors and writes are untouched | ✅ automated | `backend/lib/conditional-get.test.ts` |
| [performance](../suites/performance.md) | `PERF-P8` (idle page silence) — the video-link chip reads key under the entity every job write hints and poll only while a job is live; the deferred-send claim hints inside the park transaction | 🔶 adapter + backend | `app/lib/backend/chat.test.ts`, `backend/domains/video_links/hints.test.ts`, `backend/domains/chat/deferred-sends.test.ts`; the live network panel reading stays manual |
| [projects](../suites/projects.md) | REST run read `?fields=` (exactly the keys named, an unknown key refused as `INVALID_QUERY`, the strict query kept) and the run listing's inline byte budget (a page ends at the last row that fits with a cursor there, a single row above it still answers, a summary page is never cut) | ✅ automated | `backend/rest/v1-automations.contract.test.ts`, `scripts/openapi/spec.test.ts` |
| [projects](../suites/projects.md) | REST pagination families and door strictness after the 2026-09-12 round-d evaluation: every list's 200 schema carries `x-tale-pagination` (`keyset` / `offset` / `none`) and the family dictates its shape (the envelope-family guard, the preamble naming every collection key), `GET /projects` and `GET /projects/{id}/files` answer `continueCursor` beside the deprecated `cursor` twin (a lookup answers `isDone: true`), `GET /websites/{id}/pages` answers `isDone` + `continueCursor` and takes `?cursor=` (never with `offset`), `INVALID_LIMIT` / `INVALID_CURSOR` name their parameter under `data.issues`, a blank project `key` / folder `parentId` and an empty `contacts/bulk` batch are 400, folder names refuse the shared forbidden class (`/`, `\`, control characters), `conversations/sync` answers `ATTACHMENT_NOT_STAGED` for a malformed or foreign `storageId`, the `UNSUPPORTED_FILE_TYPE` sentence lists the allowlist sorted | ✅ automated | `scripts/openapi/spec.test.ts` (the pagination families), `backend/rest/v1-projects.test.ts`, `backend/rest/v1-websites.test.ts`, `backend/rest/shared.test.ts`, `backend/rest/v1-core.crm.test.ts`, `lib/shared/utils/plain-name.test.ts`, `backend/domains/folders/service.get-or-create.test.ts`, `backend/domains/conversations/api-sync.attachments.test.ts`, the `REST machine journey`, `conversation API` and `websites` lanes in `backend:integration` |
| [projects](../suites/projects.md) | REST doors after the 2026-09-13 round-e evaluation: `Idempotency-Key` outside printable ASCII is 400 `INVALID_HEADER` on the run start and the chat send (one reader, a blank value still no key); `POST …/tasks/{id}/start` answers a `workflowSlug` nobody saved as 404 `AUTOMATION_NOT_FOUND` and a saved-but-undeployed one as 409 `AUTOMATION_NOT_DEPLOYED` before the execute budget is charged (`not_started` left for a withdrawn deployment); the upload allowlist keys on the extension the name must carry (`CON`, `attachment-4711`, `program.exe`+`text/plain` refused at the mint and the bind, the app lanes inheriting), file and folder names stored trimmed and NFC-normalized, `FOLDER_NAME_INVALID` naming the rule under `data.issues`, a malformed upload body refused before the budget; `externalState: closed` stamps the park it makes and `open` lifts exactly that park (or `done`) back to `backlog`, a board move ending the mirror's claim; a version's `testsPassed` persists the deploy gate's refusal and the MCP save's own run, with `testsCheckedAt`, the latest verdict winning; run listings name the run `id` beside `runId` | ✅ automated | `backend/rest/shared.test.ts` (`readIdempotencyKey`), `backend/rest/v1-automations.contract.test.ts`, `backend/rest/v1-threads.test.ts`, `backend/rest/v1-tasks.test.ts`, `backend/rest/v1-projects.test.ts`, `lib/shared/file-types.test.ts`, `lib/shared/utils/plain-name.test.ts`, `backend/domains/folders/paths.test.ts`, `backend/domains/tasks/external-ref.test.ts`, `backend/domains/automations/store.deploy.test.ts`, `backend/domains/automations/store.save-version.test.ts`, `backend/domains/automations/store.run-summary.test.ts`, `lib/engine/api/dispatch.refusals.test.ts`, `scripts/openapi/spec.test.ts`, the `REST machine journey`, `REST task owner refusals` and `MCP deploy gate` lanes in `backend:integration` |
| [projects](../suites/projects.md) | REST task intake binds the Setup folder by name: `setupFolderName` resolves a root folder of the URL project without regard to case, inside the intake transaction, and stores its id as the task's `externalUrl` (the desks' binding convention the app's from-external-issue door follows, through the one shared `resolveSetupFolderId`), on the create and on every repeat; a name no root folder carries is 400 `SETUP_FOLDER_MISSING` and nothing is created; a body carrying both `setupFolderName` and `externalUrl` is 400 `INVALID_BODY` naming `setupFolderName`; `externalUrl` itself stays an absolute http(s) URL, the code is registered and introduced in contract 1.7.0 | ✅ automated | `backend/rest/v1-tasks.test.ts`, `backend/domains/tasks/external-ref.test.ts`, `backend/rest/error-codes.test.ts`, `scripts/openapi/spec.test.ts` |
| [knowledge](../suites/knowledge.md) | REST knowledge, documents and websites after the 2026-09-13 round-e evaluation: `POST /api/v1/projects/{id}/files/{documentId}/retry-indexing` (the REST twin of **Index now** — the delete sibling's gate and opaque 404 through the shared `loadProjectFile` load, the Hub door's core and its `{status, reason}`, a bind-time `skipRagIndexing` opt-out lifted, `RBAC_FORBIDDEN` and the `file:rag-retry` 429 in the shared envelope; the Hub door still answers 404 for a project file); a content-only document's download judges `If-Modified-Since` against the `Last-Modified` it issues (whole-second precision, `If-None-Match` taking precedence, HEAD alike); `POST /api/v1/websites/{id}/search` refuses a `limit` outside 1..100 or not a whole number with 400 `INVALID_BODY` naming it instead of clamping; the app's retry names an untracked blob as its own sentence; both download operations declare the conditional parameters and the 304 from one shared spec block | ✅ automated | `backend/lib/conditional-get.test.ts`, `backend/rest/v1-core.test.ts`, `backend/rest/v1-projects.test.ts`, `backend/rest/v1-websites.test.ts`, `backend/domains/documents/service.retry-indexing.test.ts`, `scripts/openapi/spec.test.ts`, the `REST machine journey`, `REST resources` and `websites REST family` lanes in `backend:integration` |
| [projects](../suites/projects.md) | REST doors after the 2026-09-14 round-g evaluation: an `Idempotency-Key` sent blank or over 255 characters is 400 `INVALID_HEADER` (never "no key"); `POST /api/v1/contacts/bulk` judges rows one by one (`errors[].issues`, `INVALID_BODY` per row, 201 for the batch); `GET /api/v1/contacts?source=` refuses a value outside the closed set; `products.imageUrl` refuses private and metadata hosts by the crawl-target rule; a contact or product `PATCH` that changes nothing writes nothing (the precondition still judged first); `If-Match` on `PATCH /api/v1/documents/{id}` → 412 `PRECONDITION_FAILED` with `data.etag`; `GET /api/v1/me` answers `capabilities.developer` per role; `GET /api/v1/projects/{id}/files/{documentId}` answers one file row with its `indexing`; the 429 envelope carries a sentence and `requestId`; `//api/v1/...` answers the JSON 404; the Permissions-Policy no longer names `bluetooth`; every MCP string argument refuses a blank | ✅ automated | `backend/rest/shared.test.ts`, `backend/rest/v1-automations.contract.test.ts`, `backend/rest/v1-threads.test.ts`, `backend/rest/v1-core.crm.test.ts`, `backend/rest/v1-core.test.ts`, `backend/domains/contacts/service.update.test.ts`, `backend/domains/products/service.test.ts`, `backend/domains/projects/service.identity.test.ts`, `backend/rest/v1-projects.test.ts`, `backend/lib/http-hygiene.test.ts`, `server.test.ts`, `packages/ui/src/server/security-headers.test.ts`, `backend/core/automations_builder/mcp_http.test.ts`, `app/routes/docs.test.tsx` (the `/docs` header links the guides and `/openapi.json`) |
| [chat](../suites/chat.md) | REST chat after the 2026-09-14 round-g evaluation: a reply that settles with no text carries no empty `text` part (a reasoning-only cut keeps its `reasoning` part; a stop before any text settles with `parts: []`), and a call the reply cap withheld says whether its arguments were cut or complete | ✅ automated | `lib/chat/turn.test.ts` |
| [knowledge](../suites/knowledge.md) | Indexing and crawling after the 2026-09-14 round-g evaluation: `indexing.errorCode` is the closed RAG vocabulary — `unsupported_type`, `image_no_vision`, `empty`, `not_text`, `malformed` are terminal (`unsupported`, a retry reproduces the answer), the rest `failed` with the job retrying `embedding_upstream`/`indexer_error`/`index_rebuilding` — the raw cause stays in the platform log and a NUL byte never reaches the corpus; the stored `mimeType` is resolved from the extension first (`json`, `yaml`, `yml` mapped); the corpus scope stamps follow a document out of a deleted or detached project and are re-stamped on a skip-unchanged re-ingest; a `tls_error` fetch kind, a same-host link naming a port never dialed, `X-Robots-Tag: noindex` honoured (`robots_noindex`), a content type the crawler cannot read recorded as `unsupported_content`; `GET /api/v1/websites?status=` refuses a value outside the enum | ✅ automated (crawl outcomes 🔶 — the page rows and the scan-end `error` verdict stay in `KNOW-F14`) | `backend/domains/knowledge/service.status-codes.test.ts`, `backend/domains/knowledge/scope-reconcile.test.ts`, `backend/domains/projects/service.delete.test.ts`, `lib/knowledge/sanitize-text.test.ts`, `backend/core/lib/knowledge/extraction/text.test.ts`, `lib/shared/file-types.test.ts`, `lib/net/safe-fetch.test.ts`, `lib/knowledge/crawl-parse.test.ts`, `backend/rest/v1-websites.test.ts`, `backend/rest/v1-projects.test.ts` |
| [automations](../suites/automations.md) | Runs after the 2026-09-14 round-g evaluation: at most one live run per task — the `automation_runs_one_live_per_task` partial unique index backs the in-transaction probe, so two concurrent `POST …/tasks/{id}/start` land exactly one run and the loser answers `already_running` with its id; `Run.failureCode` / `RunSummary.failureCode` name the stable cause (engine, provider and agent codes in one enum) and are `null` off `failed`; a cancel clears `detail` and answers the run's `status` beside `cancelled` | ✅ automated | `backend/domains/tasks/external-ref.test.ts`, `backend/rest/v1-tasks.test.ts`, `backend/integration-check.ts` (`automations: concurrent SERIALIZABLE starts land exactly one run (index backstop)`), `backend/domains/automations/store.run-summary.test.ts`, `backend/rest/v1-automations.contract.test.ts`, `backend/domains/automations/store.terminal-asks.test.ts`, `backend/domains/automations/store.terminal-approvals.test.ts` |
| [conversations](../suites/conversations.md) | A conversation mirrored onto a contact that was moved to the trash is frozen, never re-homed: a later content snapshot answers 409 `CONVERSATION_CONTACT_TRASHED`, the receipt reports `contactStatus: "trashed"`, a contact recreated under the same `externalId` inherits nothing, and a teardown still applies | ✅ automated | `backend/domains/conversations/api-sync.integration.ts` (`conversation API trashed-contact mirror is frozen and never re-homed`, run by `backend:integration`) |
| [knowledge](../suites/knowledge.md) | `KNOW-F2` half: a OneDrive or Google Drive folder sync creates its sync-root folder as soon as the config exists and files an unchanged, already-present document (an earlier one-time import at the root) under the selected folder — bound, moved, corpus scope re-stamped — while a one-time re-import and a directly picked file keep their placement | ✅ automated | `backend/core/onedrive/import_files.test.ts`, `backend/core/google_drive/import_files.test.ts`, `backend/integration-check.ts` (`onedrive sync import (grant token, reused pipeline, substrate)` — the root import adopted into `ODReports`) |
| [projects](../suites/projects.md) | REST chat after the 2026-09-14 round-h evaluation: the poll's `since` / `reasoningSince` are UTF-16 code units and a value that lands inside a surrogate pair is snapped down to the pair's start (the answered offsets say where the slice began, so `held.slice(0, textOffset) + text` always reassembles); a `content` with no visible text (Default_Ignorable code points, format controls) is 400 `must not be blank`; an archive while a turn runs is 409 `CHAT_TURN_IN_PROGRESS`, and a thread or project archived after a send was accepted settles the reply `cancelled` with the prompt kept; the execute lane is charged only after the body and the `Idempotency-Key` header pass; `GET /models` answers `harnesses`; `Location` on the thread create | ✅ automated | `backend/rest/v1-threads.test.ts`, `backend/domains/chat/rest-turn.test.ts`, `backend/rest/shared.test.ts`, `lib/shared/utils/visible-text.test.ts`, `scripts/openapi/spec.test.ts` |
| [knowledge](../suites/knowledge.md) | Crawling after the 2026-09-14 round-h evaluation: robots.txt `Disallow` governs every admission — the sitemap, the link walk AND the links a rendered page yields — and every non-listed fetch; the rules are kept on the website row (`robots_disallow`), a 5xx/429 robots.txt keeps the last known rules, a 4xx clears them; a stored page a new rule covers is retired (`deleted`, off the listing and out of the corpus) while a listed URL under the rule stays the operator's instruction; a listed page the site 404s keeps an honest row (`http_error`, one failed attempt, no chunks) and is re-probed; `http://` is refused as `WEBSITE_DOMAIN_INVALID`; `websites?scanInterval=` refuses a value outside the set; `idle` left `Website.status`; fetch errors carry no OpenSSL handles or runtime paths; `POST …/sync` and the retry-indexing doors are body-less | ✅ automated | `lib/knowledge/crawl-parse.test.ts`, `lib/net/crawl-host-policy.test.ts`, `lib/net/safe-fetch.test.ts`, `backend/rest/v1-websites.test.ts`, `app/features/websites/hooks/use-websites-table-config.test.tsx`, the `websites robots` and `websites revival` lanes in `backend:integration` |
| [knowledge](../suites/knowledge.md) | Search after the 2026-09-14 round-h evaluation: the dense leg runs an exact scan under 5,000 candidate chunks and an iterative HNSW scan above it, so a scope filter never starves the post-filter into an empty leg; a document's repeated passages are embedded once (`passage_repeat` rows carry no vector and sit outside both legs); `diagnostics.dense` says whether the vector leg ran; RRF ties break keyword-first, then by id; control characters are stripped from served passages; `query` is capped at 2000 | ✅ automated | `backend/core/knowledge/corpus.test.ts`, `backend/core/knowledge/indexing.test.ts`, `lib/knowledge/retrieve.test.ts`, `lib/knowledge/fusion.test.ts`, `lib/knowledge/sanitize-text.test.ts`, `backend/core/knowledge/search.test.ts`, the `knowledge indexing embeds a repeated passage once` lane in `backend:integration` |
| [automations](../suites/automations.md) | Runs after the 2026-09-14 round-h evaluation: at most one live run per TASK, whichever automation asks — the `automation_runs_one_live_per_task_subject` partial unique index and the per-task lock back the probe, so a second `POST …/tasks/{id}/start` naming ANOTHER automation answers `already_running` with the live run's id; a task id of `__` is the teaching 404 | ✅ automated | `backend/domains/tasks/external-ref.test.ts`, `backend/rest/v1-tasks.test.ts`, the two per-task lanes in `backend:integration` |
| [conversations](../suites/conversations.md) | The mirror after the 2026-09-14 round-h evaluation: `POST /api/v1/contacts/{id}/restore` thaws a frozen mirror (the create's own 409s when a live contact took the key; a live contact is a no-op); `GET /api/v1/conversations?source=` lists every mirror newest first, `contactStatus` narrows, the keyset cursor is signed under the source; a re-keyed contact's binding follows its current `externalId` (the old id is 409 `CONVERSATION_CONTACT_CONFLICT`); the receipt carries `contactId`; `Location` on every create of one addressable resource; the document `PATCH` answers its `ETag` | ✅ automated | `backend/rest/v1-core.crm.test.ts`, `backend/rest/v1-conversations.test.ts`, `backend/domains/contacts/service.update.test.ts`, `scripts/openapi/spec.test.ts`, the restore / re-key / listing lanes in `api-sync.integration.ts` |
| [skills](../suites/skills.md) | Doors after the 2026-09-14 round-h evaluation: the skill file read is validated (`ETag`, `Last-Modified`, 304 on `If-None-Match` / `If-Modified-Since`); a missing session is the coded `UNAUTHORIZED` envelope on `/events` and the app routes; `Expect` is stripped at the proxy; the rendered reference keeps a pasted key out of `localStorage`; the MCP `start_run` takes `idempotencyKey` through the REST ledger, `set_trigger` answers a webhook token once with `deployed`, `get_automation` reads `version: "deployed"`, `list_versions` marks the deployed row, every capability refusal carries a `code`, the registry holds deployed automations only, an enum argument outside its set is named with the set | ✅ automated | `backend/rest/v1-core.contract.test.ts`, `backend/auth/session.test.ts`, `app/routes/docs.test.tsx`, `lib/engine/api/dispatch.refusals.test.ts`, `backend/core/automations_builder/mcp_http.test.ts`, `lib/chat/capabilities.test.ts`, `backend/domains/chat/capabilities.test.ts` |
| [knowledge](../suites/knowledge.md) | `KNOW-F16` half: a cloud-sync config that fails opens a failure episode on its row (`error_since_ms` / `failure_notified_at_ms`), stamps `needs-reauth` for a dead grant, tells the owner once per episode (at once for a dead grant, after an hour otherwise, again only when the cause escalates), hints the folder rows so the badge shows without a reload, and the run that reaches the source again clears the episode and marks the bell read; the hub folder listing decorates active AND errored configs with `sync` (provider, status, needsReauth, owner) and a directly picked synced file carries the same | ✅ automated | `backend/domains/onedrive/sync-health.test.ts`, `backend/domains/onedrive/service.failure-notify.test.ts`, `app/features/documents/hooks/use-documents-table-config.test.tsx` (the Source cell), `backend/integration-check.ts` (`onedrive dead grant: one needs-reauth episode → bell + flagged folder, cleared by recovery`) |
| [notifications](../suites/notifications.md) | `NOTIF-F16` half: the `cloud_sync_failed` row's keys exist in every locale and substitute fully (static titles), both deep-link builders (bell + email) open the listing that holds the synced item, the type is actionable and its server-side inbox mirror matches the catalogs | ✅ automated | `lib/i18n/notification-keys.test.ts`, `app/features/notifications/lib/notification-target.test.ts`, `app/features/notifications/lib/notification-target.parity.test.ts`, `backend/core/notifications/notification_messages.test.ts` |
| [knowledge](../suites/knowledge.md) | `KNOW-F2` half: the Documents page lists ONE folder at a time — the root page (`GET /documents/paginated` with no `folderId`) answers only the documents that sit in no folder, a folder id answers that folder, and there is no shape that lists the whole hub; a synced OneDrive/Google Drive folder's files stand inside their folder, never beside it at the root (the first Postgres port listed every document under the root — one row rendered twice, gone from both places when the folder was deleted, 2026-09-14); a cloud import refreshes the folder list as well as the documents, so its sync-root folder shows without a reload | ✅ automated | `backend/domains/documents/service.paginated.test.ts`, `backend/integration-check.ts` (`hub root page lists unfiled documents only (synced folder files stand in their folder, not beside it)` and `paginated hub walk (keyset, disjoint, exhausts, one folder at a time)` — a document moved into `Contracts/2026` leaves the root walk and is paged from its folder) |
| [projects](../suites/projects.md) | REST `folderId=root` (contract 1.10.0): `GET /api/v1/documents?folderId=root` answers the hub documents in no folder and `GET /api/v1/projects/{id}/files?folderId=root` the project's files in no folder — the root no folder id could name — while a folder id answers that one folder (a foreign id stays the opaque 404 `FOLDER_NOT_FOUND`) and an omitted `folderId` still lists everything | ✅ automated | `backend/rest/v1-core.test.ts` (`GET /documents folder filter`), `backend/rest/v1-projects.test.ts` (`GET /projects/:id/files folder filter`), `scripts/openapi/spec.test.ts` (the fingerprint moved with 1.10.0) |
| [projects](../suites/projects.md) | REST file content honours `If-None-Match` / `If-Modified-Since`: the preconditions travel to the store with the `Range`, its 304 comes back bodiless with the validators, and the lane answers `private, no-cache`; a content-only Hub document's inline branch judges both validators itself | ✅ automated | `backend/domains/files/service.content.test.ts`, `backend/rest/v1-projects.test.ts`, `backend/rest/v1-core.test.ts`, `backend/lib/conditional-get.test.ts` |
| [conversations](../suites/conversations.md) | The Inbox status badges share one `/conversations/counts` request per connector (keyed on the fetch, narrowed in `select`) | ✅ automated | `app/lib/backend/conversations.test.ts` |
| [accessibility](../suites/accessibility.md) | Layer | Status | Where |
| [accessibility](../suites/accessibility.md) | Per-component axe (WCAG 2.1 AA) | ✅ automated | `checkAccessibility()` — `packages/ui/tests/utils/a11y.ts` (axe via `vitest-axe`); ~30+ component `.test.tsx` call it |
| [accessibility](../suites/accessibility.md) | Per-story axe (WCAG 2.1 AA) | ✅ automated | Storybook `@storybook/addon-a11y` + `@storybook/addon-vitest` (`packages/ui/src/storybook/main.ts`); rules `wcag2a/wcag2aa/wcag21aa/best-practice` (`preview.tsx`) |
| [accessibility](../suites/accessibility.md) | Keyboard flows (`A11Y-A3`/`A11Y-A5`) | 🔶 partial | `keyboard.spec.ts` (tab order / shortcuts; **no axe**) |
| [accessibility](../suites/accessibility.md) | Responsive / mobile shell (`A11Y-A14`) | 🔶 partial | `responsive.spec.ts` (viewport layout; **no axe**) |
| [accessibility](../suites/accessibility.md) | Full-page WCAG audits (`A11Y-A1`/`A11Y-A4`/`A11Y-A8`/`A11Y-A13`) | ⛔ manual-only | no axe in e2e — this guide |
| [approvals](../suites/approvals.md) | `APV-F5`–`APV-F6` (ask card) | 🔶 component | — (no e2e; `run-ask-card.test.tsx`: mirror-first submit, blank-answer disable) |
| [approvals](../suites/approvals.md) | `APV-F1`–`APV-F3`, `APV-B1` | 🔶 backend + component | — (no e2e; `backend/domains/approvals/gate.test.ts`, `gate.policy.test.ts` (real-file invalid policy, unavailable config, immediate refresh and pending-record authority), `backend/core/approvals/policy.test.ts`; `run-detail.test.tsx` renders a UUID approval, rejects the exact operation and omits null finish dates) |
| [approvals](../suites/approvals.md) | `APV-F6`–`APV-F7` (resume) | 🔶 backend | — (no e2e; `convex/automations/human_asks.test.ts`) |
| [approvals](../suites/approvals.md) | `APV-F7b` (ask alerts) | 🔶 backend | — (no e2e; `human_asks.test.ts`: bell fan-out + `getTaskOpsIndicators.askingTaskIds`) |
| [approvals](../suites/approvals.md) | `APV-F8`–`APV-F9` | 🔶 backend | — (no e2e; `convex/tasks/task_reviewer.test.ts`, `review_mutations.test.ts`, `pending_reviews.test.ts`) |
| [approvals](../suites/approvals.md) | `APV-F4`, `APV-F10`–`APV-F12` | ⛔ manual-only | — |
| [approvals](../suites/approvals.md) | `APV-B2`–`APV-B3`, `APV-A1`–`APV-A3`, `APV-P1` | ⛔ manual-only | — |
| [auth](../suites/auth.md) | `AUTH-F2`, `AUTH-F3` | ✅ automated | `auth.spec.ts` |
| [auth](../suites/auth.md) | `AUTH-F4`, `AUTH-F7`, `AUTH-F8`, `AUTH-F9` | ✅ automated | `auth-account.spec.ts` |
| [auth](../suites/auth.md) | `AUTH-F6` | ✅ automated | `onboarding.spec.ts` |
| [auth](../suites/auth.md) | `AUTH-F18` (identity protocol, consent/accessibility, signed continuation) | 🔶 backend + component | `backend/auth/oidc-integration.ts` (real HTTP/Postgres in `backend:integration`), `oauth-authorization.test.tsx`, `resume-oauth.test.ts`, `sentry-normalize.test.ts` (callback-secret redaction); live IdP/passkey/MFA round remains manual |
| [auth](../suites/auth.md) | `AUTH-F14` | ✅ automated | `rbac.spec.ts` |
| [auth](../suites/auth.md) | `AUTH-B5` (char-error path) | ✅ automated | `validation.spec.ts`, `onboarding.spec.ts` |
| [auth](../suites/auth.md) | `AUTH-F15`, `AUTH-F16`, `AUTH-B7` | ✅ automated | `auth.spec.ts` (SSO error / conditional-access / verbatim-fallback via `?error=` params) + component `log-in-sso-error.test.tsx`; backend redirect `authorize_handler.test.ts` |
| [auth](../suites/auth.md) | `AUTH-F1`, `AUTH-F13` | 🔶 partial | `auth.spec.ts` e2e + the `login-idle-notice.test.tsx` unit test |
| [auth](../suites/auth.md) | `AUTH-F5`, `AUTH-F10`–`AUTH-F12`, `AUTH-F17`, `AUTH-B3`, `AUTH-B4` | ⛔ manual-only | — (fresh DB / WebAuthn / IdP / mid-session policy / TOTP / needs 2 orgs) |
| [automations](../suites/automations.md) | `AUTO-F1` (toolbar create), `AUTO-F1b`, `AUTO-F4` (org-level row), `AUTO-F8` (yml lane, Later), `AUTO-F12` (leaf switcher), `AUTO-F35` (tab strip walk) | 🔶 partial | `automations.spec.ts` (upload → detail tabs + switcher → delete) |
| [automations](../suites/automations.md) | The leaf switcher keeps all automations after project navigation, lists organization automations above a separator and project-specific ones below, searches across both groups, routes each selection to its bound project or the organization, and resets the editor's version and draft when the automation changes | ✅ automated | `app/features/automations/components/automation-breadcrumb-switcher.browser.test.tsx` (real Chromium navigation and grouping), `automation-breadcrumb-switcher.test.tsx` (project binding destinations), `automation-editor.test.tsx` (editor identity reset), `app/components/ui/forms/searchable-select.test.tsx` (group filtering and keyboard navigation) |
| [automations](../suites/automations.md) | `AUTO-F12` workbench layout: at phone width the canvas keeps its full height above the inspector, its zoom in / zoom out / reset view cluster is never clipped, and the page scrolls; at desktop width the canvas and inspector fill the tab without scrolling the page | ✅ automated | `app/features/automations/components/automation-editor.browser.test.tsx` (real Chromium layout at 375×667 and 1280×800 with a node open in the inspector) |
| [automations](../suites/automations.md) | Everything else in `AUTO-F1`–`AUTO-F34`, `AUTO-B1`–`AUTO-B6` | ⛔ manual-only | — |
| [automations](../suites/automations.md) | `AUTO-F5`, `AUTO-F13`–`AUTO-F16`, `AUTO-F19`, `AUTO-F35`, `AUTO-F36` | 🔶 partial | unit: `automations-list.test.tsx`, `automation-detail-shell.test.tsx`, `automation-editor.test.tsx`, `automation-canvas.test.tsx`, `node-inspector.test.tsx`, `version-list.test.tsx`, `detail-paths.test.ts` |
| [automations](../suites/automations.md) | `AUTO-F18`, `AUTO-F20` (manual run input) | 🔶 partial | `automation-editor.test.tsx`: required JSON input, malformed/type-invalid refusal, displayed mock version, deployed live schema/version and explicit confirmation; real connector effects stay manual |
| [automations](../suites/automations.md) | `AUTO-F8`–`AUTO-F11` | 🔶 partial | unit: `upload-automation-dialog.test.tsx` (lanes, zip cap, skill-conflict panel — no end-to-end run) |
| [automations](../suites/automations.md) | `AUTO-F22`–`AUTO-F23`, `AUTO-F26` | 🔶 partial | unit: `effect-list.test.tsx`, `agent-execution-log.test.tsx`, `run-ask-card.test.tsx` |
| [automations](../suites/automations.md) | `AUTO-F27`–`AUTO-F29` | 🔶 partial | unit: `trigger-editor.test.tsx` |
| [automations](../suites/automations.md) | `AUTO-F30` | 🔶 partial | unit: `project-bindings-section.test.tsx` |
| [automations](../suites/automations.md) | `AUTO-F32` | 🔶 partial | unit: `automation-settings-dialog.test.tsx`, `run-step-timeline.test.tsx` |
| [chat](../suites/chat.md) | `CHAT-F30` | ⛔ manual-only | — (`search.spec.ts` has the chat-palette case, but it is fixme-skipped — seeding needs a provider key) |
| [chat](../suites/chat.md) | `CHAT-F36` (project-thread scope) — the server pins the thread's project as the tools' boundary (a project chat's `rag_search`/`rag_fetch` reach the project's files plus the hub and its work legs the project's board; the organization chat reaches the hub only, every readable board, never a project's files), the document list unions project and hub rows with `scope`/`project`/`indexing`, another project's `projectId` is refused, a lost project degrades to the hub with a label, the prompt states the boundary with the project key, `rag_fetch` reads a never-indexed text file on demand (≤ 4 MiB) or names the file and its true indexing state, the timeline never shows a raw blob ref, the project's Knowledge tab reads **Not indexed** and **Index now** opts a REST-bound file back in | 🔶 backend + component | `backend/core/chat/assistant_tools.test.ts`, `backend/core/knowledge/document_text.test.ts`, `backend/domains/knowledge/service.on-demand.test.ts`, `backend/domains/documents/agent-list.test.ts`, `backend/domains/documents/service.retry-indexing.test.ts`, `backend/domains/chat/shim.knowledge-scope.test.ts`, `backend/core/chat/turn_action.test.ts`, `backend/core/chat/project_context.test.ts`, `lib/chat/context.test.ts`, `backend/core/node_only/sandbox/workspace_tools_bridge.test.ts`, `app/features/chat/utils/activity-label.test.ts`, `app/features/projects/components/project-files-tab.test.tsx`, the `project chat document scope (union listing, indexing, on-demand read)` lane in `backend:integration`; the live model's reading of the boundary sentence stays manual |
| [chat](../suites/chat.md) | `CHAT-F3`, `CHAT-F15`, `CHAT-A1`, `CHAT-AT3` | 🔶 component | — (no e2e; `composer.test.tsx` — Enter/Shift+Enter, quote chip prop, pasted-image attach) |
| [chat](../suites/chat.md) | `CHAT-F11`–`CHAT-F12`, `CHAT-F14` | 🔶 component | — (no e2e; `message-toolbar.test.tsx`) |
| [chat](../suites/chat.md) | `CHAT-F13` | 🔶 component | — (no e2e; `message-info-dialog.test.tsx`) |
| [chat](../suites/chat.md) | `CHAT-F16` | 🔶 component | — (no e2e; `thought-timeline.test.tsx`) |
| [chat](../suites/chat.md) | `CHAT-F24` | 🔶 component | — (no e2e; `arena-verdict-bar.test.tsx` — verdict bar only) |
| [chat](../suites/chat.md) | `CHAT-F26` | 🔶 component | — (no e2e; `dictation-button.test.tsx`, `hooks/use-media-recorder-dictation.test.ts`) |
| [chat](../suites/chat.md) | `CHAT-F27`–`CHAT-F29` | 🔶 component | — (no e2e; `thread-list.test.tsx`, `thread-row.test.tsx`) |
| [chat](../suites/chat.md) | `CHAT-F32` | 🔶 component | — (no e2e; `source-cards.test.tsx`) |
| [chat](../suites/chat.md) | `CHAT-F33` | 🔶 component | — (no e2e; `citation-link.test.tsx` in `app/features/shared/markdown/`) |
| [chat](../suites/chat.md) | `CHAT-F34` | 🔶 component | — (no e2e; `step-limit-notice.test.tsx`) |
| [chat](../suites/chat.md) | `CHAT-AT7` | 🔶 component | — (no e2e; `hooks/use-file-transcription-status.test.ts`) |
| [chat](../suites/chat.md) | `CHAT-F7` (reasoning effort) — the wire behind the picker: the pick reaches an OpenAI-compatible body as `reasoning_effort` (five steps folded onto three) and an Anthropic body as `output_config.effort` (all five, `extra` as `xhigh`); a thinking budget is spelled on the Anthropic wire only; a knob the connector's wire cannot spell warns instead of vanishing; the platform's default temperature stays off the Anthropic wire for a reasoning model; every shipped catalog declares a knob its connector's wire carries | 🔶 backend | `backend/core/automations_builder/chat_wire.test.ts`, `lib/chat/effort.test.ts`, `lib/shared/providers/catalog_normalize.test.ts`, `backend/core/lib/providers/load_system_config.test.ts` (shipped knob ↔ wire guards); the live endpoint's acceptance of the parameter stays manual |
| [chat](../suites/chat.md) | `CHAT-B6` | 🔶 component | — (no e2e; `budget-banner.test.tsx`) |
| [chat](../suites/chat.md) | `CHAT-F1`–`CHAT-F2`, `CHAT-F4`–`CHAT-F10`, `CHAT-F17`–`CHAT-F23`, `CHAT-F25`, `CHAT-F31`, `CHAT-F35`, `CHAT-AT1`–`CHAT-AT2`, `CHAT-AT4`–`CHAT-AT6`, `CHAT-AT8` | ⛔ manual-only | — |
| [chat](../suites/chat.md) | `CHAT-B1`–`CHAT-B5`, `CHAT-B7`–`CHAT-B8`, `CHAT-A2`–`CHAT-A5`, `CHAT-P1`–`CHAT-P4` | ⛔ manual-only | — |
| [connectors](../suites/connectors.md) | `CONN-F1`, `CONN-F3`–`CONN-F10`, `CONN-F5a`, `CONN-F13`, `CONN-F14`, `CONN-B1`, `CONN-B4`, `CONN-B5` | 🔶 component | `app/features/settings/connectors/components/connectors-settings.test.tsx` (rows, add flow and its suggested name, row actions, denial) |
| [connectors](../suites/connectors.md) | `CONN-F1`–`CONN-F3` (empty state, picker, search) | ✅ automated | `settings.spec.ts` ("connectors: empty credentials surface and the add-catalog picker") |
| [connectors](../suites/connectors.md) | `CONN-F7`, `CONN-B1` (required-config gating) | 🔶 component | `app/features/settings/connectors/config-fields.test.tsx` |
| [connectors](../suites/connectors.md) | `CONN-F17` | 🔶 component | `app/features/settings/connectors/components/mcp-endpoint-section.test.tsx` |
| [connectors](../suites/connectors.md) | `CONN-F11`, `CONN-F12`, `CONN-F18`, `CONN-B2`, `CONN-B3`, `CONN-B6` | ⛔ manual-only | — (persistence reloads, live consent round trip, redirects, server refusals) |
| [connectors](../suites/connectors.md) | `CONN-F15`, `CONN-F16` (full round trip) | ⛔ manual-only | — (need a real vendor consent, mode B) |
| [conversations](../suites/conversations.md) | `CONV-G1`–`CONV-G3`, `CONV-F1`–`CONV-F10`, `CONV-B1`–`CONV-B4` | ⛔ manual-only | — (the `email-automation` spec, which automated the gate, redirect, channel filter, and reading pane, was retired in #2857 and has no successor) |
| [conversations](../suites/conversations.md) | `CONV-F11` | 🔶 partial | `backend/domains/conversations/api-sync.integration.ts` owns HTTP/Postgres source imports, attachments, replies, undo, replay, isolation and deletion; `use-inbox-availability.test.ts` owns the API-only gate. Visual two-app flow and keyboard behavior remain manual. |
| [conversations](../suites/conversations.md) | The mirror after the 2026-09-15 round-i evaluation: a torn-down mirror stays down — a content snapshot onto it answers 409 `CONVERSATION_CLOSED` at any version, the thread and its messages never come back — and the receipt carries `sourceDeleted` and `status` | ✅ automated | `backend/rest/v1-conversations.test.ts`, `scripts/openapi/spec.test.ts`, the tombstone lane in `api-sync.integration.ts` |
| [data-residency](../suites/data-residency.md) | Case | Status | Where |
| [data-residency](../suites/data-residency.md) | Panel render / save / probe / clear / access-denied / axe | ✅ automated | `app/features/settings/data-residency/**` (test:ui, 21 tests) |
| [data-residency](../suites/data-residency.md) | Knowledge/embedding admin actions (gating, delegation, credential checks) | ✅ automated | `convex/knowledge/actions.test.ts` |
| [data-residency](../suites/data-residency.md) | Knowledge/embedding file writers (password tri-state, SSRF gate, history, probe fallback) | ✅ automated | `convex/knowledge/file_actions.test.ts` |
| [data-residency](../suites/data-residency.md) | S3 verbs + presign + org-namespace guard (`DATA-B4`) | ✅ automated | `convex/lib/storage/object_store.integration.test.ts`, `blob_ref.test.ts` (MinIO-gated) |
| [data-residency](../suites/data-residency.md) | Document/audio/server-gen blobs land in the bucket | ✅ automated | `convex/lib/storage/object_storage_{documents,producers}.e2e.test.ts` (MinIO-gated) |
| [data-residency](../suites/data-residency.md) | Backfill: happy / idempotent / crash-safe / shared refs / multi-org / dry-run (`DATA-F5`) | ✅ automated | `convex/object_storage/backfill.e2e.test.ts` (13 cases, MinIO-gated) |
| [data-residency](../suites/data-residency.md) | External-DB retrieval incl. chat (`DATA-F6` case (a), the retrieval leg) | ✅ automated | `tests/external-db-retrieval/external-db-retrieval.test.ts` (gated) |
| [data-residency](../suites/data-residency.md) | Documents list renders an `s3:` ref (no `getUrl` crash) — the panel's own uploads | ✅ automated | `convex/documents/transform_batch_s3.test.ts` |
| [data-residency](../suites/data-residency.md) | Embed+store is batched, not all-at-once (`DATA-B5`, embed phase only) | ✅ automated | `convex/rag/lib/indexing_service.store.test.ts` (feeds a pre-built chunk array; does NOT cover the prepare/chunk phase that still scales with size — see `#2752`) |
| [data-residency](../suites/data-residency.md) | `DATA-F4`/`DATA-F6` full-app placement matrix through the real UI | ⛔ manual-only | this plan |
| [data-residency](../suites/data-residency.md) | `DATA-F7` JSON parity on the volume | ⛔ manual-only | this plan |
| [data-residency](../suites/data-residency.md) | `DATA-B5` large-file end-to-end through the real UI | ⛔ manual-only | this plan |
| [governance](../suites/governance.md) | `GOV-F1` | ✅ automated | `navigation.spec.ts` (governance redirect + settings-rail → governance nav) |
| [governance](../suites/governance.md) | `GOV-F2` | ✅ automated | `governance.spec.ts` (voice-output toggle persist/restore; system-prompt edit persist/restore) |
| [governance](../suites/governance.md) | `GOV-F3` (toggle) | 🔶 partial | `governance.spec.ts` (content-safety **enable** toggle persists/restores — filtering manual) |
| [governance](../suites/governance.md) | `GOV-B6` | ✅ automated | `governance.spec.ts` (apiKey scope with no target → `governance.budgets.targetRequired`, no row added) |
| [governance](../suites/governance.md) | `GOV-F4b` | 🔶 partial | `budget-editor.test.tsx` (apiKey scope add + target validation) + `budget_enforcement_apikey.test.ts` (backend enforcement) — no e2e happy-path (needs a seeded key) |
| [governance](../suites/governance.md) | `GOV-F4`, `GOV-F6`–`GOV-F12` | ⛔ manual-only | — |
| [governance](../suites/governance.md) | `GOV-F13`–`GOV-F17` | ⛔ manual-only | — |
| [governance](../suites/governance.md) | `GOV-B1`–`GOV-B5`, `GOV-B7` | ⛔ manual-only | — (B6 is the only automated boundary case) |
| [knowledge](../suites/knowledge.md) | `KNOW-F1` | 🔶 partial | `navigation.spec.ts` (documents route renders to its empty state; the upload flow itself is manual — the `knowledge` spec was retired in #2857) |
| [knowledge](../suites/knowledge.md) | `KNOW-F3`–`KNOW-F5`, `KNOW-F8` | ⛔ manual-only | — (the `knowledge` spec, which automated document/ODT upload and contact CRUD, was retired in #2857) |
| [knowledge](../suites/knowledge.md) | `KNOW-F2`, `KNOW-F6`, `KNOW-F7` | ⛔ manual-only | — (OneDrive sync / website crawl / document preview) |
| [knowledge](../suites/knowledge.md) | `KNOW-F9`–`KNOW-F11` | ⛔ manual-only | — (RAG backend / crawler timing; F11 needs an org without an embedding model) |
| [knowledge](../suites/knowledge.md) | `KNOW-F12` | ⛔ manual-only | — (controlled-document replacement has mutation and component coverage but no browser spec) |
| [knowledge](../suites/knowledge.md) | `KNOW-F13` | 🔶 partial | the `websites page failure` lane in `backend:integration` (the stored reason and the API fields); the dialog caption and the reload are manual |
| [knowledge](../suites/knowledge.md) | Knowledge-entry indexing status, failure details, tenant isolation and pagination; document indexing hints refresh the knowledge-entry query | 🔶 backend + component | `backend/domains/knowledge_entries/indexing.integration.ts` in `backend:integration`, `app/lib/backend/use-backend-hints.test.tsx`; observing a newly created entry reach Indexed without a reload remains manual |
| [knowledge](../suites/knowledge.md) | Crawling after the 2026-09-15 round-i evaluation: every crawler request identifies itself as `TaleBot/<version> (+https://docs.tale.dev/platform/knowledge/crawling)` on both fetch legs (the in-process probe and the sandboxed render worker), and the backend's error reporting stamps no `sentry-trace` / `baggage` onto outgoing requests | ✅ automated | `backend/core/knowledge/crawler_identity.test.ts`, `backend/core/node_only/sandbox/render_fetch.test.ts`, `backend/error-reporting.test.ts` |
| [metrics](../suites/metrics.md) | `MET-F2` (usage renders) | 🔶 partial | `metrics.spec.ts` (header, filter button, Top Assistants, period section — empty data) |
| [metrics](../suites/metrics.md) | `MET-F4` (feedback empty) | 🔶 partial | `metrics.spec.ts` (header + empty teaching panel only) |
| [metrics](../suites/metrics.md) | `MET-F8` (automations renders) | 🔶 partial | `metrics.spec.ts` (KPI labels, trend chart, table headers — empty data) |
| [metrics](../suites/metrics.md) | `MET-F9` (projects picker) | 🔶 partial | `metrics.spec.ts` (header + picker + no-project empty state) |
| [metrics](../suites/metrics.md) | `MET-F6`, `MET-F7` | 🔶 component | — (no e2e; `chat-health-metrics-page.test.tsx`, `external-turns-metrics-page.test.tsx`) |
| [metrics](../suites/metrics.md) | `MET-B2` | 🔶 component | — (no e2e; `legacy-redirects.test.tsx` route unit tests) |
| [metrics](../suites/metrics.md) | `MET-F1`, `MET-F3`, `MET-F5`, `MET-F10`–`MET-F11` | ⛔ manual-only | — |
| [metrics](../suites/metrics.md) | `MET-B1`, `MET-A1`–`MET-A2`, `MET-P1`–`MET-P2` | ⛔ manual-only | — |
| [navigation](../suites/navigation.md) | `NAV-F1`, `NAV-F2`, `NAV-F3`, `NAV-F7` | ✅ automated | `navigation.spec.ts` |
| [navigation](../suites/navigation.md) | Quick route transitions never flash the shared progress bar; slow transitions show labelled progress and cancel the reveal on completion or unmount | ✅ automated | `app/components/layout/route-progress-bar.test.tsx` |
| [navigation](../suites/navigation.md) | Loading form controls keep their loaded dimensions, position, labels and DOM identity in narrow and wide layouts; JSON chunk loading reserves the editor height | ✅ automated | `app/components/ui/forms/form-skeletons.browser.test.tsx`, `app/components/ui/forms/json-input.browser.test.tsx`; shared primitive light/dark and reduced-motion geometry in `packages/ui/src/components/feedback/skeleton.stories.tsx` |
| [navigation](../suites/navigation.md) | Table skeleton rows use the visible columns, real control shapes and matching text line heights; checkbox, switch and action positions remain stable in both themes | ✅ automated | `app/components/ui/data-table/data-table.browser.test.tsx`, `app/components/ui/data-table/data-table.test.tsx` |
| [projects](../suites/projects.md) | Loading secret and environment rows reserve the real editor geometry and remain inert until data arrives | ✅ automated | `app/components/env/env-var-list-editor.browser.test.tsx` |
| [navigation](../suites/navigation.md) | Catalog cards, chat composer, tasks, project tabs and document previews keep the loaded frame and control geometry through loading | ✅ automated | `app/components/layout/page-skeletons.browser.test.tsx` |
| [governance](../suites/governance.md) | Policy editor loading rows match live action sizes and line heights; numeric masks keep their width and right alignment | ✅ automated | `app/features/settings/governance/components/governance-skeletons.browser.test.tsx` |
| [settings](../suites/settings.md) | Color and image controls keep their natural dimensions and mounted inputs while static labels remain visible | ✅ automated | `app/features/settings/branding/components/branding-skeletons.browser.test.tsx` |
| [navigation](../suites/navigation.md) | `NAV-F4` | ✅ automated | `search.spec.ts` |
| [navigation](../suites/navigation.md) | `NAV-F9` | ✅ automated | `page-loads.spec.ts` (render-only anchors) |
| [navigation](../suites/navigation.md) | `NAV-F5`, `NAV-F6`, `NAV-F8` | ⛔ manual-only | — |
| [navigation](../suites/navigation.md) | `NAV-F11` | ✅ automated | `navigation.spec.ts` (user-menu Documentation link → `https://tale.dev/docs`) + component `user-button.test.tsx` |
| [navigation](../suites/navigation.md) | `NAV-F10` | ⛔ manual-only | — (no DataTable bulk-action spec exists) |
| [navigation](../suites/navigation.md) | `NAV-F12`–`NAV-F14` | ⛔ manual-only | — (env-gated: version bump / iOS Safari UA / production build with a waiting SW) |
| [navigation](../suites/navigation.md) | `NAV-B1` | ✅ automated | `navigation.spec.ts` (not-found shell) |
| [navigation](../suites/navigation.md) | `NAV-B2` | ✅ automated | `navigation.spec.ts` (styled 404 for an unknown route inside the shell, incl. document title) |
| [navigation](../suites/navigation.md) | `NAV-B10` | 🔶 partial | `navigation.spec.ts` (standalone 404 for an unknown route outside the dashboard, incl. document title and the way back to the organization) + component `route-not-found.test.tsx` (also a miss beneath the sign-in layout: one `main`, no nested frame); the signed-out log-in hand-off and both themes stay manual |
| [navigation](../suites/navigation.md) | `NAV-B3`, `NAV-B4` | ⛔ manual-only | — |
| [navigation](../suites/navigation.md) | `NAV-B5` | ⛔ manual-only | — (env-gated: needs browser network emulation / stopping the local backend) |
| [notifications](../suites/notifications.md) | `NOTIF-F1`–`NOTIF-F8` | ⛔ manual-only | — |
| [notifications](../suites/notifications.md) | `NOTIF-F9`–`NOTIF-F11` | ⛔ manual-only | — (need two accounts in one org; mode A works) |
| [notifications](../suites/notifications.md) | `NOTIF-F12` | ⛔ manual-only | — (env-gated: cron/agent-driven, documentation row) |
| [notifications](../suites/notifications.md) | `NOTIF-F13` | 🔶 backend | — (no e2e; `convex/automations/human_asks.test.ts`: fan-out scope, prefs, fold rewrite, dismissal) |
| [notifications](../suites/notifications.md) | `NOTIF-F15` | 🔶 backend | `backend/rest/v1-notifications.test.ts` (recipient/admin isolation, native targets, signed cursor scope, translations); `tests/e2e/specs/projects.spec.ts` opens a task notification while signed out and verifies the same task sheet after login; `auth.spec.ts` preserves document/conversation destinations; deployment and connected-app round remain manual |
| [notifications](../suites/notifications.md) | `NOTIF-B1`–`NOTIF-B3` | ⛔ manual-only | — |
| [notifications](../suites/notifications.md) | `NOTIF-A1`–`NOTIF-A3` | ⛔ manual-only | — |
| [performance](../suites/performance.md) | `PERF-P5` (pagination) | 🔶 partial | `projects-depth.spec.ts` (functional, NOT timed) |
| [performance](../suites/performance.md) | `PERF-P1`–`PERF-P4`, `PERF-P6`, `PERF-P7` | ⛔ manual-only | — (no load-timing assertions in e2e; the chat specs that proved P2/P3 functionally retired in #2857) |
| [performance](../suites/performance.md) | `PERF-B4` (provider error) | ⛔ manual-only | — (the `chat-scenarios` spec retired in #2857) |
| [performance](../suites/performance.md) | `PERF-B1`–`PERF-B3`, `PERF-A1`, `PERF-A2` | ⛔ manual-only | — (load characteristics / DOM attributes, not asserted) |
| [projects](../suites/projects.md) | `PROJ-F1`, `PROJ-F9`, `PROJ-F12`, `PROJ-F16`, `PROJ-F19` | ✅ automated | `projects.spec.ts` (create→task in both views→backlog column→delete) |
| [projects](../suites/projects.md) | `PROJ-F2`, `PROJ-F6` | ✅ automated | `projects-depth.spec.ts` (rename + instructions persist) |
| [projects](../suites/projects.md) | `PROJ-F7` | ✅ automated | `projects-depth.spec.ts` (secret create then delete) |
| [projects](../suites/projects.md) | `PROJ-F10`, `PROJ-F12` | ✅ automated | `projects-depth.spec.ts` (status/priority/label persist) |
| [projects](../suites/projects.md) | `PROJ-B2` | ✅ automated | `validation.spec.ts` (delete-confirm phrase gating) |
| [projects](../suites/projects.md) | `PROJ-B1` | 🔶 partial | `project-create-dialog.test.tsx` (unit, not e2e) |
| [projects](../suites/projects.md) | `PROJ-B4` | 🔶 partial | `task-create` unit/component coverage (not e2e) |
| [projects](../suites/projects.md) | `PROJ-F3`, `PROJ-F4`, `PROJ-F5`, `PROJ-F8`, `PROJ-F11`, `PROJ-F13`, `PROJ-F14`, `PROJ-F15` | ⛔ manual-only | — |
| [projects](../suites/projects.md) | `PROJ-F17`, `PROJ-F18`, `PROJ-F20`, `PROJ-F21` | ⛔ manual-only | — |
| [projects](../suites/projects.md) | `PROJ-B3` | ⛔ manual-only | — |
| [projects](../suites/projects.md) | REST chat after the 2026-09-15 round-i evaluation: `MessagePart` is a `type` discriminator with an explicit mapping over seven named part schemas (`TextPart` … `HumanInputPart`), and the contract declares nullability with `nullable` only — no OpenAPI 3.1 type list anywhere in the document | ✅ automated | `scripts/openapi/spec.test.ts` |
| [responsive](../suites/responsive.md) | `RESP-F1`, `RESP-F2` | ✅ automated | `responsive.spec.ts` (app-shell: rail hidden, tab bar + More sheet) |
| [responsive](../suites/responsive.md) | `RESP-F3` | ✅ automated | `responsive.spec.ts` (floating Save dock: 1 visible Save, dirty→enabled, reload-discard) |
| [responsive](../suites/responsive.md) | `RESP-F3` dock frame: one inset between the floating dock's border and its actions on every side, a dock whose actions wrap hugs its widest row, and the dock re-fits when its actions or the viewport change | ✅ automated | `packages/ui/src/components/layout/mobile-floating-actions.browser.test.tsx` (real Chromium at 390×844 and 430×932, one row and wrapped rows) |
| [responsive](../suites/responsive.md) | `RESP-F4` | 🔶 partial | `responsive.spec.ts` (chat input renders + enabled at mobile; **no send / no attach**) |
| [responsive](../suites/responsive.md) | `RESP-F5` | 🔶 partial | `responsive.spec.ts` (contacts list usable at mobile; **no row/stack assertions**) |
| [responsive](../suites/responsive.md) | `RESP-F6`, `RESP-F7` | ⛔ manual-only | — |
| [responsive](../suites/responsive.md) | `RESP-F9` | ⛔ manual-only | — (needs canvas content, mode B) |
| [responsive](../suites/responsive.md) | `RESP-B1`–`RESP-B3`, `RESP-A1`–`RESP-A3`, `RESP-P1` | ⛔ manual-only | — |
| [settings](../suites/settings.md) | `SET-F3` (account name) | ✅ automated | `settings.spec.ts` |
| [settings](../suites/settings.md) | `SET-F12` (org rename) | ✅ automated | `settings-depth.spec.ts` |
| [settings](../suites/settings.md) | `SET-F18` (teams create/delete) | ✅ automated | `settings-depth.spec.ts` |
| [settings](../suites/settings.md) | `SET-F32` (API key lifecycle) | ✅ automated | `settings-depth.spec.ts` |
| [settings](../suites/settings.md) | `SET-F11` (theme & language) | ✅ automated | `preferences.spec.ts` (Manage-account menu) |
| [settings](../suites/settings.md) | `SET-F6` (preference toggles) | 🔶 partial | `settings-depth.spec.ts` (custom-instructions toggle only; memories + org-default hint manual) |
| [settings](../suites/settings.md) | `SET-F28` (branding) | 🔶 partial | `settings-depth.spec.ts` (accent color only; logo/favicon/reset manual) |
| [settings](../suites/settings.md) | `SET-F20`–`SET-F21` (providers page + add) | 🔶 partial | `settings.spec.ts` (empty state + add-catalog picker + the suggested name on a fresh org; no credential is actually created) |
| [settings](../suites/settings.md) | `SET-F21a` (suggested name numbered past a held one; an untouched suggestion closes without a prompt) | 🔶 component | `app/features/settings/providers/components/providers-settings.test.tsx`, `lib/shared/utils/credential-name.test.ts` (a mocked credential list — persisted rows and the rename stay manual) |
| [settings](../suites/settings.md) | `SET-F22a` (provider changes apply without a reload) — a credential write, a catalog refresh, a `model_access` or `vision_model` save and another session's `provider_credential` hint each refresh every provider-derived read: the agent model roster, the runtime status, the resolved vision model, the embedding recommendations and the unpinned serving preview; the catalog listing is one cache entry for the providers page and the automation builder; an unrelated policy save refreshes none of them | 🔶 component | `app/features/settings/providers/hooks/provider-reads.test.tsx` (real hooks, adapter rows and query client; `fetch` stubbed) — the repaint of each page and of a second tab stays manual |
| [settings](../suites/settings.md) | `SET-F24` (connectors page) | 🔶 partial | `settings.spec.ts` (empty state + add-catalog picker) |
| [settings](../suites/settings.md) | `SET-F14`–`SET-F15` (members) | 🔶 partial | `rbac.spec.ts` (a member cannot see the add-member control; the add flows themselves are manual) |
| [settings](../suites/settings.md) | `SET-F1` (rail) | 🔶 component | — (no e2e; `settings-rail.test.tsx`) |
| [settings](../suites/settings.md) | `SET-F9` (notification prefs) | 🔶 component | — (no e2e; `notification-preferences-settings.test.tsx`) |
| [settings](../suites/settings.md) | `SET-F16`–`SET-F17` (member dialogs) | 🔶 component | — (no e2e; `member-add-dialog.test.tsx`, `member-row-actions.test.tsx`, `member-table.test.tsx`) |
| [settings](../suites/settings.md) | `SET-F33` (MCP endpoint) | 🔶 component | — (no e2e; `mcp-endpoint-section.test.tsx`) |
| [settings](../suites/settings.md) | `SET-F35` (Enterprise SSO) | 🔶 component | — (no e2e; `enterprise-sso-form.test.tsx`) |
| [settings](../suites/settings.md) | `SET-F36` (data residency) | 🔶 component | — (no e2e; `data-residency-settings.test.tsx`) |
| [settings](../suites/settings.md) | `SET-F2`, `SET-F4`–`SET-F5`, `SET-F7`–`SET-F8`, `SET-F13`, `SET-F19`, `SET-F22`–`SET-F23`, `SET-F25`–`SET-F27`, `SET-F29`–`SET-F31`, `SET-F34` | ⛔ manual-only | — |
| [settings](../suites/settings.md) | `SET-B1`–`SET-B10`, `SET-A1`–`SET-A5`, `SET-P1`–`SET-P3` | ⛔ manual-only | — |
| [skills](../suites/skills.md) | `SKILL-B1`, `SKILL-B3` (server side) | 🔶 partial | — (no e2e; `convex/skills/bundle_zip.test.ts` covers the backend) |
| [skills](../suites/skills.md) | `SKILL-F1`–`SKILL-F12`, `SKILL-B2`, `SKILL-A1`–`SKILL-A2`, `SKILL-P1` | ⛔ manual-only | — |
| [skills](../suites/skills.md) | REST skills door after the 2026-09-12 round-d evaluation: `PUT /api/v1/skills/{slug}` answers 201 for the bundle it created and 200 for the one it updated (the file layer reports `created`), and the `GET`'s 304 is the validated-read middleware's verdict — `private, no-cache` as on the 200, the weak and edge-suffixed (`"…-gzip"`) tags matched — never a `no-store` of the route's own; and, after the 2026-09-13 round-e evaluation (E6-03), a byte-identical `PUT` is a no-op — 200 with the stored `etag` and `updatedAt`, `SKILL.md` and its history untouched, a stale `If-Match` on it still 412 | ✅ automated | `backend/rest/v1-core.contract.test.ts`, `backend/core/skills/file_actions.test.ts`, `backend/integration-check.ts` (the `skills REST door` record) |
| [tasks](../suites/tasks.md) | `TASK-F2`, `TASK-F6` (create, both views) | ✅ automated | `projects.spec.ts` (create task, board↔list visibility, create into Backlog, promote via status picker) |
| [tasks](../suites/tasks.md) | `TASK-F7` (status/priority/label) | ✅ automated | `projects-depth.spec.ts` (live-edit persists across board and list) |
| [tasks](../suites/tasks.md) | `TASK-F11` (assign) | 🔶 partial | `return-loops.spec.ts` (assigning notifies + calls back the assignee; picker sections themselves manual) |
| [tasks](../suites/tasks.md) | `TASK-F3`, `TASK-F5` | 🔶 component | — (no e2e; `kanban-board.test.tsx`, `tasks-list.test.tsx`) |
| [tasks](../suites/tasks.md) | `TASK-F8` | 🔶 component | — (no e2e; `task-comments.test.tsx`, `mention-text.test.tsx`) |
| [tasks](../suites/tasks.md) | `TASK-F9` | 🔶 component | — (no e2e; `task-attachments.test.tsx`, `task-input-files.test.tsx`) |
| [tasks](../suites/tasks.md) | `TASK-F12` | 🔶 component | — (no e2e; `task-agent-run-entry.test.tsx`, `task-run-failure-banner.test.tsx`, `task-subject-panel.test.tsx`) |
| [tasks](../suites/tasks.md) | `TASK-F14` | 🔶 component | — (no e2e; `task-outcome-files.test.tsx`; backend `convex/tasks/task_agent_outputs.test.ts`) |
| [tasks](../suites/tasks.md) | `TASK-F16` | 🔶 component | — (no e2e; `task-timeline.test.tsx`, `task-archive-dialog.test.tsx`) |
| [tasks](../suites/tasks.md) | `TASK-B1` | 🔶 unit | — (no e2e; `app/features/tasks/lib/dependencies.test.ts` + `convex/tasks/dependencies.test.ts`; the toast manual) |
| [tasks](../suites/tasks.md) | `TASK-F1`, `TASK-F4`, `TASK-F10`, `TASK-F13`, `TASK-F15` | ⛔ manual-only | — (no e2e drives DnD, the run/transcript dialogs, or the review decision) |
| [tasks](../suites/tasks.md) | `TASK-B2`–`TASK-B4`, `TASK-A1`–`TASK-A3`, `TASK-P1`–`TASK-P2` | ⛔ manual-only | — |
| [video-links](../suites/video-links.md) | `VID-F1`, `VID-F4` | ⛔ manual-only | — |
| [video-links](../suites/video-links.md) | `VID-F2`, `VID-F3` | 🔶 partial | components only: `convex/video_links/captions_parser.test.ts`, `url_safety.test.ts`, `synthetic_file_metadata.test.ts`, `donor_reuse.test.ts`; no e2e job run |
| [video-links](../suites/video-links.md) | `VID-F5` | 🔶 partial | `lib/shared/video-url.test.ts` (playlist detection); the ConvexError surface is manual |
| [video-links](../suites/video-links.md) | `VID-F6`, `VID-F7` | 🔶 partial | `backend/core/video_links/ytdlp_live.test.ts` — gated `YOUTUBE_LIVE_TEST=1` (the CI Unit job in `.github/workflows/checks.yml` runs it from a datacenter IP) |
| [video-links](../suites/video-links.md) | `VID-B1`–`VID-B3` | 🔶 partial | `convex/video_links/ytdlp.test.ts` (stderr classifier, env-flag builders, log sanitizer); the live job-row/log behaviour is manual |

## Documentation rebase regressions

- `backend/rest/v1-core.test.ts` verifies that document `If-Match` uses a fresh
  read under the write lock, refuses a concurrent representation change with
  412, and retains access and lifecycle checks. A separate local HTTP probe
  held a real PostgreSQL row lock and confirmed the concurrent edit survives.
- `backend/core/node_only/sandbox/llm_gateway_admin.test.ts` covers private IPs
  and DNS names, all-address metadata refusal, missing DNS, operator opt-in,
  cached-key rechecks, and public/private destination transitions. Gateway
  requests are mocked; these cases do not prove production DNS or inference.
- `app/features/documents/components/rag-status-badge.test.tsx` holds terminal
  indexing explanations and the absence of a retry action. Native modal layout
  and uploaded-file behavior are exercised in the documentation round.

- `app/features/projects/components/project-files-tab.test.tsx` verifies that
  terminal unsupported files retain a visible label and recovery hint without
  a retry action. Native phone layouts remain part of manual judgment.
- `app/lib/nav-memory.test.ts` covers unavailable Storage getters as well as
  recorded locations, tab independence and expiry. A blocked storage area does
  not prevent the other area from working or interrupt clearing on sign-out.
- `app/lib/backend/engagement.test.ts` pins native website creation dates and
  the current whole-list page envelope, retaining scan timestamps and cursors.

## Seams

Managed container prefixes are covered by `tools/cli/src/lib/deployment/inputs.test.ts`,
`runtime-prepare.test.ts`, `runtime-apply.test.ts`, `prepare-config.test.ts` and
`apply.test.ts` in the same directory. They verify bounded explicit names, original
source topology, declaration/bundle agreement, snapshot-before-recreation, retained
deployment identity and native IDs, unchanged volumes and secret bytes, foreign-name
refusal, observed container names, interrupted rollout and exact replay. These tests
do not establish multiple complete runtimes on one Docker daemon.

Managed hostname migration is covered by `tools/cli/src/lib/deployment/identity.test.ts`,
`email-attestation.test.ts`, `native-client-fresh.test.ts` and `fresh-inputs.test.ts`
in the same directory. They verify exact source-origin admission, retained user and
organization bindings, unchanged client credentials, refusal before authentication,
partial completion and replay. `tools/cli/src/lib/config/platform-apply.test.ts`
also exercises the managed configuration bridge, retained organization bindings,
resource and credential preservation, reverse migration and interrupted-plan
recovery. These tests do not prove production DNS or TLS routing.

General native configuration is also covered by `tools/cli/tests/platform-configuration.test.ts`
(real HTTP with source/compiled CLI commands), `tools/cli/src/lib/config/platform-apply.test.ts`
(reviewed plans, stale preimages, default credential changes, partial failures and readback),
and `backend/domains/configuration-writes.test.ts` plus the provider/credential route suites
(actual local file/history behavior, native permissions and compare-and-set).
`packages/shared/src/schemas/` owns the common schema tests and the node-free import guard.
These suites do not prove inference hardware, actual model quality or production readiness.

- **The Playwright suite drives the same origin a round does.** Never run
  `bun run test:e2e` beside a round: it signs in, creates and deletes data, and
  leaves the stack in its end state.
- **The vitest lanes (`test`, `test:ui`, `test:browser`) boot and tear down
  their own stack**, so they are safe beside a round.

## Moving a box here

When a spec takes a box over end to end, **delete the box and add its row here
in the same commit**, naming the spec. A box that survives its automation is
manual effort spent twice; `bun run lint:manual` rejects a box ID here that no
suite defines.
