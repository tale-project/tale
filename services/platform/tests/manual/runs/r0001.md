# R1 — the findings the guides were carrying when they became suites

| | |
|---|---|
| **Round** | R1 |
| **Date** | 2026-09-08 |
| **Tree** | the commit that adopted the shared manual-test shape |
| **Stack** | — (no round was driven) |
| **Tester** | — |

## Scope

**This record was not produced by a round.** It exists because the guides
these suites came from each carried an **Issues Found** table, and a suite is
not a place findings live. Every row that still had content was moved here
verbatim on 2026-09-08, so nothing was lost when the tables went away.

Nothing below has been re-driven. Treat each entry as a claim made by whoever
filed it, on the date their description names — the first real round is what
confirms or closes it. A finding that is fixed gets its closing note in
**that** round's record, not here.

## What was exercised

Nothing. 542 boxes across 20 suites were carried over unrun; the first round's
record is where a result appears.

## Findings

2 blocker · 6 bug · 8 polish · 1 docs — carried over, not re-driven.
Severities are the shared [rubric](../readme.md#severity-rubric); the old
tables used their own scales (`crit/high/med/low`, `P1/P2/P3`), and each entry
records what it said.

| ID | Severity | Box | What |
|---|---|---|---|
| R1-1 | `blocker` | `DATA-F4` | Documents list crashed ("Invalid storage ID") for any org with a BYO-S3 doc — `transform_to_document_item.ts` called `storage.getUrl` on an `s3:` ref. Now routes `s3:` refs through … |
| R1-2 | `blocker` | `DATA-B5` | A ~96 MB text file failed to index (opaque `InternalServerError` / OOM) — the indexer embedded all chunks up front. Now embeds one batch at a time; 96 MB indexes. |
| R1-3 | `bug` | `DATA-F2` | "Test connection" after Save (password field blanked) probed with an empty password → "password authentication failed" on a valid saved config. Now reuses the stored secret. |
| R1-4 | `bug` | `DATA-F3` | Object-storage "Test connection" could not be run after Save — Test stayed disabled until both keys were re-typed, and the server probe hard-required a key pair (no stored-secret f … |
| R1-5 | `bug` | `DATA-B1` | A failed S3 probe surfaced the raw `<Error>…` XML truncated mid-tag. `object_store.ts:safeErrorBody` now parses `<Code>`/`<Message>` → a clean `SignatureDoesNotMatch: …` line. |
| R1-6 | `bug` | `DATA-B5` | A failed large-file indexing job showed a bare "Unknown error occurred" (empty `ragError`) — an interrupted action surfaces an Error with no message. `updateFileRagStatus` now reco … |
| R1-7 | `bug` | `GOV-F17` | Flipping **Enable moderation provider** ON while the endpoint is unconfigured autosaves, fails server-side, and surfaces a raw Convex validation error (unlocalized JSON) as a toast … |
| R1-8 | `bug` | `NOTIF-F11` | A plain-member account's task modal is completely read-only — no comment box, no assign/status/due-date controls — even on a task where that member is the **assignee**. Members can … |
| R1-9 | `polish` | `—` | Coverage gap (carried forward): the surface ships with zero e2e specs — the pre-rewrite `automations` + `email-automation` specs were retired in #2857 and no successor has been aut … |
| R1-10 | `polish` | `CHAT-B3` | LaTeX is not rendered in chat markdown — the shared renderer (`app/features/shared/markdown/markdown-renderer.tsx`) ships only `remark-gfm`, no math plugin, so `$$…$$` and `$…$` di … |
| R1-11 | `polish` | `DATA-B4` | Cross-org key access returned 500 instead of failing closed. Now 404. |
| R1-12 | `polish` | `DATA-F2` | Default SSL mode `require` fails the probe against a no-TLS local ParadeDB with a cryptic TLS error; set `disable` for the throwaway container (plan updated). |
| R1-13 | `polish` | `DATA-F5` | The plan claimed a "Move existing files" backfill button in the panel — none exists; the backfill is operator-CLI-only today (`startObjectStorageBlobBackfill` has no app caller). P … |
| R1-14 | `polish` | `DATA-B5` | A genuine 100 MB text file (57 k chunks) indexes to completion on BOTH internal `_storage` AND external S3+ParadeDB, but the single inline action peaks at ~1.5 GB executor RSS and |
| R1-15 | `polish` | `GOV-F16` | Three checkboxes in the PII **Detection patterns** list are all visibly labeled "Sensitive" — indistinguishable duplicate labels. |
| R1-16 | `polish` | `GOV-F14` | Opening the retention edit drawer / diff dialog logs Radix "Missing `Description` or `aria-describedby` for DialogContent" warnings. |
| R1-17 | `docs` | `CONV-F9, CONV-F10` | **RESOLVED — status transitions persist.** The audit-genesis denial (the `auditLogChainGenesis` insert being RLS-denied inside a user mutation) is off the write path: close/reopen/ … |

### R1-1 · data-residency · `DATA-F4`

**Surface** `/dashboard/{org}/documents` · **Severity** `blocker` (recorded as
`P1` in the old table)

Documents list crashed ("Invalid storage ID") for any org with a BYO-S3 doc —
`transform_to_document_item.ts` called `storage.getUrl` on an `s3:` ref. Now
routes `s3:` refs through `buildBlobServeUrl`.

### R1-2 · data-residency · `DATA-B5`

**Surface** `/dashboard/{org}/documents` · **Severity** `blocker` (recorded as
`P1` in the old table)

A ~96 MB text file failed to index (opaque `InternalServerError` / OOM) — the
indexer embedded all chunks up front. Now embeds one batch at a time; 96 MB
indexes.

### R1-3 · data-residency · `DATA-F2`

**Surface** `/dashboard/{org}/settings/data-residency` · **Severity** `bug`
(recorded as `P2` in the old table)

"Test connection" after Save (password field blanked) probed with an empty
password → "password authentication failed" on a valid saved config. Now
reuses the stored secret.

### R1-4 · data-residency · `DATA-F3`

**Surface** `/dashboard/{org}/settings/data-residency` · **Severity** `bug`
(recorded as `P2` in the old table)

Object-storage "Test connection" could not be run after Save — Test stayed
disabled until both keys were re-typed, and the server probe hard-required a
key pair (no stored-secret fallback, unlike the knowledge side). Now testable
with blank fields; the probe reuses the stored keys via `orgSlug`.

### R1-5 · data-residency · `DATA-B1`

**Surface** `/dashboard/{org}/settings/data-residency` · **Severity** `bug`
(recorded as `P2` in the old table)

A failed S3 probe surfaced the raw `<Error>…` XML truncated mid-tag.
`object_store.ts:safeErrorBody` now parses `<Code>`/`<Message>` → a clean
`SignatureDoesNotMatch: …` line.

### R1-6 · data-residency · `DATA-B5`

**Surface** `/dashboard/{org}/documents` · **Severity** `bug` (recorded as
`P2` in the old table)

A failed large-file indexing job showed a bare "Unknown error occurred" (empty
`ragError`) — an interrupted action surfaces an Error with no message.
`updateFileRagStatus` now records a plain default when the failure reason is
empty. (Root indexing interruption tracked separately.)

### R1-7 · governance · `GOV-F17`

**Surface** `…/settings/governance/guardrails` · **Severity** `bug` (recorded
as `med` in the old table)

Flipping **Enable moderation provider** ON while the endpoint is unconfigured
autosaves, fails server-side, and surfaces a raw Convex validation error
(unlocalized JSON) as a toast — and the enable state silently does not
persist.

### R1-8 · notifications · `NOTIF-F11`

**Surface** `/dashboard/{org}/projects/{projectId}/tasks/board` · **Severity**
`bug` (recorded as `high` in the old table) · **Evidence**
`defect-f11-teammate-task-modal-readonly-no-comment-composer.png`

A plain-member account's task modal is completely read-only — no comment box,
no assign/status/due-date controls — even on a task where that member is the
**assignee**. Members cannot comment on tasks at all, which blocks
collaboration (and the NOTIF-F11 scripted direction).

### R1-9 · automations · `—`

**Surface** all automation routes · **Severity** `polish` (recorded as `low`
in the old table)

Coverage gap (carried forward): the surface ships with zero e2e specs — the
pre-rewrite `automations` + `email-automation` specs were retired in #2857 and
no successor has been authored; every case here is manual.

### R1-10 · chat · `CHAT-B3`

**Surface** `/dashboard/{org}/chat/{threadId}` · **Severity** `polish`
(recorded as `low` in the old table) · **Evidence**
`defect-b2-latex-not-rendered.png`

LaTeX is not rendered in chat markdown — the shared renderer
(`app/features/shared/markdown/markdown-renderer.tsx`) ships only
`remark-gfm`, no math plugin, so `$$…$$` and `$…$` display as literal source.

### R1-11 · data-residency · `DATA-B4`

**Surface** `http_api/storage` (backend endpoint) · **Severity** `polish`
(recorded as `P3` in the old table)

Cross-org key access returned 500 instead of failing closed. Now 404.

### R1-12 · data-residency · `DATA-F2`

**Surface** `/dashboard/{org}/settings/data-residency` · **Severity** `polish`
(recorded as `note` in the old table)

Default SSL mode `require` fails the probe against a no-TLS local ParadeDB
with a cryptic TLS error; set `disable` for the throwaway container (plan
updated).

### R1-13 · data-residency · `DATA-F5`

**Surface** `/dashboard/{org}/settings/data-residency` · **Severity** `polish`
(recorded as `note` in the old table)

The plan claimed a "Move existing files" backfill button in the panel — none
exists; the backfill is operator-CLI-only today
(`startObjectStorageBlobBackfill` has no app caller). Plan corrected; UI gap
tracked as an issue.

### R1-14 · data-residency · `DATA-B5`

**Surface** `convex/rag/lib/indexing_service.ts` · **Severity** `polish`
(recorded as `note` in the old table)

A genuine 100 MB text file (57 k chunks) indexes to completion on BOTH
internal `_storage` AND external S3+ParadeDB, but the single inline action
peaks at ~1.5 GB executor RSS and OOM-false-fails on the first try under
concurrent load; **Retry** dedups to Indexed in ~13 s. Corrected the false
"peak memory stays flat regardless of document size" `prepareDocument`
docstring (embed phase is bounded; prepare/chunk phase still scales) and the
manual-plan coverage row, both cross-linking `#2752`. No code-behaviour
change.

### R1-15 · governance · `GOV-F16`

**Surface** `…/settings/governance/guardrails` · **Severity** `polish`
(recorded as `low` in the old table)

Three checkboxes in the PII **Detection patterns** list are all visibly
labeled "Sensitive" — indistinguishable duplicate labels.

### R1-16 · governance · `GOV-F14`

**Surface** `…/settings/governance/policies-limits` · **Severity** `polish`
(recorded as `low` in the old table)

Opening the retention edit drawer / diff dialog logs Radix "Missing
`Description` or `aria-describedby` for DialogContent" warnings.

### R1-17 · conversations · `CONV-F9, CONV-F10`

**Surface** `/dashboard/{org}/conversations/open` · **Severity** `docs`
(recorded as `resolved` in the old table) · **Evidence**
``scratchpad/shots/conversations/probe-open.png``

**RESOLVED — status transitions persist.** The audit-genesis denial (the
`auditLogChainGenesis` insert being RLS-denied inside a user mutation) is off
the write path: close/reopen/spam/read and every `bulk_*` route their audit
through the raw internal `createAuditLog` (`audit_logs/emit.ts` → an
`internalMutation` with raw ctx in `internal_mutations.ts`), and
`conversations/status_transitions_rls.test.ts` (#1972) is a green regression
driving the REAL RLS + audit chain. Caveat: the regression proves an _editor_;
a plain _member_'s permission is separate

## Not reached

Everything. See the scope above.

## Verdict

The suites are the tests and this is the history — the split the shared shape
exists for. The next round starts from a clean journal and judges the tree
rather than the paperwork.
