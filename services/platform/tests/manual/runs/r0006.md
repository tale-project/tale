# R6 — Document folder views after the recovery rebase

| | |
| --- | --- |
| **Round** | R6 |
| **Date** | 2026-09-14 |
| **Tree** | `e6cb3b2e9`, rebased onto `4f7b426371`, with the accompanying docs and capture changes |
| **Stack** | Platform 3000, backend 3005, isolated Postgres 5547, object storage 59000 |
| **Browser** | Playwright Chromium |
| **Viewports** | EN 1440×900; DE/FR 390×900; screenshot manifest dimensions |
| **State** | Existing synthetic organization; retained data, with the review folder renamed Operations |
| **Tester** | Coding agent |

## Scope

Verify the incoming fix that lists unfiled documents at the Hub root. This is a
focused continuation of R5, not a new full-platform baseline. The incoming CLI
recovery behavior is covered by executable command and deployment fixtures.

## Baseline

The prior published head had six successful CI workflows. After the rebase, the
repository check passed all 51 tasks. The task's backend was restarted from the
new source; the capture runner renewed its rejected saved session by signing the
existing synthetic owner back in. No organization or database was reset.

## What was exercised

| Surface | Result | Scope |
| --- | --- | --- |
| `KNOW-F2` folder browsing | Pass | The Operations folder is visible at the root while its `review.txt` file is absent. Opening the folder shows that file and excludes the root’s onboarding checklist. Reload and browser Back retain the correct view in all three language/viewport cases. Folder creation, moving and deletion are not claimed in this round. |
| Native document list | Pass | Unfiltered read returns 14 documents; explicit unfiled scope returns 13; the selected folder returns one. Exact filename checks distinguish `review.txt` from `q2-support-review.txt`. |
| REST folder parameters | Pass | 198 focused service/REST tests plus the document adapter test verify the root sentinel, omitted filters, project scope and existing document behavior. |
| Library filters and captures | Pass | Drove the real Indexed and Upload filters; recaptured the document list at 1440×540 and both document dialogs. Source images were opened and reviewed. |
| `KNOW-F2` import entry | Pass | Opened From Microsoft 365 before connecting an account and inspected the first-use dialog. No external authorization or import was performed. |

No browser page errors occurred in the three folder cases. The native app list
uses an empty `folderId` for the unfiled scope; the documented REST API uses
`folderId=root`. These are separate route contracts.

## Findings

| ID | Severity | Box | Finding and resolution |
| --- | --- | --- | --- |
| R6-1 | `docs` | `KNOW-F2` | The suite said the Microsoft 365 menu entry was both always present and conditional on an existing account. The action menu and live first-use dialog confirm it is available before connection. Removed the contradictory expectation. |

## Verdict

The observed folder behavior matches the revised EN/DE/FR guides. The manual
expectation is corrected; the incoming service and REST regressions remain intact.
The screenshot fixture preserves all existing documents. No production data,
external cloud account, or video workflow was exercised.
