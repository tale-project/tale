'use server';

export async function registerUser(
  _formData: FormData,
): Promise<{ success: boolean; error?: string }>
{
  // Temporary stub for development/testing. Replace with real implementation.
  return { success: false, error: 'registerUser is not implemented in this environment.' };
}

