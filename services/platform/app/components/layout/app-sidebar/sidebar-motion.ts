/** Shortcut chip rendered inside the rail's hover tooltips (and the Home
 * panel's New chat tooltip), e.g. the `⌥ ⌘ N` beside "New chat". */
export const TOOLTIP_SHORTCUT_CLASS =
  'text-muted bg-muted-foreground/60 ml-3 rounded-sm px-1 py-0.5 text-xs';
