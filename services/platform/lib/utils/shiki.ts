// Re-exported from @tale/ui so existing call sites keep their import
// path while the canonical singleton (and language list) lives in the
// shared package.
export { highlightCode, resolveLanguage } from '@tale/ui/markdown/shiki';
