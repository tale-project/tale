/* tslint:disable */

/**
 * Product-status enum vocabulary shared by the product dialogs.
 */

// Product status types
export enum PRODUCT_STATUS {
  Active = 'active',
  Inactive = 'inactive',
  Draft = 'draft',
  Archived = 'archived',
}

export type ProductStatus = PRODUCT_STATUS;
