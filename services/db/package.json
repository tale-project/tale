{
  "name": "@tale/db",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "setup": "echo 'Docker service - no local setup'",
    "dev": "docker compose up db -d && docker compose logs -f db",
    "build": "echo 'No build step'",
    "docker:build": "docker compose build db",
    "logs": "docker compose logs -f db",
    "shell": "docker exec -it tale-db psql -U tale -d tale"
  }
}
