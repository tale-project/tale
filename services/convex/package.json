{
  "name": "@tale/convex",
  "version": "0.1.0",
  "private": true,
  "description": "Tale Convex backend image — self-hosted Convex API/HTTP-actions + dashboard",
  "scripts": {
    "setup": "echo 'Docker service - no local setup'",
    "serve": "docker compose up convex -d && docker compose logs -f convex",
    "build": "echo 'No build step'",
    "docker:build": "docker compose build convex",
    "logs": "docker compose logs -f convex",
    "shell": "docker exec -it tale-convex sh"
  }
}
