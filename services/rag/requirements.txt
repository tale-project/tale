# FastAPI and server dependencies
# fastapi, uvicorn, pydantic, and pydantic-settings versions are determined by cognee dependency

# Cognee - RAG framework
# Includes baml extras for structured output
cognee[baml]==0.5.1

# Additional dependencies for cognee
# Note: openai version is determined by cognee's dependencies
# langchain versions are determined by cognee's dependencies

# Enable document support in unstructured
# - xlsx: Excel files (.xls, .xlsx)
# - docx: Word documents (.doc, .docx)
# - pptx: PowerPoint presentations (.ppt, .pptx)
# Note: PDF support removed - now handled by PyMuPDF + Vision API
unstructured[xlsx,docx,pptx]==0.18.26

# PyMuPDF for PDF text extraction (no system dependencies)
# Replaces tesseract/poppler with lightweight pure-Python solution
PyMuPDF==1.26.7

# Database drivers (for cognee storage and PGVector)
asyncpg==0.31.0
psycopg2-binary==2.9.11
pgvector==0.4.2

# Utilities
python-multipart==0.0.20
python-dotenv==1.2.1
loguru==0.7.3
httpx==0.28.1
aiofiles==25.1.0
tenacity==9.1.2

