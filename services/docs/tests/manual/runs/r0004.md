# R4 — Review incoming content after rebasing the docs PR

| | |
| --- | --- |
| **Round** | R4 |
| **Date** | 2026-09-14 |
| **Tree** | Docs overhaul rebased onto `e3220e06363bca04bab8e201cd18fc9d1f6333eb` |
| **Stack** | Fresh production docs on 3202; current isolated platform on 3000 |
| **Browser** | Playwright Chromium |
| **Viewports** | 1440px and 390px; README gallery 1100px and 390px |
| **State** | Synthetic fixture organization and repository content |
| **Tester** | Coding agents with independent native-language review |

## Scope and baseline

The prior full review covered 381 non-video pages. This pass reconciled the
21 incoming articles and their wider reader journeys with the new main:
API contract 1.9.0, terminal indexing and crawler limits, provider network
requirements, remembered navigation and separate documentation origins.
A second incoming change consolidated the UI packages and replaced the docs shell.
Its contributor guidance, package READMEs, shared message overlays and authoring
contracts received the same review. The new component site has its own manual
record. All product-guide changes have equivalent German and French guidance. Earlier
manual records remain unchanged; videos and embeds remain outside scope.

The rebase retained the prior published tree and an exact three-way combined
tree before replay. The combined tree was restored before editorial conflict
resolution, preserving the earlier README gallery, skills and product fixes.

## What was exercised

| Surface | Evidence |
| --- | --- |
| Content and locales | Source-based review of every incoming article and related guides, plus independent native-language reads. Unsupported promises were replaced with exact prerequisites, observable results and recovery. |
| Running platform | Navigation, Swagger links, provider settings, document indexing and conditional document writes were driven on the rebased code. The platform’s R5 records those observations and fixes. |
| Images | Six README source images retaken through the manifest; all source frames visually inspected. Added a readable terminal-indexing dialog capture from a real empty-file fixture. |
| Docs navigation and accessibility | EN/DE/FR at 1440px and 390px: first Tab, skip-link activation, native drawer labels, keyboard trap, scroll lock, Escape/focus restoration, section selection and theme persistence. Light/dark samples were visually inspected. |
| README gallery | Six locale/viewport cases loaded every thumbnail and full-resolution target without document overflow. The two-column, three-row table was retained. |

## Findings

| ID | Severity | Finding | Resolution |
| --- | --- | --- | --- |
| R4-1 | `docs` | Incoming contract prose and older navigation instructions needed the same editorial and native-language treatment as the existing overhaul. | Integrated the verified facts into task guides and reference tables, preserving API limitations, separate deployment boundaries and complete locale structure. |
| R4-2 | `bug` | The new article could not receive skip-link focus. The rail also moved Chromium's sequential focus start past the skip link. | Restore the focusable article target and scroll only the rail container. Six browser regressions failed before the fix and passed afterward. |
| R4-3 | `bug` | Section and back-to-top controls ignored reduced motion. | Request instant scrolling when reduced motion is enabled. Two regressions failed before the fix; all ten focused tests passed afterward. Live probes observed instant requests. |

## Verification limits

Automated corpus traversal checks rendering, images, headings and links; it is
not a claim that every pixel received a human visual judgment. Live platform
observations use an isolated OSS stack and deterministic model responses.
Enterprise certifications remain based on the owner’s confirmation. Production
commercial operations and video content are not claimed as tested.

## Verdict

The production build, 263 documentation tests and nine prerender tests passed.
All 762 page/width cases passed, with current source hashes, loaded images and no
document overflow or runtime errors. All 5,120 rendered internal links resolved.
The README review passed all six gallery cases and 550 local targets across
48 README/support files. All 185 video-related files and 54 embeds remain
unchanged. The PR-wide repository gate is recorded with the final integration.
