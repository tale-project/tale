# R1 — the findings the guides were carrying when they became suites

| | |
|---|---|
| **Round** | R1 |
| **Date** | 2026-09-08 |
| **Tree** | the commit that adopted the shared manual-test shape |
| **Stack** | — (no round was driven) |
| **Tester** | — |

## Scope

**This record was not produced by a round.** It exists because the guides
these suites came from each carried an **Issues Found** table, and a suite is
not a place findings live. Every row that still had content was moved here
verbatim on 2026-09-08, so nothing was lost when the tables went away.

Nothing below has been re-driven. Treat each entry as a claim made by whoever
filed it, on the date their description names — the first real round is what
confirms or closes it. A finding that is fixed gets its closing note in
**that** round's record, not here.

## What was exercised

Nothing. 100 boxes across 6 suites were carried over unrun; the first round's
record is where a result appears.

## Findings

1 bug · 1 polish — carried over, not re-driven. Severities are the shared
[rubric](../readme.md#severity-rubric); the old tables used their own scales
(`crit/high/med/low`, `P1/P2/P3`), and each entry records what it said.

| ID | Severity | Box | What |
|---|---|---|---|
| R1-1 | `bug` | `LOC-F2` | Remaining hard-coded English chrome on localized pages: the code **Copy code**/**Copied** button (`@tale/ui` `highlighted-code.tsx`) and the heading **Copy link to this section** ( … |
| R1-2 | `polish` | `NAV-B1` | ~~The styled 404 works (suggestions correctly offer **Platform / Chat / Basics** — 2026-07-06 live pass), but the document title stays **Tale documentation** instead of reflecting |

### R1-1 · locale · `LOC-F2`

**Surface** `https://tale.dev/docs/de/self-hosted/install/quickstart` ·
**Severity** `bug` (recorded as `med` in the old table)

Remaining hard-coded English chrome on localized pages: the code **Copy
code**/**Copied** button (`@tale/ui` `highlighted-code.tsx`) and the heading
**Copy link to this section** (`@tale/ui` `anchored-heading.tsx`). Skip link,
sidebar landmark, and page actions are i18n-wired. Breadcrumbs/TOC are
correctly German (**Brotkrumennavigation**, **Auf dieser Seite**). Extend the
audit to the newer chrome: the video player's captions `<track>` label
(hard-coded language-name map in `@tale/ui` `video.tsx` —
correct-by-construction per locale, but re-check if locales grow) and the PWA
update banner/offline toast (`pwa.*` — translated in `de.yml`/`fr.yml`; verify
it renders localized when it fires)

### R1-2 · navigation · `NAV-B1`

**Surface** `https://tale.dev/docs/platform/chat/basicz` · **Severity**
`polish` (recorded as `low` in the old table) · **Evidence** `Tale**,
`docs.notFoundTitle`) and `noindex`; the shared server (`@tale/ui/server`)
serves it with a real HTTP 404 for any unknown path once `dist/404/index.html`
exists (verified via `tests/prerender/seo.test.ts`). Re-verify live once the
docs container probe
(`services/platform/tests/integration/container-docs-test.ts`) asserts the
HTTP status — not yet added.`

~~The styled 404 works (suggestions correctly offer **Platform / Chat /
Basics** — 2026-07-06 live pass), but the document title stays **Tale
documentation** instead of reflecting **Page not found**; the HTTP status is
also 200 (SPA fallback)~~ **RESOLVED (#2620):** `scripts/prerender.ts` now
prerenders `/404` with the real `<title>` (**Page not found \

## Not reached

Everything. See the scope above.

## Verdict

The suites are the tests and this is the history — the split the shared shape
exists for. The next round starts from a clean journal and judges the tree
rather than the paperwork.
