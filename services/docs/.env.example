
# Optional Sentry-compatible error metadata, configured at runtime for both
# browser and server. Empty disables all reporting. No visitor/form data.
SENTRY_DSN=
SENTRY_ENVIRONMENT=production
TALE_VERSION=dev
