/**
 * English terminology config. No half-compounds in source-language prose;
 * the calques list is reserved for future use.
 */

import type { LocaleTerminologyConfig } from '../types';

export const TERMINOLOGY_EN: LocaleTerminologyConfig = {
  halfCompounds: [],
  calques: [],
};
