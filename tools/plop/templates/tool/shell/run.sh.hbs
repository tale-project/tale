#!/usr/bin/env bash
set -euo pipefail

# {{description}}
# Wrapped as a workspace script so it can be invoked via:
#   bun run --filter @tale/{{kebabCase name}} run

echo "TODO: implement {{kebabCase name}}"
