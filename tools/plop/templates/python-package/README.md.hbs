# tale-{{kebabCase name}}

{{description}}

```bash
bun run --filter @tale/{{kebabCase name}} lint
bun run --filter @tale/{{kebabCase name}} format
```

Source-only Python package — installed as an editable workspace dep by services that consume it.
