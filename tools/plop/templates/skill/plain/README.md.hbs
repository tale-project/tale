# {{kebabCase name}}

{{description}}

A plain (docs-only) skill — its guidance lives entirely in [`SKILL.md`](SKILL.md);
there is no bundled code. If it ships to product agents, register it in
`tools/skills/src/manifest.ts` and run `bun run skills:sync`.
