#!/usr/bin/env bun
/**
 * {{kebabCase name}} — example skill script.
 *
 * Self-contained: imports only `node:*` / `bun` builtins (and the global `Bun`
 * API) so it runs wherever the skill is installed, with no node_modules. The
 * `skills:check` guard enforces that. Invoke it from the skill directory:
 *
 *   bun scripts/main.ts [name]
 */

const target = process.argv[2] ?? 'world';
await Bun.write(Bun.stdout, `hello ${target} from {{kebabCase name}}\n`);
