import { describe, expect, it } from 'bun:test';

import { greet } from './main';

describe('{{kebabCase name}}', () => {
  it('greets the given target', () => {
    expect(greet('world')).toBe('hello world from {{kebabCase name}}');
  });
});
