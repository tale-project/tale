{
  "name": "@tale/skills",
  "version": "0.1.0",
  "private": true,
  "description": "Skill source-of-truth sync + cross-harness adapter generator (skills/ → .claude/skills, builtin-configs/skills, .cursor, .github/instructions)",
  "bin": {
    "tale-skills": "./src/index.ts"
  },
  "type": "module",
  "scripts": {
    "lint": "bunx oxlint",
    "lint:fix": "bunx oxlint --fix",
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "1.3.11",
    "typescript": "6.0.2"
  }
}
