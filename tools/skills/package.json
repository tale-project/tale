{
  "name": "@tale/skills",
  "version": "0.1.0",
  "private": true,
  "description": "Skill mirror sync (.agents/skills → .claude/skills) + portability guards for shipped skills (builtin-configs/skills, skills/)",
  "bin": {
    "tale-skills": "./src/index.ts"
  },
  "type": "module",
  "scripts": {
    "lint": "bunx oxlint",
    "lint:fix": "bunx oxlint --fix",
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "yaml": "2.8.3"
  },
  "devDependencies": {
    "@types/bun": "1.3.11",
    "typescript": "6.0.2"
  }
}
