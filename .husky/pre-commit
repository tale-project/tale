# Staged-file formatting + fast SAST via lint-staged (config: package.json).
# lint-staged owns the partially-staged-file problem the old hand-rolled
# grep/xargs/git-add pipeline got wrong (it silently staged unstaged hunks).
bunx lint-staged
