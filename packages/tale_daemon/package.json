{
  "name": "tale-daemon",
  "version": "0.1.0",
  "private": true,
  "description": "Tale external runtime daemon: runs task work dispatched to local coding-agent CLIs (Claude Code, Codex, OpenCode) and reports back.",
  "bin": {
    "tale-daemon": "./src/cli.ts"
  },
  "type": "module",
  "scripts": {
    "lint": "bunx oxlint",
    "lint:fix": "bunx oxlint --fix",
    "typecheck": "bunx tsc --noEmit",
    "test": "bunx vitest --run"
  },
  "devDependencies": {
    "@types/node": "24.10.1",
    "vitest": "4.1.8"
  }
}
