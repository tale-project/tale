// Barrel: prefer subpath imports (e.g. `@tale/webui/seo/document-meta`).
// Kept for type re-exports only. Markdown lives in `@tale/ui/markdown`.
export type { RegionalLocale, SupportedLocale } from '@tale/ui/i18n/locales';
