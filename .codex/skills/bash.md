# bash

<!-- Generated by `bun run skills:sync` (tools/skills) from the skill SKILL.md + .claude/skill-globs.json — do not edit by hand. -->

Conventions for shell scripts in this repo — strict mode (`set -euo pipefail`), quote every expansion, array argv, `trap` cleanup/signals, no `eval`, redact secrets before logging, `[[ ]]` tests, loud env-var checks, idempotent/re-entrant entrypoints, shellcheck- and opengrep-clean.

Read the [`bash` skill](../../.claude/skills/bash/SKILL.md) in full and follow it — it is the canonical guide. This file only routes Codex to it.
