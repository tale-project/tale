# release

<!-- Generated by `bun run skills:sync` (tools/skills) from the skill SKILL.md + .claude/skill-globs.json — do not edit by hand. -->

How to cut a tagged release — push a `vX.Y.Z` tag that triggers the automated Release pipeline (`.github/workflows/release.yml`), which builds the multi-arch service images, the GitHub release, and the CLI binaries.

Read the [`release` skill](../../.claude/skills/release/SKILL.md) in full and follow it — it is the canonical guide. This file only routes Codex to it.
