---
description: Conventions for shell scripts in this repo — strict mode (`set -euo pipefail`), quote every variable, `trap` cleanup, no `eval`, redact secrets before logging, `[[ ]]` tests, loud env-var checks, idempotent/re-entrant scripts, shellcheck-clean.
globs: **/*.sh,**/Dockerfile*,**/*entrypoint*.sh
alwaysApply: false
---

# bash

<!-- Generated by .claude/gen-skill-adapters.mjs from .claude/skills/ — do not edit by hand; edit the SKILL.md or .claude/skill-globs.json and re-run the generator. -->

When working on matching code, read the [`bash` skill](../../.claude/skills/bash/SKILL.md) in full and follow it — it is the canonical guide. This file only routes Cursor to it.
