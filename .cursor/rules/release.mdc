---
description: How to cut a tagged release — push a `vX.Y.Z` tag that triggers the automated Release pipeline (`.github/workflows/release.yml`), which builds the multi-arch service images, the GitHub release, and the CLI binaries.
alwaysApply: false
---

# release

<!-- Generated by `bun run skills:sync` (tools/skills) from the skill SKILL.md + .claude/skill-globs.json — do not edit by hand. -->

When this guide applies (Cursor surfaces this rule by its description above), read the [`release` skill](../../.claude/skills/release/SKILL.md) in full and follow it — it is the canonical guide. This file only routes Cursor to it.
