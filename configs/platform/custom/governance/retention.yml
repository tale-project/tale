_metadata:
  envPrefix: TALE_RETENTION_
  envNames:
    FILES_MIN: documents.min
    FILES_MAX: documents.max
    FILES_DEFAULT: documents.default
    USER_TEMP_MIN: userTempHours.min
    USER_TEMP_MAX: userTempHours.max
    USER_TEMP_DEFAULT: userTempHours.default
    AGENT_TEMP_MIN: agentTempHours.min
    AGENT_TEMP_MAX: agentTempHours.max
    AGENT_TEMP_DEFAULT: agentTempHours.default
    CONVERSATIONS_MIN: chatHistory.min
    CONVERSATIONS_MAX: chatHistory.max
    CONVERSATIONS_DEFAULT: chatHistory.default
    AUDIT_MIN: auditLog.min
    AUDIT_MAX: auditLog.max
    AUDIT_DEFAULT: auditLog.default
    EXECUTIONS_MIN: workflowLog.min
    EXECUTIONS_MAX: workflowLog.max
    EXECUTIONS_DEFAULT: workflowLog.default
    ANALYTICS_MIN: usageLedger.min
    ANALYTICS_MAX: usageLedger.max
    ANALYTICS_DEFAULT: usageLedger.default
    LOGIN_ATTEMPTS_MIN: loginAttempt.min
    LOGIN_ATTEMPTS_MAX: loginAttempt.max
    LOGIN_ATTEMPTS_DEFAULT: loginAttempt.default
    CHAT_FILTER_MIN: chatFilterEvents.min
    CHAT_FILTER_MAX: chatFilterEvents.max
    CHAT_FILTER_DEFAULT: chatFilterEvents.default
    FEEDBACK_MIN: messageFeedback.min
    FEEDBACK_MAX: messageFeedback.max
    FEEDBACK_DEFAULT: messageFeedback.default
    CONTACTS_MIN: contacts.min
    CONTACTS_MAX: contacts.max
    CONTACTS_DEFAULT: contacts.default
    INBOX_MIN: externalConversations.min
    INBOX_MAX: externalConversations.max
    INBOX_DEFAULT: externalConversations.default
    NOTIFICATIONS_MIN: notifications.min
    NOTIFICATIONS_MAX: notifications.max
    NOTIFICATIONS_DEFAULT: notifications.default
    AGENT_RUNS_MIN: agentRuns.min
    AGENT_RUNS_MAX: agentRuns.max
    AGENT_RUNS_DEFAULT: agentRuns.default
documents:
  min: 30
  max: 3650
  default: 365
  unit: days
userTempHours:
  min: 1
  max: 720
  default: 24
  unit: hours
agentTempHours:
  min: 1
  max: 720
  default: 24
  unit: hours
chatHistory:
  min: 1
  max: 3650
  default: 90
  unit: days
auditLog:
  min: 365
  max: 3650
  default: 730
  unit: days
workflowLog:
  min: 1
  max: 365
  default: 30
  unit: days
usageLedger:
  min: 30
  max: 3650
  default: 365
  unit: days
loginAttempt:
  min: 90
  max: 365
  default: 90
  unit: days
chatFilterEvents:
  min: 1
  max: 365
  default: 90
  unit: days
messageFeedback:
  min: 30
  max: 3650
  default: 365
  unit: days
contacts:
  min: 30
  max: 3650
  default: 730
  unit: days
externalConversations:
  min: 30
  max: 3650
  default: 730
  unit: days
notifications:
  min: 1
  max: 365
  default: 30
  unit: days
agentRuns:
  min: 30
  max: 400
  default: 180
  unit: days
